# round393 社区关注成功与帖子作者资料入口补齐

## 1. 本轮背景
- 承接 `round392`。
- 上轮已经完成：
  - `社区个人主页 -> 最近发布 -> 帖子详情 -> 返回个人主页` 真机闭环；
  - 社区个人主页与帖子详情页头部均已稳定从 `y=36` 起始；
  - 深层页统一淡蓝方形返回按钮未回退。
- 当前继续推进的重点是：
  - 真实验证社区“关注/取消关注”链路是否能在平板与电脑后端联通条件下成功；
  - 补齐更自然的“他人资料页”进入路径，避免真机验收长期依赖开发联调入口。

## 2. 本轮目标
- 在真实平板上验证帖子详情中的关注按钮是否能成功写回；
- 继续确认这不是“假网络错误”而是真实可用链路；
- 若现有页面缺少自然入口，则只做最小代码补齐；
- 把本轮真实边界、布局与顶部安全区结论写入文档。

## 3. 本轮真机执行与结果

### 3.1 帖子详情关注作者成功
- 起始证据：
  - `D:\ZeroIsle_Notes\.local\android-mcp-server\round393_postdetail_entry.xml`
  - `D:\ZeroIsle_Notes\.local\android-mcp-server\round393_postdetail_entry.png`
- 现场结论：
  - 当前前台已命中 `screen.community.postDetail`；
  - 页面头部继续从 `y=36` 起始；
  - `action.community.postDetail.back` 仍是统一淡蓝方形箭头返回按钮；
  - 关注按钮在点击前显示为 `关注`。

- 点击关注后的证据：
  - `D:\ZeroIsle_Notes\.local\android-mcp-server\round393_follow_after_tap.xml`
  - `D:\ZeroIsle_Notes\.local\android-mcp-server\round393_follow_after_tap.png`
- 现场结论：
  - `action.community.postDetail.follow` 已从 `关注` 切换为 `已关注`；
  - 说明平板与电脑后端当前联通条件下，社区关注接口真实可用；
  - 本轮没有出现默认安卓网络弹窗，也没有出现“无网络”误判；
  - 结合 logcat 中的社区接口成功日志，可写实判定“关注成功链路已通过真机验证”。

### 3.2 当前阻碍不是网络，而是自然入口不足
- 结合真机 XML 与源码核对，本轮确认：
  - 帖子详情页虽然已有关注按钮，但作者名、评论作者此前都只是纯文本；
  - 这会导致“帖子详情 -> 他人资料页 -> 关注状态复核”这条链路在真实页面上不自然；
  - 也意味着社区功能本身不是没通，而是缺少一个合理的资料页进入入口。

## 4. 本轮代码调整

### 4.1 只补齐帖子详情中的作者资料入口
- 修改文件：
  - `D:\ZeroIsle_Notes\src\screens\community\PostDetailScreen.js`
  - `D:\ZeroIsle_Notes\src\redux\slices\communitySlice.js`
- 修改原则：
  - 不重做成熟的帖子详情布局；
  - 不改既有头部、统计区、评论区视觉结构；
  - 只补真机验收所缺的自然入口。

- 实际改动：
  - 给帖子作者区补 `action.community.postDetail.author`；
  - 给评论作者补 `action.community.postDetail.commentAuthor.*`；
  - 给回复作者补 `action.community.postDetail.replyAuthor.*`；
  - 评论与回复数据映射中增加 `authorId`，确保点击时能稳定导航到 `UserProfile`；
  - 其余页面风格、顶部安全区与按钮体系保持不动。

### 4.2 单测结果
- 已执行：
  - `yarn test --runTestsByPath src/redux/slices/__tests__/communitySlice.test.js`
- 结果：
  - `PASS`

## 5. 本轮补证边界与写实说明
- 在作者资料入口补齐后，尝试继续做“帖子详情作者 -> 个人主页”的真机补证；
- 过程中出现两类前台噪声：
  - 一次抓证据超时；
  - 一次应用热更新/重载后被系统带回桌面；
  - 重新拉起后又先出现根容器过渡态，随后回到首页。

- 这些现象说明：
  - 当前阻碍的是取证过程的前台稳定性，而不是作者入口代码已经明确失败；
  - 不能把“热更新把前台带回首页/桌面”误记成“作者资料页打不开”；
  - 下一轮应在更稳的单线程点按节奏下，从社区重新进入帖子详情，再补 `作者 -> 个人主页` 的终态截图/XML。

## 6. 本轮布局与顶部规范结论
- 本轮继续确认以下规范未回退：
  - 帖子详情页头部起点仍是 `y=36`；
  - 顶部没有被平板状态栏遮挡；
  - 深层页继续统一使用已有淡蓝方形箭头返回按钮；
  - 本轮代码没有引入新的异常顶部留白；
  - 没有新增默认安卓网络弹窗。

## 7. 本轮结论
- 已明确通过：
  - `帖子详情 -> 关注作者成功`；
  - 当前平板与电脑后端联通环境下，社区关注接口可真实写回；
  - 帖子详情页顶部安全区、统一返回按钮和合理留白未回退。

- 已完成代码补齐、待下一轮补终态证据：
  - `帖子详情作者 -> 个人主页`
  - `帖子详情评论作者 -> 个人主页`

## 8. 下一轮建议重点
- 从社区重新进入帖子详情，补：
  - `作者 -> 个人主页`
  - `评论作者 -> 个人主页`
  - `个人主页中的关注状态与帖子详情关注状态是否联动`
- 继续保持：
  - 顶部安全区从 `y=36` 起始
  - 统一淡蓝方形返回按钮
  - 合理留白
  - 网络异常时只使用项目内统一样式弹层
