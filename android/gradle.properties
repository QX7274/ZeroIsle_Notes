# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# Default value: -Xmx512m -XX:MaxMetaspaceSize=256m
org.gradle.jvmargs=-Xmx8192m -XX:MaxMetaspaceSize=2048m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8 -XX:+UseG1GC -XX:MaxGCPauseMillis=200

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
org.gradle.parallel=false

# 启用Gradle构建缓存
org.gradle.caching=true

# 启用Gradle守护进程
org.gradle.daemon=true

# 配置按需配置
org.gradle.configureondemand=true

# 启用离线模式，使用本地缓存
# 暂时禁用离线模式，因为我们需要下载依赖
org.gradle.offline=false

# 缓存目录配置
org.gradle.dependency.verification.console=verbose

# 禁用依赖验证
org.gradle.dependency.verification=off

# 添加代理设置
# systemProp.http.proxyHost=127.0.0.1
# systemProp.http.proxyPort=7897
# systemProp.https.proxyHost=127.0.0.1
# systemProp.https.proxyPort=7897

# 设置超时时间
systemProp.http.connectionTimeout=300000
systemProp.http.socketTimeout=300000
systemProp.https.connectionTimeout=300000
systemProp.https.socketTimeout=300000

# 优先使用 IPv4，规避某些网络环境下的 IPv6 连接问题
systemProp.java.net.preferIPv4Stack=true


# 添加更多镜像源
systemProp.maven.url.google=https://maven.aliyun.com/repository/google
systemProp.maven.url.central=https://maven.aliyun.com/repository/central
systemProp.maven.url.jcenter=https://maven.aliyun.com/repository/jcenter
systemProp.maven.url.public=https://maven.aliyun.com/repository/public

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true
# 自动将Android Support库转换为AndroidX
android.enableJetifier=true

# Use this property to specify which architecture you want to build.
# You can also override it from the CLI using
# ./gradlew <task> -PreactNativeArchitectures=x86_64
reactNativeArchitectures=armeabi-v7a,arm64-v8a,x86,x86_64

# Use this property to enable support to the new architecture.
# This will allow you to use TurboModules and the Fabric render in
# your application. You should enable this flag either if you want
# to write custom TurboModules/Fabric components OR use libraries that
# are providing them.
newArchEnabled=false

# Use this property to enable or disable the Hermes JS engine.
# If set to false, you will be using JSC instead.
hermesEnabled=true

# 指定react-native-camera使用的变体
reactNativeCameraVariant=general

# CMake 配置
# 使用新的推荐配置方式替代已弃用的选项
# android.defaults.buildfeatures.buildconfig=true
# 改为在 app/build.gradle 中使用 android.buildFeatures.buildConfig = true
android.buildFeatures.prefab=true
# 虽然 prefabVersion 是实验性的，但目前仍需要它
# 这个警告是正常的，可以忽略：WARNING: The option setting 'android.prefabVersion=2.0.0' is experimental.
android.prefabVersion=2.0.0

# 指定使用的架构
reactNativeArchitectures=armeabi-v7a,arm64-v8a,x86,x86_64

# 忽略CMake警告
android.cmake.ignoreUnusedVariables=true
