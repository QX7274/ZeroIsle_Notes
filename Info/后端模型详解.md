# 零屿笔记（ZeroIsle Notes）后端模型详解

## 1. 笔记核心模型 (notes/models.py)

### 1.1 Category (分类)

笔记分类模型，用于对笔记进行分类管理。

```python
class Category(models.Model):
    name = models.CharField(max_length=50, verbose_name='分类名称')
    user = models.ForeignKey(User, on_delete=models.CASCADE, related_name='categories', verbose_name='用户')
    color = models.CharField(max_length=7, default='#000000', verbose_name='颜色')
    created_at = models.DateTimeField(default=timezone.now, verbose_name='创建时间')
    updated_at = models.DateTimeField(auto_now=True, verbose_name='更新时间')
    
    class Meta:
        verbose_name = '分类'
        verbose_name_plural = verbose_name
        unique_together = ('name', 'user')
```

主要字段：
- `name`: 分类名称
- `user`: 所属用户
- `color`: 分类颜色，用于UI显示
- `created_at`: 创建时间
- `updated_at`: 更新时间

### 1.2 Tag (标签)

标签模型，用于给笔记添加标签。

```python
class Tag(models.Model):
    name = models.CharField(max_length=50, verbose_name='标签名称', unique=True)
    color = models.CharField(max_length=7, default='#000000', verbose_name='颜色')
    user = models.ForeignKey(User, on_delete=models.CASCADE, related_name='tags', verbose_name='用户')
    created_at = models.DateTimeField(default=timezone.now, verbose_name='创建时间')
    updated_at = models.DateTimeField(auto_now=True, verbose_name='更新时间')
    
    class Meta:
        verbose_name = '标签'
        verbose_name_plural = verbose_name
        unique_together = ('name', 'user')
```

主要字段：
- `name`: 标签名称
- `color`: 标签颜色，用于UI显示
- `user`: 所属用户
- `created_at`: 创建时间
- `updated_at`: 更新时间

### 1.3 Note (笔记)

笔记模型，存储笔记的核心内容。

```python
class Note(models.Model):
    title = models.CharField(max_length=200, verbose_name='标题')
    content = models.TextField(verbose_name='内容')
    user = models.ForeignKey(User, on_delete=models.CASCADE, related_name='notes', verbose_name='用户')
    category = models.ForeignKey(Category, on_delete=models.SET_NULL, null=True, blank=True, related_name='notes', verbose_name='分类')
    tags = models.ManyToManyField(Tag, blank=True, related_name='notes', verbose_name='标签')
    is_pinned = models.BooleanField(default=False, verbose_name='是否置顶')
    is_archived = models.BooleanField(default=False, verbose_name='是否归档')
    created_at = models.DateTimeField(default=timezone.now, verbose_name='创建时间')
    updated_at = models.DateTimeField(auto_now=True, verbose_name='更新时间')
    
    class Meta:
        verbose_name = '笔记'
        verbose_name_plural = verbose_name
        ordering = ['-is_pinned', '-updated_at']
```

主要字段：
- `title`: 笔记标题
- `content`: 笔记内容
- `user`: 所属用户
- `category`: 所属分类
- `tags`: 关联的标签（多对多关系）
- `is_pinned`: 是否置顶
- `is_archived`: 是否归档
- `created_at`: 创建时间
- `updated_at`: 更新时间

### 1.4 NoteVersion (笔记版本)

笔记版本历史模型，用于记录笔记的历史版本。

```python
class NoteVersion(models.Model):
    note = models.ForeignKey(Note, on_delete=models.CASCADE, related_name='versions', verbose_name='笔记')
    title = models.CharField(verbose_name='标题', max_length=200)
    content = models.TextField(verbose_name='内容')
    created_at = models.DateTimeField(verbose_name='创建时间', auto_now_add=True)
    
    class Meta:
        verbose_name = '笔记版本'
        verbose_name_plural = '笔记版本'
        ordering = ['-created_at']
```

主要字段：
- `note`: 关联的笔记
- `title`: 版本标题
- `content`: 版本内容
- `created_at`: 版本创建时间

### 1.5 NoteAttachment (笔记附件)

笔记附件模型，用于存储笔记的附件文件。

```python
class NoteAttachment(models.Model):
    note = models.ForeignKey(Note, on_delete=models.CASCADE, related_name='attachments', verbose_name='笔记')
    file = models.FileField(upload_to='note_attachments/%Y/%m/%d/', verbose_name='文件')
    file_name = models.CharField(verbose_name='文件名', max_length=255)
    file_size = models.IntegerField(verbose_name='文件大小(字节)')
    file_type = models.CharField(verbose_name='文件类型', max_length=100)
    created_at = models.DateTimeField(verbose_name='上传时间', auto_now_add=True)
    
    class Meta:
        verbose_name = '笔记附件'
        verbose_name_plural = '笔记附件'
        ordering = ['-created_at']
```

主要字段：
- `note`: 关联的笔记
- `file`: 附件文件
- `file_name`: 文件名
- `file_size`: 文件大小
- `file_type`: 文件类型
- `created_at`: 上传时间

### 1.6 NoteShare (笔记分享)

笔记分享模型，用于记录笔记的分享信息。

```python
class NoteShare(models.Model):
    note = models.ForeignKey(Note, on_delete=models.CASCADE, related_name='shares', verbose_name='笔记')
    shared_by = models.ForeignKey(User, on_delete=models.CASCADE, related_name='shared_notes', verbose_name='分享者')
    shared_with = models.ForeignKey(User, on_delete=models.CASCADE, related_name='received_notes', verbose_name='接收者')
    can_edit = models.BooleanField(default=False, verbose_name='是否可以编辑')
    created_at = models.DateTimeField(default=timezone.now, verbose_name='创建时间')
    updated_at = models.DateTimeField(auto_now=True, verbose_name='更新时间')
    is_accepted = models.BooleanField(default=False, verbose_name='是否接受')

    class Meta:
        verbose_name = '笔记分享'
        verbose_name_plural = verbose_name
        unique_together = ('note', 'shared_with')
```

主要字段：
- `note`: 被分享的笔记
- `shared_by`: 分享者
- `shared_with`: 接收者
- `can_edit`: 接收者是否可以编辑
- `created_at`: 创建时间
- `updated_at`: 更新时间
- `is_accepted`: 接收者是否接受分享

### 1.7 NoteSync (笔记同步)

笔记同步状态模型，用于记录笔记在不同设备上的同步状态。

```python
class NoteSync(models.Model):
    note = models.ForeignKey(Note, on_delete=models.CASCADE, related_name='sync_records', verbose_name='笔记')
    device_id = models.CharField(verbose_name='设备ID', max_length=100)
    last_synced = models.DateTimeField(verbose_name='最后同步时间')
    version = models.IntegerField(verbose_name='版本号', default=1)
    is_conflict = models.BooleanField(verbose_name='是否存在冲突', default=False)
    conflict_resolved = models.BooleanField(verbose_name='冲突是否已解决', default=False)
    
    class Meta:
        verbose_name = '笔记同步状态'
        verbose_name_plural = '笔记同步状态'
        unique_together = ['note', 'device_id']
        ordering = ['-last_synced']
```

主要字段：
- `note`: 关联的笔记
- `device_id`: 设备ID
- `last_synced`: 最后同步时间
- `version`: 版本号
- `is_conflict`: 是否存在冲突
- `conflict_resolved`: 冲突是否已解决

### 1.8 OCR相关模型

OCR模型和训练数据模型，用于手写识别功能。

```python
class OCRModel(models.Model):
    name = models.CharField(max_length=100, verbose_name='模型名称')
    version = models.CharField(max_length=50, verbose_name='模型版本')
    model_path = models.CharField(max_length=255, verbose_name='模型路径')
    is_active = models.BooleanField(default=True, verbose_name='是否激活')
    created_at = models.DateTimeField(auto_now_add=True, verbose_name='创建时间')
    updated_at = models.DateTimeField(auto_now=True, verbose_name='更新时间')
    
    class Meta:
        verbose_name = 'OCR模型'
        verbose_name_plural = 'OCR模型'

class OCRTrainingData(models.Model):
    image = models.ImageField(upload_to='ocr_training/', verbose_name='训练图片')
    text = models.TextField(verbose_name='标注文本')
    model = models.ForeignKey(OCRModel, on_delete=models.CASCADE, related_name='training_data', verbose_name='所属模型')
    created_at = models.DateTimeField(auto_now_add=True, verbose_name='创建时间')
    
    class Meta:
        verbose_name = 'OCR训练数据'
        verbose_name_plural = 'OCR训练数据'
```

### 1.9 Whisper相关模型

Whisper模型和训练数据模型，用于语音识别功能。

```python
class WhisperModel(models.Model):
    name = models.CharField(max_length=100, verbose_name='模型名称')
    version = models.CharField(max_length=50, verbose_name='模型版本')
    model_path = models.CharField(max_length=255, verbose_name='模型路径')
    is_active = models.BooleanField(default=True, verbose_name='是否激活')
    created_at = models.DateTimeField(auto_now_add=True, verbose_name='创建时间')
    updated_at = models.DateTimeField(auto_now=True, verbose_name='更新时间')
    
    class Meta:
        verbose_name = 'Whisper模型'
        verbose_name_plural = 'Whisper模型'

class WhisperTrainingData(models.Model):
    audio = models.FileField(upload_to='whisper_training/', verbose_name='训练音频')
    text = models.TextField(verbose_name='标注文本')
    model = models.ForeignKey(WhisperModel, on_delete=models.CASCADE, related_name='training_data', verbose_name='所属模型')
    created_at = models.DateTimeField(auto_now_add=True, verbose_name='创建时间')
    
    class Meta:
        verbose_name = 'Whisper训练数据'
        verbose_name_plural = 'Whisper训练数据'
```

## 2. 用户认证模型 (users/models.py)

### 2.1 User (用户)

用户模型，继承自Django的AbstractUser，添加了额外的字段。

```python
class User(AbstractUser):
    phone = models.CharField(max_length=20, blank=True, null=True, verbose_name='手机号')
    avatar = models.ImageField(upload_to='avatars/', blank=True, null=True, verbose_name='头像')
    bio = models.TextField(blank=True, null=True, verbose_name='个人简介')
    is_verified = models.BooleanField(default=False, verbose_name='是否验证')
    last_login_ip = models.GenericIPAddressField(blank=True, null=True, verbose_name='最后登录IP')
    
    class Meta:
        verbose_name = '用户'
        verbose_name_plural = verbose_name
```

主要字段：
- `username`: 用户名（继承自AbstractUser）
- `email`: 邮箱（继承自AbstractUser）
- `phone`: 手机号
- `avatar`: 头像
- `bio`: 个人简介
- `is_verified`: 是否验证
- `last_login_ip`: 最后登录IP

### 2.2 ThirdPartyAccount (第三方账号)

第三方账号模型，用于存储用户的第三方账号绑定信息。

```python
class ThirdPartyAccount(models.Model):
    PLATFORM_CHOICES = (
        ('wechat', '微信'),
        ('qq', 'QQ'),
        ('weibo', '微博'),
    )
    
    user = models.ForeignKey(User, on_delete=models.CASCADE, related_name='third_party_accounts', verbose_name='用户')
    platform = models.CharField(max_length=20, choices=PLATFORM_CHOICES, verbose_name='平台')
    open_id = models.CharField(max_length=100, verbose_name='OpenID')
    access_token = models.CharField(max_length=255, verbose_name='访问令牌')
    refresh_token = models.CharField(max_length=255, blank=True, null=True, verbose_name='刷新令牌')
    expires_at = models.DateTimeField(blank=True, null=True, verbose_name='过期时间')
    created_at = models.DateTimeField(auto_now_add=True, verbose_name='创建时间')
    updated_at = models.DateTimeField(auto_now=True, verbose_name='更新时间')
    
    class Meta:
        verbose_name = '第三方账号'
        verbose_name_plural = verbose_name
        unique_together = ('platform', 'open_id')
```

主要字段：
- `user`: 关联的用户
- `platform`: 平台（微信、QQ、微博等）
- `open_id`: 平台提供的OpenID
- `access_token`: 访问令牌
- `refresh_token`: 刷新令牌
- `expires_at`: 令牌过期时间
- `created_at`: 创建时间
- `updated_at`: 更新时间

## 3. 知识图谱模型 (knowledge_graph/models.py)

### 3.1 KnowledgeNode (知识节点)

知识节点模型，表示知识图谱中的节点。

```python
class KnowledgeNode(models.Model):
    NODE_TYPE_CHOICES = (
        ('concept', '概念'),
        ('entity', '实体'),
        ('event', '事件'),
        ('topic', '主题'),
    )
    
    title = models.CharField(max_length=100, verbose_name='标题')
    description = models.TextField(blank=True, null=True, verbose_name='描述')
    node_type = models.CharField(max_length=20, choices=NODE_TYPE_CHOICES, verbose_name='节点类型')
    user = models.ForeignKey(User, on_delete=models.CASCADE, related_name='knowledge_nodes', verbose_name='用户')
    notes = models.ManyToManyField('notes.Note', blank=True, related_name='knowledge_nodes', verbose_name='相关笔记')
    created_at = models.DateTimeField(auto_now_add=True, verbose_name='创建时间')
    updated_at = models.DateTimeField(auto_now=True, verbose_name='更新时间')
    
    class Meta:
        verbose_name = '知识节点'
        verbose_name_plural = verbose_name
```

主要字段：
- `title`: 节点标题
- `description`: 节点描述
- `node_type`: 节点类型（概念、实体、事件、主题）
- `user`: 所属用户
- `notes`: 关联的笔记（多对多关系）
- `created_at`: 创建时间
- `updated_at`: 更新时间

### 3.2 KnowledgeRelation (知识关系)

知识关系模型，表示知识图谱中节点之间的关系。

```python
class KnowledgeRelation(models.Model):
    RELATION_TYPE_CHOICES = (
        ('is_a', '是一种'),
        ('part_of', '是部分'),
        ('related_to', '相关于'),
        ('cause', '导致'),
        ('precede', '先于'),
        ('follow', '后于'),
        ('custom', '自定义'),
    )
    
    source = models.ForeignKey(KnowledgeNode, on_delete=models.CASCADE, related_name='outgoing_relations', verbose_name='源节点')
    target = models.ForeignKey(KnowledgeNode, on_delete=models.CASCADE, related_name='incoming_relations', verbose_name='目标节点')
    relation_type = models.CharField(max_length=20, choices=RELATION_TYPE_CHOICES, verbose_name='关系类型')
    custom_name = models.CharField(max_length=50, blank=True, null=True, verbose_name='自定义关系名称')
    description = models.TextField(blank=True, null=True, verbose_name='关系描述')
    user = models.ForeignKey(User, on_delete=models.CASCADE, related_name='knowledge_relations', verbose_name='用户')
    created_at = models.DateTimeField(auto_now_add=True, verbose_name='创建时间')
    updated_at = models.DateTimeField(auto_now=True, verbose_name='更新时间')
    
    class Meta:
        verbose_name = '知识关系'
        verbose_name_plural = verbose_name
        unique_together = ('source', 'target', 'relation_type', 'user')
```

主要字段：
- `source`: 源节点
- `target`: 目标节点
- `relation_type`: 关系类型（是一种、是部分、相关于等）
- `custom_name`: 自定义关系名称
- `description`: 关系描述
- `user`: 所属用户
- `created_at`: 创建时间
- `updated_at`: 更新时间

## 4. 提醒功能模型 (reminder/models.py)

### 4.1 Reminder (提醒)

提醒模型，用于设置笔记相关的提醒。

```python
class Reminder(models.Model):
    REPEAT_CHOICES = (
        ('none', '不重复'),
        ('daily', '每天'),
        ('weekly', '每周'),
        ('monthly', '每月'),
        ('custom', '自定义'),
    )
    
    title = models.CharField(max_length=100, verbose_name='标题')
    description = models.TextField(blank=True, null=True, verbose_name='描述')
    note = models.ForeignKey('notes.Note', on_delete=models.CASCADE, related_name='reminders', verbose_name='关联笔记')
    user = models.ForeignKey(User, on_delete=models.CASCADE, related_name='reminders', verbose_name='用户')
    remind_at = models.DateTimeField(verbose_name='提醒时间')
    repeat = models.CharField(max_length=20, choices=REPEAT_CHOICES, default='none', verbose_name='重复')
    custom_repeat = models.CharField(max_length=100, blank=True, null=True, verbose_name='自定义重复')
    is_completed = models.BooleanField(default=False, verbose_name='是否完成')
    created_at = models.DateTimeField(auto_now_add=True, verbose_name='创建时间')
    updated_at = models.DateTimeField(auto_now=True, verbose_name='更新时间')
    
    class Meta:
        verbose_name = '提醒'
        verbose_name_plural = verbose_name
        ordering = ['remind_at']
```

主要字段：
- `title`: 提醒标题
- `description`: 提醒描述
- `note`: 关联的笔记
- `user`: 所属用户
- `remind_at`: 提醒时间
- `repeat`: 重复类型（不重复、每天、每周、每月、自定义）
- `custom_repeat`: 自定义重复规则
- `is_completed`: 是否完成
- `created_at`: 创建时间
- `updated_at`: 更新时间

## 5. 社区功能模型 (community/models.py)

### 5.1 Post (帖子)

帖子模型，用于社区功能中的帖子。

```python
class Post(models.Model):
    title = models.CharField(max_length=200, verbose_name='标题')
    content = models.TextField(verbose_name='内容')
    user = models.ForeignKey(User, on_delete=models.CASCADE, related_name='posts', verbose_name='用户')
    note = models.ForeignKey('notes.Note', on_delete=models.SET_NULL, blank=True, null=True, related_name='posts', verbose_name='关联笔记')
    tags = models.ManyToManyField('notes.Tag', blank=True, related_name='posts', verbose_name='标签')
    view_count = models.IntegerField(default=0, verbose_name='浏览次数')
    like_count = models.IntegerField(default=0, verbose_name='点赞次数')
    comment_count = models.IntegerField(default=0, verbose_name='评论次数')
    is_pinned = models.BooleanField(default=False, verbose_name='是否置顶')
    created_at = models.DateTimeField(auto_now_add=True, verbose_name='创建时间')
    updated_at = models.DateTimeField(auto_now=True, verbose_name='更新时间')
    
    class Meta:
        verbose_name = '帖子'
        verbose_name_plural = verbose_name
        ordering = ['-is_pinned', '-created_at']
```

主要字段：
- `title`: 帖子标题
- `content`: 帖子内容
- `user`: 发布用户
- `note`: 关联的笔记
- `tags`: 关联的标签
- `view_count`: 浏览次数
- `like_count`: 点赞次数
- `comment_count`: 评论次数
- `is_pinned`: 是否置顶
- `created_at`: 创建时间
- `updated_at`: 更新时间

### 5.2 Comment (评论)

评论模型，用于社区功能中的评论。

```python
class Comment(models.Model):
    content = models.TextField(verbose_name='内容')
    user = models.ForeignKey(User, on_delete=models.CASCADE, related_name='comments', verbose_name='用户')
    post = models.ForeignKey(Post, on_delete=models.CASCADE, related_name='comments', verbose_name='帖子')
    parent = models.ForeignKey('self', on_delete=models.CASCADE, blank=True, null=True, related_name='replies', verbose_name='父评论')
    like_count = models.IntegerField(default=0, verbose_name='点赞次数')
    created_at = models.DateTimeField(auto_now_add=True, verbose_name='创建时间')
    updated_at = models.DateTimeField(auto_now=True, verbose_name='更新时间')
    
    class Meta:
        verbose_name = '评论'
        verbose_name_plural = verbose_name
        ordering = ['-created_at']
```

主要字段：
- `content`: 评论内容
- `user`: 评论用户
- `post`: 关联的帖子
- `parent`: 父评论（用于回复功能）
- `like_count`: 点赞次数
- `created_at`: 创建时间
- `updated_at`: 更新时间

### 5.3 Like (点赞)

点赞模型，用于记录用户的点赞行为。

```python
class Like(models.Model):
    CONTENT_TYPE_CHOICES = (
        ('post', '帖子'),
        ('comment', '评论'),
    )
    
    user = models.ForeignKey(User, on_delete=models.CASCADE, related_name='likes', verbose_name='用户')
    content_type = models.CharField(max_length=20, choices=CONTENT_TYPE_CHOICES, verbose_name='内容类型')
    post = models.ForeignKey(Post, on_delete=models.CASCADE, blank=True, null=True, related_name='likes', verbose_name='帖子')
    comment = models.ForeignKey(Comment, on_delete=models.CASCADE, blank=True, null=True, related_name='likes', verbose_name='评论')
    created_at = models.DateTimeField(auto_now_add=True, verbose_name='创建时间')
    
    class Meta:
        verbose_name = '点赞'
        verbose_name_plural = verbose_name
        unique_together = [
            ('user', 'content_type', 'post'),
            ('user', 'content_type', 'comment'),
        ]
```

主要字段：
- `user`: 点赞用户
- `content_type`: 内容类型（帖子、评论）
- `post`: 关联的帖子
- `comment`: 关联的评论
- `created_at`: 创建时间

## 6. 搜索功能模型 (search/models.py)

### 6.1 SearchHistory (搜索历史)

搜索历史模型，用于记录用户的搜索历史。

```python
class SearchHistory(models.Model):
    user = models.ForeignKey(User, on_delete=models.CASCADE, related_name='search_history', verbose_name='用户')
    query = models.CharField(max_length=200, verbose_name='搜索关键词')
    created_at = models.DateTimeField(auto_now_add=True, verbose_name='搜索时间')
    
    class Meta:
        verbose_name = '搜索历史'
        verbose_name_plural = verbose_name
        ordering = ['-created_at']
```

主要字段：
- `user`: 搜索用户
- `query`: 搜索关键词
- `created_at`: 搜索时间

### 6.2 SearchIndex (搜索索引)

搜索索引模型，用于存储搜索索引信息。

```python
class SearchIndex(models.Model):
    CONTENT_TYPE_CHOICES = (
        ('note', '笔记'),
        ('knowledge_node', '知识节点'),
        ('post', '帖子'),
    )
    
    content_type = models.CharField(max_length=20, choices=CONTENT_TYPE_CHOICES, verbose_name='内容类型')
    object_id = models.IntegerField(verbose_name='对象ID')
    title = models.CharField(max_length=200, verbose_name='标题')
    content = models.TextField(verbose_name='内容')
    user = models.ForeignKey(User, on_delete=models.CASCADE, related_name='search_indices', verbose_name='用户')
    created_at = models.DateTimeField(auto_now_add=True, verbose_name='创建时间')
    updated_at = models.DateTimeField(auto_now=True, verbose_name='更新时间')
    
    class Meta:
        verbose_name = '搜索索引'
        verbose_name_plural = verbose_name
        unique_together = ('content_type', 'object_id')
```

主要字段：
- `content_type`: 内容类型（笔记、知识节点、帖子）
- `object_id`: 对象ID
- `title`: 标题
- `content`: 内容
- `user`: 所属用户
- `created_at`: 创建时间
- `updated_at`: 更新时间
