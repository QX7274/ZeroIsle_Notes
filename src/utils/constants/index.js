/**
 * 常量索引
 */
import * as colors from './colors';
import * as config from './config';
import * as dimensions from './dimensions';

export {
  colors,
  config,
  dimensions,
};

export default {
  colors,
  config,
  dimensions,
};
