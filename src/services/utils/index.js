/**
 * 工具服务模块导出
 */

import { eventEmitter } from './eventEmitter';
import { logService } from '../../utils/logService';

export {
  eventEmitter,
  logService
};

export default {
  eventEmitter,
  logService
};
