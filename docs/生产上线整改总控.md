# 生产上线整改总控

> 唯一执行入口：本文件是当前生产上线整改、功能审查、环境准备、真机验证与发布判断的唯一总控入口。  
> 配套台账：[DOC_GAP_TRACKER](D:/ZeroIsle_Notes/docs/DOC_GAP_TRACKER.md)  
> 历史映射：[ARCHIVE_TO_GAP_MAPPING](D:/ZeroIsle_Notes/docs/ARCHIVE_TO_GAP_MAPPING.md)  
> 当前基线提交：`a1931d5`，已进入 `main`

## 1. 目标与成功标准

### 1.1 总目标
- 把仓库从“可开发、可演示”推进到“可审查、可验证、可发布上线”的工程状态。
- 先解决阻塞上线的安全、部署、配置、CI、测试环境、移动端可测试性问题，再进入功能审查与 Android 真机平板验证。
- 在整改过程中同步完成软件瘦身，删除无用、过时、可再生噪声文件，但不删除规划功能本身，不删除未完成功能的规划与实现依据。

### 1.2 成功标准
- 所有整改项、功能审查项、真机验证项都登记在 `docs/DOC_GAP_TRACKER.md`。
- 代码、文档、验证证据、Git 记录四者同步闭环，不允许出现“代码改了但文档和验证缺失”。
- 每个可独立验收的修复完成后，必须立即更新文档、提交、推送；提交标题与正文全部使用中文，且正文至少写清“问题、原因、修改、验证、风险”。
- Android 专用 MCP/插件现已就位并接入真实平板，但在覆盖范围、证据完整性与阻塞项清零前，仍不得宣称已完成 Android 真机平板验证。
- 软件瘦身只允许删除缓存、编译产物、临时报告、测试转储、可再生 dump 等无用资产；不得删除规划功能、实现计划、模块状态文档、未完成需求资产。
- 环境准备完成后，必须使用工具开展功能审查与测试，判断功能是否按规划完成、是否足够完整上线、体验是否顺畅、响应是否足够快。

## 2. 固定执行规则

### 2.1 Git 规则
- 基线提交 `a1931d5` 作为当前生产整改起点，已直接进入 `main`。
- 后续每个整改批次默认使用 `QX/上线整改-批次XX-主题` 分支。
- 每完成一个独立修复，就立即更新总控文档、GAP 台账、验证记录，并执行中文提交与推送。
- 所有提交标题、提交正文、文档回填、推送说明全部使用中文，内容必须详细精确。
- 任何未经过文档登记、未绑定提交哈希、未附验证结果的修复，都不计入“已完成”。
- 当前执行分支固定记录为 `QX/上线整改-批次05-功能审查与路由通知修复`，最近三次已推送提交为 `8c8c1a6`、`419e2d9`、`7f2a8d1`；后续文档回填与真机证据必须以该现场快照为当前起点。
- `docs/adapter-pattern.md` 当前属于用户未提交工作区文件，禁止纳入任何整改提交、补丁或推送，除非用户单独明确授权。

### 2.2 环境规则
- Python 统一使用 Conda `ZeroIsle` 环境。
- 当前已定位到本机 Conda 根目录为 `D:\APP\Anaconda`，系统 `PATH` 尚未暴露 `conda` 命令。
- Python 相关操作统一通过 `D:\APP\Anaconda\condabin\conda.bat` 或 `D:\APP\Anaconda\Scripts\conda.exe` 进入项目环境后执行，不使用系统默认 `python`。
- 当前可发现的项目环境实际目录名为 `Zeroisle`，文档中保留“ZeroIsle”作为项目规范名时，必须补充说明“实际 Conda env 名称以本机环境列表为准”。
- 推荐激活方式：`& 'D:\APP\Anaconda\condabin\conda.bat' activate Zeroisle`
- 推荐直接执行方式：`& 'D:\APP\Anaconda\Scripts\conda.exe' run -n Zeroisle python --version`
- Token 经济规则统一纳入 RTK：后续 shell 搜索、读文件、diff、git 查看、测试输出默认优先使用 `rtk` 前缀，避免原始长输出持续消耗上下文。
- 本机 RTK 规范来源为 `C:\Users\QX\.codex\RTK.md`，本地工具目录已确认存在于 `D:\rtk`。
- 后续优先命令示例：
  - `rtk read <file>`
  - `rtk grep "<pattern>" .`
  - `rtk find "<glob>" .`
  - `rtk git status`
  - `rtk git diff`
  - `rtk pytest -q`
  - `rtk npm test`
- 在 PowerShell 下不再优先使用原始 `Get-Content`、`git diff`、`git status`、`npm test` 等长输出命令做常规巡检；仅在 RTK 不能满足场景时回退，并在执行日志中说明原因。
- 前端依赖安装统一使用 `yarn install`。
- Android 前端运行统一使用 `yarn android`。
- 仓库根目录的 `yarn install` / `yarn android` 仅对应 React Native 主应用。
- `admin_system/frontend` 是独立后台前端子项目，不受根目录 `yarn install` 自动覆盖；如需运行该子项目，必须进入对应目录单独安装与启动。
- 不再把 `npm install`、`npm run android` 作为默认文档命令；若排障时临时使用，必须在日志中说明原因与范围。

### 2.3 软件瘦身规则
- 允许删除：
  - `__pycache__`、`.pyc`、Gradle/pytest 本地缓存
  - 临时测试输出、Compose 临时日志、Android 工具盘点文本
  - 审计结果缓存、UI dump、可再生 XML/JSON/TXT 报告
- 禁止删除：
  - 规划中未完成功能对应的源码、测试、设计、说明文档
  - `archive/*`、`module-status/*`、`optimization-docs/*`、`PENDING_WORK_GUIDE.md` 等仍需参考的规划资产
  - 当前总控文档、GAP 台账、历史映射、实现计划与模块功能核查记录
- 每次瘦身必须先登记删除对象、删除原因、是否可再生、验证方式、影响范围与风险。
- 删除完成后必须验证不会影响当前规划功能与后续实施路径。
- Android MCP、ADB、Detox、功能审查相关证据文件在对应 GAP 未关闭前不得删除，至少保留 `png/xml/logcat/txt` 证据链到对应条目完成复核。

### 2.4 功能审查规则
- 环境准备完成后，立即使用可用工具开展功能审查与测试。
- 功能审查至少回答三个问题：
  - 规划功能是否已经实现
  - 功能是否足够完整，能否支持上线使用
  - 使用体验是否良好，速度是否可接受
- React Native 侧体验整改继续沿用现有 Redux Toolkit 主线，重点是收口复杂业务态边界、减少组件影子状态与双写状态，不引入“从零改造 RTK”的返工。
- UI 体验整改默认遵守蓝白、现代、轻玻璃风格；真机点击链相关页面必须优先补稳定 `testID`、更大的热区、诚实页内提示与无后端阶段的优雅降级。
- 当前尚未部署到服务器，默认不以公网前后端互通作为阶段验收前提。
- 在未部署阶段，默认使用手机热点或更稳定的同局域网方案，让电脑与平板处于同一网络，以便进行安装、联调、页面检查与日志采集。

## 3. 批次计划

| 批次 | 名称 | 目标 | 退出条件 |
|---|---|---|---|
| 0 | 基线与入口收敛 | 冻结基线、统一执行入口、收敛历史口径 | 总控文档、GAP 台账、历史映射稳定可用 |
| 1 | 安全整改 | 清理真实凭据、危险默认值、远程连接兜底 | P0 安全阻塞项全部入账并完成首轮整改 |
| 2 | 瘦身与环境准备 | 删除无用/过时文件，统一 Conda、yarn、局域网方案 | 首轮瘦身完成，环境与命令规范落地 |
| 3 | 部署与配置整改 | 统一入口、健康检查、依赖就绪检查、启动顺序、配置契约 | 部署链路文档化并完成最小演练 |
| 4 | CI 与测试可执行性整改 | 流水线 hard-fail、生效测试环境、移动端 smoke 能力 | 关键检查 hard-fail 生效，测试环境可执行 |
| 5 | 功能审查与真机验证 | 审查规划功能完成度、体验、性能，并完成 Android 真机平板验证 | 形成上线可用性结论与阻塞清单 |

## 4. 责任智能体

| 智能体 | 责任范围 | 交付要求 |
|---|---|---|
| 总控智能体 | 批次拆分、文档维护、状态核对、Git 规范检查、风险升级 | 保证文档与代码状态一致 |
| 后端/安全智能体 | 密钥治理、环境变量契约、Mongo/Redis/Neo4j、测试环境修复 | 输出安全与配置整改结论 |
| 瘦身与环境智能体 | 无用文件清理、Conda/yarn 标准化、局域网联调准备 | 输出删除清单、环境清单与风险 |
| 部署/CI 智能体 | Docker、Compose、ASGI 入口、健康检查、流水线 | 输出部署链路与 hard-fail 结果 |
| 移动端/Android 智能体 | `testID`、Detox、Gradle、ADB、平板前置准备 | 输出 smoke、安装链路与真机前置 |
| 验证智能体 | 工具化功能审查、回归、证据整理、上线判断 | 输出可上线/待整改/待真机结论 |

## 5. 当前已完成进展
- 基线提交已完成并推送到 `main`：`a1931d5`
- 批次 1 已完成首轮整改并形成独立提交：`c7b162d`
- 批次 2 已完成首轮瘦身与环境准备基线收敛提交：`1d96221`
- 已建立总控文档、GAP 台账和 `archive` 映射三件套
- 已完成首轮安全清理：
  - 移除示例环境文件和测试脚本中的真实 Mongo 连接串
  - 移除 `mongodb_realm_service.py` 中默认远程连接串
  - 将 Neo4j 默认密码收紧为空
- 已完成首轮部署与健康检查收敛：
  - 新增 `/ready/`
  - `/health/` 改为轻量 liveness
  - Dockerfile 与 Compose 统一到 ASGI 主线
- 已完成首轮 CI 与移动可测试性收敛：
  - `mypy`、`yarn lint`、`yarn bundle:verify` 改为 hard-fail
  - 补齐 Detox smoke 用例和关键页面 `testID`

## 6. 当前阻塞与风险

| 类型 | 状态 | 说明 | 下一步 |
|---|---|---|---|
| Android 真机验证 | IN_PROGRESS | Android MCP 工具链已接入 `.local/android-mcp-server`，真实设备 `HGR3Y9MA / TB128FU` 已可连接并产出 UI dump、截图与 logcat；`round20` 红框已定位并通过 `metro.config.js` 收口 Windows watch mode 风险，当前 Metro `http://127.0.0.1:8081/status` 已恢复 `200 packager-status:running`，`index.bundle` 也已恢复 `200`，约 `2431ms` 返回、长度约 `38911423`。真机 `round22` 已重新确认首页、我的页、提醒列表页、提醒新增页主体可见；`round27` 已确认 reminder 离线创建后回到 `screen.reminderList` 且新增本地提醒 `round27localok`；`round29` 已确认完成按钮链不再被本地旧快照覆盖，已完成分组中可见 `round27localok`；`round30` 通过精确确认弹窗按钮坐标后已确认删除链闭环，`round30_delete_committed.xml` 中 `round27localok` 已消失，logcat 同步出现 `save_offline_reminder_delete` 记账成功；`round31` 已确认启停按钮链在远端失败时会离线记账 `save_offline_reminder_update`，并且列表项 `content-desc` 与真机 UI 中都能显式看到“已暂停”状态，说明启停链已从“业务可用但感知弱”收口到“业务与视觉都可感知”。`round32` 已补齐首页 -> 我的 -> 提醒页的继续复测：`round32_profile_dump.xml` 与 `round32_reminder_before.xml` 已确认提醒页在离线视图下能稳定恢复本地提醒 `round24_local_live`、`round26_local_live`，说明列表本地回退主链已能优先展示本地真相源；同时本轮又修正了 `ReminderListView` 的同步成功提示，改为读取 `syncReminders().unwrap()` 返回的真实 `synced` 数量，不再继续沿用调用前的旧 `unsyncedCount`。通知侧新增 Firebase 初始化失败时的诚实降级日志，避免继续把“已降级为本地通知”误记成“Firebase 初始化成功”，但这仍只属于开发态噪声收口，不等同于推送链已验收。当前 reminder 主风险已进一步收敛为“未同步数量真机提示仍待在联网场景复测、更深层同步回放风险、hooks 依赖遗留告警、共享链真机验证待补” | 保持 `yarn android -> adb reverse tcp:8081 tcp:8081 -> Android MCP/ADB 复测` 默认流程，下一步继续补 reminder 的联网同步提示 round32/33 复测与共享链真机验证 |
| 后端测试环境 | IN_PROGRESS | `manage.py check --settings=backend.settings.testing` 已恢复通过；`mongodb_realm_service`、`document_converter`、`notification` 本轮继续完成惰性初始化收敛；仍需继续清理其余启动期重初始化与真实依赖噪声 | 继续按模块清理可选路由与服务初始化副作用 |
| 路由历史问题 | IN_PROGRESS | `code_editor` 已补齐 `/api/v1/code/*` 外部挂载与历史 `code.*` 兼容别名，前端 `CodeRunner` 对应的 `src/services/codeService.js` 服务别名也已回补；本轮已进一步将 `snippets/executions` 的最小持久化链收敛到 Mongo 用户与 Mongo 文档口径，但更深层 CRUD、筛选、真实前后端联调仍需继续验证 | 继续扩展 `code_editor` 的列表/详情/更新/删除与筛选验证，确认不再残留 Django ORM 语义假设 |
| 群组/协作专项 | IN_PROGRESS | 已推进到第十刀：前四刀已完成外部契约、Mongo 用户映射、详情展示口径与共享信令地址收口；第五刀已完成邀请成员搜索化；第六至第八刀已完成共享观看三态、切房/回调解绑、状态隔离、联调可观测性与本地 `shared-screen-lab` 诊断壳；第九刀已打通共享 HTTP 最小 smoke；第十刀已收口“先连房后推流”时既有 PeerConnection 不补轨的前置阻塞 | 下一步继续做 WebRTC 端到端联调、暂停/恢复链路补齐、共享会话全局状态建模，并在 Android 专用 MCP 就位后补真机可视化证据 |
| 通知用户映射口径 | IN_PROGRESS | `notification` 批量已读、未读数、全部删除与对象权限判断已统一到 Mongo 用户映射，但当前只完成最小契约验证，通知列表、单条读写和更深层真实调用链仍未完成回归 | 继续扩展通知模块接口回归用例，并结合后续功能审查确认前后端调用链一致性 |
| 可选功能入口稳定性 | IN_PROGRESS | 已修复 `mind_map`、`document_converter`、`personal_activity` 首轮导入阻塞；仍需继续审查其他模块级服务初始化 | 进入功能审查前继续做启动期副作用清理 |
| 前端工具链可执行性 | IN_PROGRESS | Detox/Jest 入口、attached-device Gradle 构建链与 Android `androidTest` 产物已打通；attached-device smoke 已在真实平板上跑通 7/7，当前剩余工作已转为 Android 专用 MCP 真机可视化验证、功能完整性审查与体验评估 | 维持 attached-device smoke 通过状态，并等待 Android 专用 MCP/插件接管真机可视化验证 |
| Token 上下文消耗 | IN_PROGRESS | 当前多轮整改已进入“文档 + 多工具 + 多验证”高上下文阶段，需要统一采用 RTK 压缩输出，避免在搜索、读文件、diff、测试日志上反复浪费 token | 将 RTK 写入执行规则与台账，后续默认使用 `rtk` 代理常规 shell 巡检与验证命令 |
| 环境标准化 | IN_PROGRESS | 已定位 `D:\APP\Anaconda`、`conda.bat` / `conda.exe` 与 `Zeroisle` 环境；当前主要剩余问题是系统 `PATH` 未暴露 `conda`，且执行日志仍需持续统一到项目规范 | 持续按 `ZeroIsle` / `Zeroisle` 双口径规则记录执行命令并补充验证日志 |
| 仓库噪声资产 | IN_PROGRESS | 历史上已有大量 `__pycache__`、测试输出、UI dump、临时报告被纳入 Git | 在批次 2 执行首轮安全瘦身并补 `.gitignore` 兜底 |

## 7. 局域网联调方案
- 当前未部署服务器，默认不把公网前后端互通作为阶段验收前提。
- 默认方案：使用手机热点，让电脑与平板处于同一局域网下，进行 Android 安装、页面检查、功能回归和日志采集。
- 推荐补充方案：
  - 真机阶段优先使用 USB 调试，并执行 `adb reverse tcp:8081 tcp:8081` 保证 Metro 连接稳定。
  - React Native 真机默认复测顺序固定为：`yarn android` -> `adb reverse tcp:8081 tcp:8081` -> 确认本机 `8081` 为 Metro 监听 -> Android MCP/ADB 点击、截图、UI dump、logcat 取证。
  - 对多设备或平板可视化检查，可再切换到热点同局域网方案。
  - 若需要更稳定的远程调试链路，可在同一局域网下启用无线 ADB，并把连接步骤写入执行日志。
  - 本地双端联调阶段，默认先执行 `yarn local:web`，把 `http://127.0.0.1:8091/shared-screen-lab/` 作为共享链本地诊断入口；该入口只用于联调排障，不代表 React Native Web 主应用已完整补齐。
  - 当前 Android 工程 `sdk.dir` 已切到 `C:\Users\QX\AppData\Local\Android\Sdk`，命令行 `adb` 也统一来自 `C:\Users\QX\AppData\Local\Android\Sdk\platform-tools\adb.exe`；本轮已确认 attached-device 构建实际使用该用户目录 SDK/NDK（`ndkVersion=26.1.10909125`），后续如再切换路径，必须同步更新 `local.properties`、环境变量和执行日志。
  - 若浏览器或 API 需要访问本机服务，统一记录本机局域网 IP、端口、设备访问路径和热点名称。
  - `shared-screen-lab` 默认使用 `8091`，不得再占用 `8081`；若本地诊断入口或其它工具重新抢占 `8081`，必须先恢复 Metro 再继续真机复测。
- 每次联调必须记录：
  - 电脑 IP
  - 平板型号
  - Android 版本
  - 屏幕分辨率与 ABI
  - 热点来源
  - 是否可达
  - `adb.exe` 实际路径与版本
  - `sdk.dir` 实际路径
  - `adb devices -l` 输出
  - 是否执行 `adb reverse tcp:8081 tcp:8081`
  - Metro 监听端口核查结果
  - Metro 访问方式是 USB reverse 还是 `PC_IP:8081`
  - App 包名 `com.zeroisle_notes`
  - 构建类型 `debug` / `release`
  - Android 专用 MCP/插件名称、版本、设备 ID
  - 日志采集方式

## 8. 执行日志

| 日期 | 批次 | 执行动作 | 涉及 GAP | 责任智能体 | 结果 | 风险/阻塞 | 下一步 |
|---|---|---|---|---|---|---|---|
| 2026-05-11 | 0 | 建立总控文档、GAP 台账与 `archive` 映射 | GAP-BASELINE-001, GAP-BASELINE-002 | 总控智能体 | 执行入口已统一 | 仅完成文档收敛 | 开始首轮代码整改 |
| 2026-05-11 | 1/3/4 | 完成首轮安全、部署、CI、移动可测试性基线整改 | GAP-SEC-001, GAP-DEPLOY-001, GAP-CI-001, GAP-MOBILE-001 | Codex + 并行智能体 | 已完成代码修改并形成提交 `c7b162d` | Android 真机验证未开始；后端 testing 仍有 Mongo 强连接 | 继续环境准备与瘦身 |
| 2026-05-11 | 1/3/4 | 执行首轮最小验证 | GAP-DEPLOY-001, GAP-CI-001, GAP-MOBILE-001 | Codex | `npm run bundle:verify` 通过 | Django `manage.py check` 因 Mongo 强连接本地 `127.0.0.1:27017` 失败 | 在批次 2 继续清理环境与无用文件，并推进测试环境修复 |
| 2026-05-11 | 2 | 恢复 GAP 台账并对齐 `archive` 映射 | GAP-BASELINE-002 | Codex | GAP 台账已恢复，历史映射已同步新批次结构 | 仍需完成首轮瘦身落地与环境定位 | 继续盘点可删产物并核查 Conda / yarn / 局域网准备 |
| 2026-05-11 | 2 | 完成首轮瘦身候选盘点 | GAP-SLIM-001, GAP-SLIM-002 | Codex | 已识别 54 个已跟踪 `__pycache__`/`.pyc`、41 个已跟踪测试输出、37 个已跟踪 UI dump，以及多份 Android/Compose/audit 临时文件 | 这些产物历史上已被纳入 Git，删除前需确认无当前流程依赖 | 仅删除可再生产物并补 `.gitignore` 防止再次入库 |
| 2026-05-11 | 2 | 核实 Conda、yarn 与 Android 调试前置 | GAP-ENV-001, GAP-ENV-002, GAP-ENV-003 | Codex + 子智能体 | 已确认 `D:\APP\Anaconda` 为 Conda 根目录，`Zeroisle` 可执行 Python 3.10.20，根目录 `yarn android` 有效，`adb` 与 `sdk.dir` 存在双路径现象 | `conda` 未入 PATH；后台前端子项目需单独安装；真机验证前需统一 ADB/SDK 口径 | 回写文档并在本批次执行首轮安全瘦身 |
| 2026-05-11 | 2 | 执行首轮安全瘦身 | GAP-SLIM-001, GAP-SLIM-002 | Codex | 已删除未跟踪缓存、pytest/`manage_check` 日志，已从 Git 移除 54 个 `admin_system/backend/**/__pycache__/*.pyc` 并补齐 `.gitignore` 防回流规则 | 证据型 dump、audit、E2E 结果文件仍保留，待后续确认引用关系后再处理 | 进行提交前验证并形成本批次中文提交 |
| 2026-05-11 | 2 | 验证环境已具备进入下一步设备测试准备 | GAP-ENV-001, GAP-ENV-003 | Codex | `conda run -n Zeroisle python --version` 返回 `Python 3.10.20`；`adb devices -l` 已识别真实平板 `TB128FU` | Android 专用 MCP/插件尚未接入，不能据此宣称真机验证完成 | 后续在 Android 专用 MCP/插件就位后接管真实平板可视化验证 |
| 2026-05-12 | 2 | 提交批次 2 首轮瘦身与环境准备变更 | GAP-BASELINE-002, GAP-SLIM-001, GAP-SLIM-002, GAP-ENV-001, GAP-ENV-002, GAP-ENV-003 | Codex | 已形成中文提交 `1d96221`，包含文档收敛、`.gitignore` 补强、已跟踪 `.pyc` 清理与环境约束回填 | 仍未处理证据型 dump/audit/E2E 文件；Android 专用 MCP/插件尚未接入 | 推送当前分支并继续进入功能审查准备 |
| 2026-05-12 | 3 | 修复 testing 自检阻塞与首轮可选功能入口导入副作用 | GAP-TEST-001, GAP-ROUTE-001 | Codex | 已形成中文提交 `f519473`；`manage.py check --settings=backend.settings.testing` 通过；`notes` 信号单测 `2 passed`；`mind_map`、`document_converter`、`personal_activity` 首轮导入阻塞已解除 | 仍需继续清理剩余模块级服务初始化与功能入口稳定性问题 | 更新台账并继续功能审查准备 |
| 2026-05-12 | 3 | 继续清理导入即初始化副作用，压缩后续功能审查返工风险 | GAP-TEST-001, GAP-ROUTE-001 | Codex | 已形成中文提交 `ff72009`；完成 `mongodb_realm_service` 同步客户端惰性初始化、`document_converter` 服务懒加载，以及 `notification` 顶层实例化移除；`manage.py check --settings=backend.settings.testing` 再次通过，并完成最小导入验证 | 仍需继续扫描其他模块级服务单例 | 进入前端工具化功能审查准备，并继续补齐批次 3/4 的功能核查入口 |
| 2026-05-12 | 4/5 | 定位并修复前端功能审查准备阶段的首轮脚本级阻塞 | GAP-CI-001, GAP-MOBILE-001, GAP-REVIEW-001 | Codex | 已形成中文提交 `0f95407`；修复根目录 React Native/Jest/ESLint CLI 调用路径，新增 `screen.reminderList` 稳定标识，并将 smoke 中 AI/提醒链路切换为稳定入口与真实首屏断言 | `bundle:verify` 仍未形成成功产物；Detox 依赖仍缺失；release/e2e 构建下仍受 `__DEV__` 跳过登录影响 | 继续补齐 Detox 依赖、拆分 e2e/Jest 执行链，并处理登录前提与 Metro 长耗时问题 |
| 2026-05-12 | 4/5 | 接入 RTK 作为默认低 token shell 代理规则 | GAP-REVIEW-001, GAP-TOKEN-001 | Codex | 已确认 `C:\Users\QX\.codex\AGENTS.md` 引用 `C:\Users\QX\.codex\RTK.md`，并核实本地 RTK 目录位于 `D:\rtk`；已将 RTK 纳入总控规则 | 尚未完成全量命令习惯切换；历史执行记录仍混有原始 PowerShell 长输出 | 后续搜索、读文件、diff、测试巡检默认切换到 `rtk` 前缀 |
| 2026-05-12 | 4/5 | 补齐 Detox Android 最小接入骨架与页面级 smoke 锚点 | GAP-CI-001, GAP-MOBILE-001, GAP-REVIEW-001, GAP-TOKEN-001 | Codex + 子智能体 | 已补 `detox` 依赖声明、`test:e2e:jest` 独立脚本、`androidTest` 原生入口、`android.att.debug` 真机配置，以及 AI/提醒/知识图谱关键 `testID` 与 smoke 用例扩展 | 仍需执行 `yarn install`、Detox 构建与真机 attached-device 验证；`createPlaceholderLibs` 伪造 `.so` 仍是高风险项 | 先安装依赖并验证 `test:e2e:jest` / `detox build`，再决定是否单独清理伪造 `.so` 链路 |
| 2026-05-12 | 4/5 | 验证 Detox 接入链路并继续收敛 Android 构建阻塞 | GAP-CI-001, GAP-MOBILE-001, GAP-REVIEW-001 | Codex | `e2e/jest.config.js` 已切到 Detox 20 模板，`jest --config e2e/jest.config.js --listTests` 可识别 `e2e/starter.test.js`；Detox 构建已从“找不到命令/找不到配置”推进到真实 Android 构建阶段 | 当前主阻塞转为 `react-native-reanimated:buildCMakeDebug[arm64-v8a]` 链接 `libc++_shared.so` 权限失败；运行侧还要求显式提供 `ANDROID_SDK_ROOT` / `ANDROID_HOME`，且构建失败时不会产出 `app-debug-androidTest.apk` | 下一步单独处理 NDK/权限/原生构建链，并把 SDK 环境变量固定写入执行规范 |
| 2026-05-12 | 4/5 | 移除 Android 伪造 `.so` 任务并复测真实 NDK 阻塞 | GAP-CI-001, GAP-MOBILE-001, GAP-REVIEW-001 | Codex + 子智能体 | 已从 `android/app/build.gradle` 移除 `createPlaceholderLibs` 及其对 `mergeDebugNativeLibs` / `stripDebugDebugSymbols` 的挂载，清理 `android/app/build` 与 `node_modules/react-native-reanimated/android/.cxx` 后重新执行 attached-device `detox build`；结果确认当前阻塞与伪造产物无关，仍稳定收敛到 `D:\Android\Sdk\ndk\26.1.10909125\...\libc++_shared.so` 的 `Permission denied` | 当前不是 Detox 配置问题，也不是页面锚点缺口；系统层 NDK 单文件 ACL 异常仍阻塞原生构建，尚未产出 `app-debug-androidTest.apk` | 优先执行“重装同版 NDK 或迁移到用户目录 SDK”方案，再回到 attached-device 构建验证 |
| 2026-05-12 | 4/5 | 修复 attached-device 原生构建链并产出 Detox Android 产物 | GAP-CI-001, GAP-MOBILE-001, GAP-REVIEW-001 | Codex | 已统一切换到用户目录 Android SDK/NDK，补齐 `react-native-document-opener`、`@react-native-voice/voice` 的 `minSdk` 契约，新增 Skia Gradle 8.8 真实 prefab `.so` 同步步骤，并保留 Detox Kotlin 兼容补丁；`node node_modules/detox/local-cli/cli.js build -c android.att.debug` 成功，已产出 `app-debug.apk` 与 `app-debug-androidTest.apk` | 真机可视化验证仍未开始；Android 专用 MCP/插件未就位前不得宣称真机验证完成 | 进入 attached-device 安装与 smoke，等待 Android 专用 MCP/插件接管真机验证 |
| 2026-05-12 | 4/5 | 固化第三方补丁并完成真实平板 APK 安装链验证 | GAP-SLIM-001, GAP-CI-001, GAP-MOBILE-001, GAP-REVIEW-001 | Codex | 已删除无效/噪声 patch 与临时对照文件，重建 `detox`、`@shopify/react-native-skia` 最小 `patch-package` 补丁；`adb -s HGR3Y9MA install -r` 已成功安装 `app-debug.apk` 与 `app-debug-androidTest.apk`，并执行 `adb reverse tcp:8081 tcp:8081` | Android 专用 MCP/插件仍未接管可视化验证；当前只证明构建与安装链已打通，不代表页面交互和体验审查已完成 | 继续进入 attached-device smoke、功能可达性核查与 Android MCP 真机可视化验证准备 |
| 2026-05-12 | 4/5 | 收口真实平板 attached-device smoke 可达性并修复无后端联调降级问题 | GAP-CI-001, GAP-MOBILE-001, GAP-REVIEW-001 | Codex | 已完成 `HandwritingRecognitionModule` 惰性初始化、`ProfileSettings` 移除粒子背景、提醒创建页固定底部操作栏、知识图谱/知识分析页无后端空态锚点与分析入口修复；`node node_modules/detox/local-cli/cli.js test -c android.att.debug --testNamePattern="Smoke"` 在 `HGR3Y9MA` 上 7/7 通过 | Android 专用 MCP/插件仍未接入，当前仅能宣称 attached-device smoke 与基础页面可达性通过，不能宣称真机可视化验证完成 | 进入总控文档回写、中文提交推送，并等待 Android 专用 MCP/插件完成可视化验收 |
| 2026-05-12 | 5 | 收口功能审查首轮接口契约断点 | GAP-ROUTE-001, GAP-NOTIFY-001, GAP-REVIEW-001 | Codex | 已补齐 `/api/v1/code/*` 对外路由、`code.*` 历史兼容别名、`src/services/codeService.js` 前端服务别名，并将 `notification` 批量接口统一到 Mongo 用户映射；`python manage.py test notification.tests.test_views code_editor.tests.test_urls --settings=backend.settings.testing` 与 `python manage.py check --settings=backend.settings.testing` 均通过，并已形成提交 `22f0bed` | `code_editor` 的 `snippets/executions` 深层 CRUD 与真实联调仍未完全验证，通知模块也仅完成批量接口与权限判断的最小契约验证，尚未完成更深层真实调用回归 | 继续基于功能审查结果拆分 `code_editor` 持久化整改，并扩展通知/群组/语音等高风险模块的上线判断 |
| 2026-05-12 | 5 | 收口 `code_editor` 最小持久化契约混用问题 | GAP-ROUTE-001, GAP-REVIEW-001 | Codex | 已将 `code_editor` 的 `CodeSnippet` / `CodeExecution` 相关序列化、视图保存逻辑与执行服务收敛到 Mongo 用户和 Mongo 文档口径，并补充 `code_editor.tests.test_persistence_contract`；`python manage.py test code_editor.tests.test_urls code_editor.tests.test_persistence_contract --settings=backend.settings.testing` 与 `python manage.py check --settings=backend.settings.testing` 通过，并已形成提交 `1dd520c` | 本轮只证明最小持久化契约、`mongo_user` 映射和前端片段返回结构兼容已恢复，尚未覆盖完整 CRUD、复杂筛选与真实页面级联调 | 下一步继续扩展 `code_editor` 的列表/详情/更新/删除、按标签/语言筛选以及真实前端消费验证 |
| 2026-05-12 | 5 | 扩展 `code_editor` 最小 CRUD 与筛选回归覆盖 | GAP-ROUTE-001, GAP-REVIEW-001 | Codex | 已在 `code_editor.tests.test_persistence_contract` 中补齐 update、retrieve、rerun、by_tag、by_language 等最小用例；`python manage.py test code_editor.tests.test_urls code_editor.tests.test_persistence_contract --settings=backend.settings.testing` 扩展到 12 项并全部通过，`python manage.py check --settings=backend.settings.testing` 继续通过 | 本轮仍主要证明“最小 CRUD 与筛选链”具备测试兜底，不代表真实数据库联调和页面级消费已全部完成；根据并行审查结果，下一批更值得立即切入的高风险模块已转为群组/协作 | 先提交本轮验证收口，再进入群组/协作专项审查与 GAP 建档 |
| 2026-05-12 | 5 | 建立群组/协作专项台账并完成首刀外部契约修复 | GAP-GROUP-001, GAP-REVIEW-001 | Codex | 已将 `groups` 主资源改为直接挂载在 `/api/v1/groups/`，补齐 `join-by-code` / `generate-join-code` 短横线路径契约，并将 `SharedScreenViewSet.perform_create()` 改为 DRF 异常流；`python manage.py test groups.tests.test_urls groups.tests.test_view_contracts --settings=backend.settings.testing` 8 项通过，`python manage.py check --settings=backend.settings.testing` 继续通过 | 当前仅完成群组模块“首刀外部契约与共享屏幕创建校验”收口，`members__user` / `group__members__user` 这类 Mongo 成员查询链仍未修复，真实列表与权限链仍存在空结果或 500 风险 | 继续切入 `GAP-GROUP-002`，收口群组列表/共享屏幕列表的 Mongo 成员关系查询与最小权限回归 |
| 2026-05-12 | 5 | 回填群组专项首刀提交哈希 | GAP-GROUP-001 | Codex | 已将群组专项首刀修复提交 `ea3e6ab` 绑定到总控与 GAP 台账，确保代码、验证、文档、Git 记录一致 | `GAP-GROUP-002` 尚未开始实现，群组列表与共享屏幕列表的 Mongo 成员查询风险仍在 | 继续切入群组 queryset/permission 链收口 |
| 2026-05-12 | 5 | 收口群组 Mongo 成员查询与用户映射链 | GAP-GROUP-002, GAP-REVIEW-001 | Codex | 已将 `GroupViewSet`、`GroupInvitationViewSet`、`SharedScreenViewSet` 以及 `IsGroupMember` / `IsGroupCreatorOrAdmin` 统一到 `request.mongo_user / get_mongo_user_from_django()`；群组与共享屏幕列表不再依赖 `members__...` 反向关系，改为先查 `GroupMember` 再回查可见 `Group`；新增 `groups.tests.test_mongo_contracts`，与既有 URL/共享屏幕契约测试合计 14 项通过，`manage.py check` 继续通过 | `GroupDetailSerializer` 仍直接使用 `request.user` 判断创建者/管理员，加入码显示口径还没有完全收敛到 Mongo 用户；真实页面联调和体验审查也还未完成 | 继续修复群组详情展示口径，并联动前端页面做下一轮功能审查 |
| 2026-05-12 | 5 | 回填群组专项第二刀提交哈希 | GAP-GROUP-002 | Codex | 已将群组专项第二刀修复提交 `4fcfbf5` 绑定到总控与 GAP 台账，确保查询链修复、验证证据与 Git 记录一致 | `GroupDetailSerializer` 与前端页面消费链仍未继续收口 | 继续进入群组详情显示口径与前端页面审查 |
| 2026-05-12 | 5 | 收口群组详情展示口径与前端未接线路由 | GAP-GROUP-003, GAP-REVIEW-001 | Codex | 已将 `GroupDetailSerializer` 的加入码展示逻辑统一到 `request.mongo_user / get_mongo_user_from_django()`，并新增 `groups.tests.test_serializer_contracts`；前端 `src/components/groups/GroupDetail.js` 已移除未注册的 `EditGroup` 菜单项，避免详情页直接导航失败；群组最小回归已扩展到 16 项并全部通过，`manage.py check` 继续通过；对应提交 `3e0cfe2` 已推送到 `QX/上线整改-批次05-功能审查与路由通知修复` | 当前还未进入前端页面级真实联调，邀请成员页仍停留在“输入 userId”这种较原始形态，详情页生成加入码菜单也还可以继续做更细的权限可见性优化 | 继续进入页面消费链审查，并结合 UI/体验做下一轮收口 |
| 2026-05-12 | 5 | 收口共享屏幕信令地址、身份口径与无效管理动作 | GAP-GROUP-004, GAP-REVIEW-001 | Codex | 已将群组详情能力下发扩展到 `can_invite` / `can_generate_join_code`，前端详情页只在具备能力时显示对应动作；`src/services/webrtc/webrtcService.js` 已改为基于 `API_URL` 直连 `/ws/webrtc/{room_id}/`，不再误拼到 `/api/v1/ws/...`；共享页改为使用当前登录用户身份初始化信令、按群组展示活跃共享列表，并在移动端明确阻止创建假共享会话；`python manage.py test groups.tests.test_urls groups.tests.test_view_contracts groups.tests.test_mongo_contracts groups.tests.test_serializer_contracts --settings=backend.settings.testing` 17 项通过，目标文件 `eslint` 已通过 | 邀请成员页仍是手填 `userId` 的调试型流程，共享观看端目前只完成最小加入入口，尚未完成检索式邀请体验与 Android 专用 MCP 真机可视化验证；仓库级 `babel-plugin-transform-remove-console` 缺失与 `baseline-browser-mapping` 过期提醒仍待后续集中治理 | 继续进入邀请流程体验改造、共享深层联调与文档/Git 闭环 |
| 2026-05-12 | 5 | 回填群组专项第四刀提交哈希 | GAP-GROUP-004 | Codex | 已将群组专项第四刀修复提交 `f56f6b8` 绑定到总控与 GAP 台账，确保共享屏幕信令链修复、详情页能力字段收口与 17 项群组回归验证证据一致 | 邀请成员页、共享观看端深层体验和 Android 专用 MCP 真机可视化验证仍未完成 | 继续进入邀请流程体验改造与后续页面联调 |
| 2026-05-12 | 5 | 启动邀请成员流程搜索化整改 | GAP-GROUP-005, GAP-REVIEW-001 | Codex | 已确认现有邀请页仍是“手填 userId”调试链，且 `backend/users` 的 `/users/search/` 仅限管理员，不适合真实群组邀请；本轮转为在 `groups` 域内补 `/api/v1/groups/{id}/invite-candidates/` 候选搜索契约，并同步将前端邀请页改为搜索选择式流程；对应提交 `49c933b` 已推送到当前批次分支 | Android 专用 MCP 真机可视化验证仍未开始，共享观看端更深层联调也还未完成 | 继续进入共享观看端与群组剩余体验收口，并等待 Android 专用 MCP/插件接管真机验证 |
| 2026-05-12 | 5 | 回填群组专项第五刀提交哈希 | GAP-GROUP-005 | Codex | 已将群组专项第五刀修复提交 `49c933b` 绑定到总控与 GAP 台账，确保邀请成员搜索选择链、群组域候选搜索契约与 15 项最小回归验证证据一致 | Android 专用 MCP 真机可视化验证与群组共享观看端更深层体验验证仍未完成 | 继续进入共享观看端和后续功能审查闭环 |
| 2026-05-12 | 5 | 启动共享观看端三态体验收口 | GAP-GROUP-006, GAP-REVIEW-001 | Codex | 已确认共享页此前虽然完成了最小活跃共享列表与加入入口，但仍缺少“共享中 / 观看中 / 空闲”三态页面、离开观看能力、手动刷新与更清晰的状态反馈；本轮先在前端收口共享观看端状态机与 UI 分区，并补最小目标文件校验；对应提交 `fed56c8` 已推送到当前批次分支 | Android 专用 MCP 真机可视化验证与 WebRTC 深层联调证据仍待后续补齐 | 继续进入共享链真实联调证据补强与后续功能审查 |
| 2026-05-12 | 5 | 回填群组专项第六刀提交哈希 | GAP-GROUP-006 | Codex | 已将群组专项第六刀修复提交 `fed56c8` 绑定到总控与 GAP 台账，确保共享观看端三态页面收口与目标文件静态校验证据一致 | 当前仍缺 Android 专用 MCP 真机可视化验证与 WebRTC 深层联调证据 | 继续进入共享链真实联调证据补强与后续上线评审 |
| 2026-05-12 | 5 | 启动共享观看链前置联调修复 | GAP-GROUP-007, GAP-REVIEW-001 | Codex | 已确认共享观看链在真实联调前还存在房间切换复用旧连接、回调重复累积和观看态虽有状态但无稳定远端流承载区的问题；本轮先补 WebRTC 服务切房间断连、回调解绑和观看态远端视频承载与等待提示；对应提交 `f092c10` 已推送到当前批次分支 | Android 专用 MCP 真机可视化验证与 WebRTC 端到端证据仍待后续补齐 | 继续完成共享链真实观看证据补强，并在条件具备时转入真机验证 |
| 2026-05-12 | 5 | 回填群组专项第七刀提交哈希 | GAP-GROUP-007 | Codex | 已将群组专项第七刀修复提交 `f092c10` 绑定到总控与 GAP 台账，确保共享观看链前置联调修复与目标文件静态校验证据一致 | 当前仍缺 Android 专用 MCP 真机可视化验证与 WebRTC 端到端证据 | 继续完成共享链真实观看证据补强与后续上线评审 |
| 2026-05-12 | 5 | 收口共享观看态自动回收与会话快照同步 | GAP-GROUP-008, GAP-REVIEW-001 | Codex | 已确认共享观看页在真实联调前仍存在“房主离开、共享结束或刷新后目标会话消失，但页面仍停留在观看中”的假状态风险；本轮在 `src/components/groups/ScreenShare.js` 增加观看对象最新快照同步、房主离开自动退场和共享会话失效自动回收，目标文件 `eslint` 已通过 | Android 专用 MCP 真机可视化验证与 WebRTC 端到端证据仍待后续补齐；仓库级 `babel-plugin-transform-remove-console` 缺失与 `baseline-browser-mapping` 过期提醒仍待集中治理 | 继续补共享链真实联调证据，并在条件具备时切换 Android 专用 MCP 真机验证 |
| 2026-05-12 | 5 | 回填群组专项第八刀提交哈希 | GAP-GROUP-008 | Codex | 已将群组专项第八刀修复提交 `ffe9d5f` 绑定到总控与 GAP 台账，确保共享观看态自动回收收口与目标文件静态校验证据一致 | 当前仍缺 Android 专用 MCP 真机可视化验证与 WebRTC 端到端证据 | 继续补共享链真实联调证据，并在条件具备时切换 Android 专用 MCP 真机验证 |
| 2026-05-12 | 5 | 收口共享链专属状态隔离 | GAP-GROUP-008, GAP-REVIEW-001 | Codex | 已确认共享页此前仍复用群组全局 `isLoading/error`，会把群详情、邀请成员等无关请求的加载与失败误映射到共享按钮和错误提示；本轮在 `src/redux/slices/groupsSlice.js` 拆出 `screenShareLoading/screenShareError`，并让 `src/components/groups/ScreenShare.js` 仅消费共享链专属状态，进入页面时同步清理旧共享错误；目标文件 `eslint` 已通过 | Android 专用 MCP 真机可视化验证与 WebRTC 端到端证据仍待后续补齐；仓库级 `babel-plugin-transform-remove-console` 缺失与 `baseline-browser-mapping` 过期提醒仍待集中治理 | 继续补共享链联调可观测性，并在条件具备时切换 Android 专用 MCP 真机验证 |
| 2026-05-12 | 5 | 回填群组专项第八刀第二阶段提交哈希 | GAP-GROUP-008 | Codex | 已将共享链专属状态隔离修复提交 `11195e5` 绑定到总控与 GAP 台账，确保共享页误报错/误禁用收口与目标文件静态校验证据一致 | 当前仍缺 Android 专用 MCP 真机可视化验证与 WebRTC 端到端证据 | 继续补共享链联调可观测性，并在条件具备时切换 Android 专用 MCP 真机验证 |
| 2026-05-12 | 5 | 收口共享链联调可观测性 | GAP-GROUP-008, GAP-REVIEW-001 | Codex | 已确认共享观看链此前虽然补了状态隔离与自动回收，但联调时仍难区分“信令未连上”“已连上但远端未推流”“远端流等待超时”等不同故障；本轮在 `src/services/webrtc/webrtcService.js` 增加连接状态回调，并在 `src/components/groups/ScreenShare.js` 补信令状态徽标、错误原因与 12 秒远端流等待超时提示；目标文件 `eslint` 已通过 | Android 专用 MCP 真机可视化验证与 WebRTC 端到端证据仍待后续补齐；仓库级 `babel-plugin-transform-remove-console` 缺失与 `baseline-browser-mapping` 过期提醒仍待集中治理 | 继续补共享链真实联调证据，并在条件具备时切换 Android 专用 MCP 真机验证 |
| 2026-05-12 | 5 | 回填群组专项第八刀第三阶段提交哈希 | GAP-GROUP-008 | Codex | 已将共享链联调可观测性修复提交 `f41f2ff` 绑定到总控与 GAP 台账，确保信令状态、错误原因与远端流超时提示的校验证据一致 | 当前仍缺 Android 专用 MCP 真机可视化验证与 WebRTC 端到端证据 | 继续补共享链真实联调证据，并在条件具备时切换 Android 专用 MCP 真机验证 |
| 2026-05-12 | 5 | 追加共享链页面内联调证据面板 | GAP-GROUP-008, GAP-REVIEW-001 | Codex | 已确认本地 WebRTC 联调入口仍未完全闭环，单靠现有状态徽标和超时提示仍难快速回传“当前会话是谁、连到了哪个房间、卡在哪个阶段”；本轮在 `src/components/groups/ScreenShare.js` 新增页面内联调摘要面板、最近关键事件时间线与“复制摘要”能力，汇总当前角色、共享会话 ID、WebRTC 房间、连接状态/详情、远端流状态与超时信息；目标文件 `eslint` 已通过 | Android 专用 MCP 真机可视化验证与 WebRTC 端到端证据仍待后续补齐；本轮仅补页面内排障证据，不得据此宣称共享链已完成端到端联调或真机验证完成；仓库级 `babel-plugin-transform-remove-console` 缺失与 `baseline-browser-mapping` 过期提醒仍待集中治理 | 继续补本地双端联调入口与 Android 专用 MCP 真机验证证据 |
| 2026-05-12 | 5 | 收口本地共享链联调入口 | GAP-ENV-003, GAP-GROUP-008, GAP-REVIEW-001 | Codex | 已确认 Electron 开发态原先只是假定 `localhost:8081` 可用，但仓库没有稳定的桌面依赖和 RN Web 主入口；本轮已在 `package.json` 新增 `yarn local:web`，在 `scripts/serve-local-web.js` 提供 `shared-screen-lab` 静态诊断页，并将 `electron/main.js` 开发态入口明确指向 `http://127.0.0.1:8091/shared-screen-lab/`，把共享链本地诊断入口稳定落到 `8091`，同时把 `8081` 留给 React Native Metro；`node --check scripts/serve-local-web.js` 已通过，且独立端口 `8091` 下页面返回 200 并命中关键文案 | Android 专用 MCP 真机可视化验证与 WebRTC 端到端证据仍待后续补齐；本轮只解决本地联调入口缺失，不等同于 Electron 桌面版、React Native Web 主应用或共享链真机联调已完成 | 继续回填文档/Git 记录，并进入下一轮共享链真实联调证据补强 |
| 2026-05-12 | 5 | 收口共享链最小契约运行稳定性并补正式 smoke 工具 | GAP-SLIM-001, GAP-GROUP-008, GAP-REVIEW-001 | Codex | 已新增 `backend/scripts/shared_screen_smoke.py` 与对应说明文档，删除 `backend/tmp_smoke.py`、`backend/tmp_e2e_debug.py` 两个过时临时脚本；同时补齐 testing/mongomock UUID 口径、Mongo 用户失效映射回退、群组创建者自动入群、Mongo QuerySet `get_object()` 兼容与群组/共享显式序列化器；`python manage.py test groups.tests.test_mongo_contracts groups.tests.test_view_contracts groups.tests.test_serializer_contracts --settings=backend.settings.testing` 共 15 项通过 | 完整 `shared_screen_smoke.py` HTTP 流程仍待继续收口 API 级引用反解稳定性；当前结果只证明共享 HTTP 最小契约相关回归已恢复，不等同于 WebRTC 端到端联调或 Android 专用 MCP 真机验证完成 | 回填提交哈希与推送记录，并把完整 HTTP smoke 阻塞拆入下一轮共享链专项修复 |
| 2026-05-12 | 5 | 回填共享 HTTP 最小 smoke 全绿结果 | GAP-GROUP-009, GAP-REVIEW-001 | Codex | 已实际跑通 `groups.tests.test_shared_screen_smoke --settings=backend.settings.testing`，最小链路“注册 -> 登录 -> 建群 -> 生成加入码 -> 创建共享 -> 列表 -> 加入共享”通过；并再次确认该结果仅证明共享 HTTP 最小契约可达，不等同于 WebRTC 端到端联调完成，也不等同于 Android 专用 MCP 真机验证完成 | 当前剩余阻塞已从 HTTP 契约转为 WebRTC 媒体协商、暂停/恢复链路与 Android 专用 MCP 真机可视化验证 | 继续把 WebRTC 前置硬阻塞拆成独立修复并补文档/Git 闭环 |
| 2026-05-12 | 5 | 收口共享发起端先连房后推流补轨问题 | GAP-GROUP-010, GAP-REVIEW-001 | Codex | 已确认 `src/services/webrtc/webrtcService.js` 在“先连接房间、后开始共享”场景下不会把本地屏幕轨道补挂到既有 PeerConnection；本轮补齐本地轨道同步、重复 `addTrack` 去重与停共享后的旧轨道回收，并新增 `src/services/webrtc/__tests__/webrtcService.test.js` 最小单测兜底；`jest --runTestsByPath src/services/webrtc/__tests__/webrtcService.test.js --watch=false` 3 项通过，目标文件 `eslint` 已通过 | 该修复只收口本地 WebRTC 联调前置逻辑，不等同于 WebRTC 端到端联调完成；Android 专用 MCP/插件仍未就位，真机可视化验证继续保持 `BLOCKED` | 继续补 pause/resume 全链路、共享会话全局状态建模与 Android 专用 MCP 真机验证证据 |
| 2026-05-12 | 5 | 收口共享暂停/恢复链路与暂停态假可用问题 | GAP-GROUP-011, GAP-REVIEW-001 | Codex | 已确认共享暂停/恢复动作虽然在后端存在，但前端此前完全未接，导致 `paused` 会话仍被展示为可加入入口；本轮补齐 `pause/resume` API、Redux 与共享页最小操作链，并在后端 `join` 明确拒绝暂停中的共享；`D:/APP/Anaconda/Scripts/conda.exe run -n Zeroisle python manage.py test groups.tests.test_view_contracts --settings=backend.settings.testing` 4 项通过，目标文件 `eslint` 已通过 | 该修复只收口共享状态契约与假可用入口，不等同于 WebRTC 端到端联调完成；当前仍未补齐共享会话全局状态建模和 Android 专用 MCP 真机可视化验证 | 继续补共享会话全局状态、pause/resume 更深层联动和 Android 专用 MCP 真机验证证据 |
| 2026-05-12 | 5 | 收口共享会话全局状态建模第一阶段 | GAP-GROUP-012, GAP-REVIEW-001 | Codex | 已确认共享页此前把“当前会话”拆散保存在组件本地 `shareId / joinedShare / isSharing` 等状态中，导致共享列表、页面角色与信令状态容易漂移；本轮在 `src/redux/slices/groupsSlice.js` 新增 `activeScreenShareSession` 全局状态模型、selector 与 reducer，在 `src/components/groups/ScreenShare.js` 将发起/加入/离开/刷新同步开始收口到该全局会话状态，并新增 `src/redux/slices/__tests__/groupsSlice.test.js`；`node node_modules/jest/bin/jest.js --runTestsByPath src/redux/slices/__tests__/groupsSlice.test.js --watch=false` 4 项通过，目标文件 `eslint` 已通过 | 当前共享页仍保留部分本地 UI 状态与 `ref` 协助联调，WebRTC service 事件也尚未完全抽成 Redux 驱动；该修复只解决“状态源不唯一”的主风险，不等同于 WebRTC 端到端联调完成，也不等同于 Android 专用 MCP 真机可视化验证完成 | 继续补共享页剩余本地状态收口、WebRTC 事件到 Redux 的进一步统一，以及 Android 专用 MCP 真机验证证据 |
| 2026-05-12 | 5 | 收口共享会话全局状态建模第二阶段 | GAP-GROUP-012, GAP-REVIEW-001 | Codex | 已继续从 `src/components/groups/ScreenShare.js` 中移除 `latestShareIdRef`、`latestIsSharingRef`、`joinedShareRef` 等本地会话影子状态，并让共享结束清理、成员离开回收与诊断口径更多直接依赖 Redux 的 `activeScreenShareSession`；`node node_modules/jest/bin/jest.js --runTestsByPath src/redux/slices/__tests__/groupsSlice.test.js --watch=false` 4 项继续通过，目标文件 `eslint` 已继续通过 | 当前共享页仍保留标题输入、诊断面板、远端流等待超时等本地 UI 状态，WebRTC service 事件也尚未完全抽成 Redux 驱动；该修复仍只解决共享会话状态统一问题，不等同于 WebRTC 端到端联调完成，也不等同于 Android 专用 MCP 真机可视化验证完成 | 继续补 WebRTC 事件与更多共享页 UI 状态的 Redux 收口，并等待 Android 专用 MCP/插件接管真实平板验证 |
| 2026-05-12 | 5 | 收口共享会话全局状态建模第三阶段 | GAP-GROUP-012, GAP-REVIEW-001 | Codex | 已将“远端流已到达 / 观看等待超时”诊断信号并入 `src/redux/slices/groupsSlice.js` 的 `activeScreenShareSession`，并在 `src/components/groups/ScreenShare.js` 中让观看遮罩、诊断面板与等待提示更多依赖 Redux 会话状态；`src/redux/slices/__tests__/groupsSlice.test.js` 已扩展到 5 项并继续通过，目标文件 `eslint` 继续通过 | 当前共享页仍保留标题输入、手动刷新、对话框开关和诊断事件时间线等本地 UI 状态，WebRTC service 事件也尚未完全抽成 Redux 驱动；该修复仍只收口会话与联调诊断状态统一问题，不等同于 WebRTC 端到端联调完成，也不等同于 Android 专用 MCP 真机可视化验证完成 | 继续补 WebRTC service 事件到 Redux 的进一步统一，并等待 Android 专用 MCP/插件接管真实平板验证 |
| 2026-05-12 | 5 | 收口共享会话全局状态建模第四阶段 | GAP-GROUP-012, GAP-REVIEW-001 | Codex | 已将 WebRTC 连接生命周期进一步映射到 `activeScreenShareSession`，让 `connected / error / closed` 与 `sharing / viewing / paused / error / idle` 的页面会话状态更一致，并在 `src/redux/slices/__tests__/groupsSlice.test.js` 新增连接生命周期 patch 回归；`node node_modules/eslint/bin/eslint.js src/components/groups/ScreenShare.js src/redux/slices/groupsSlice.js src/redux/slices/__tests__/groupsSlice.test.js` 与 `node node_modules/jest/bin/jest.js --runTestsByPath src/redux/slices/__tests__/groupsSlice.test.js --watch=false` 通过 | 当前共享页仍保留部分本地 UI 状态与诊断事件时间线，WebRTC service 事件也尚未完全 Redux 化；该修复只收口连接生命周期与会话状态映射，不等同于 WebRTC 端到端联调完成，也不等同于 Android 专用 MCP 真机可视化验证完成 | 继续补 WebRTC service 事件统一、双端真实联调证据，并等待 Android 专用 MCP/插件接管真实平板验证 |
| 2026-05-12 | 5 | 收口共享快照引用漂移导致的重复同步与假诊断 | GAP-GROUP-012, GAP-REVIEW-001 | Codex | 已确认观看态同步此前使用 `liveJoinedShare !== joinedShare` 做对象引用比较，只要共享列表刷新返回了“内容相同但引用已变”的快照，就会重复触发 `patchActiveScreenShareSession()` 与“共享快照已同步”诊断事件；本轮在 `src/components/groups/ScreenShare.js` 改为基于 `id/status/title/webrtc_room_id/user.id/group.id` 的快照签名比较，仅在真正业务字段变化时才同步共享快照，并在 `src/redux/slices/__tests__/groupsSlice.test.js` 补 1 条最小回归；目标文件 `eslint` 与 `groupsSlice` 单测 7 项通过 | 当前共享页仍保留标题输入、对话框开关、诊断事件时间线等本地 UI 状态，WebRTC service 事件也尚未完全 Redux 化；该修复只减少共享列表刷新导致的假同步与重复诊断，不等同于 WebRTC 端到端联调完成，也不等同于 Android 专用 MCP 真机可视化验证完成 | 继续补共享链剩余本地 UI 状态与 WebRTC service 事件的 Redux 收口，并等待 Android 专用 MCP/插件接管真实平板验证 |
| 2026-05-12 | 5 | 回填共享会话全局状态建模第四阶段提交哈希 | GAP-GROUP-012, GAP-REVIEW-001 | Codex | 已将“连接生命周期状态映射统一”对应提交与推送结果回填为 `0e9ade8`，确保总控文档、GAP 台账与 Git 记录一致；当前分支已推送到 `origin/QX/上线整改-批次05-功能审查与路由通知修复` | 本轮仍未触及 WebRTC 双端真实联调与 Android 专用 MCP 真机可视化验证 | 继续按共享链剩余高风险返工点和仓库级前端阻塞项拆下一刀 |
| 2026-05-12 | 5 | 回填共享会话全局状态建模第五阶段提交哈希 | GAP-GROUP-012, GAP-REVIEW-001 | Codex | 已将“共享快照引用漂移导致的重复同步与假诊断”对应提交与推送结果回填为 `addb5e1`，确保总控文档、GAP 台账与 Git 记录一致；当前分支已推送到 `origin/QX/上线整改-批次05-功能审查与路由通知修复` | 本轮仍未触及 WebRTC 双端真实联调与 Android 专用 MCP 真机可视化验证 | 继续按共享链剩余高风险返工点和仓库级前端阻塞项拆下一刀 |
| 2026-05-12 | 5 | 收口共享列表等值刷新与同值会话 patch 的引用漂移 | GAP-GROUP-012, GAP-REVIEW-001 | Codex | 已确认 `src/redux/slices/groupsSlice.js` 里的 `fetchScreenShares.fulfilled` 与 `patchActiveScreenShareSession` 在关键业务字段未变时仍会替换 `sharedScreens` 与 `activeScreenShareSession` 引用，导致 `src/components/groups/ScreenShare.js` 在共享列表刷新、重复联调事件或等值会话 patch 下继续整页重渲染。本轮在 slice 层新增共享快照签名比较、等值列表复用与同值 patch no-op 守卫，并在 `src/redux/slices/__tests__/groupsSlice.test.js` 补 2 条最小回归；目标文件 `eslint` 与 `groupsSlice` 单测继续通过 | 当前共享页仍保留部分本地 UI 状态，WebRTC service 订阅 effect 也尚未拆出“卸载清理”和“依赖变化重绑”风险；该修复只进一步收口 Redux 引用漂移问题，不等同于 WebRTC 端到端联调完成，也不等同于 Android 专用 MCP 真机可视化验证完成 | 继续处理 WebRTC 订阅 effect 清理时机与重复诊断去重，并等待 Android 专用 MCP/插件接管真实平板验证 |
| 2026-05-12 | 5 | 收口 WebRTC 订阅 effect 的依赖变化误清理风险 | GAP-GROUP-012, GAP-REVIEW-001 | Codex | 已确认 `src/components/groups/ScreenShare.js` 中 WebRTC 订阅 effect 依赖 `activeSession.role/shareId/status`、`joinedShare.user.id` 与 `isCurrentSharePaused`，导致共享状态正常流转时也会先执行 cleanup，再误触发 `webrtcService.disconnect()` 与宿主态 `endScreenShare()`。本轮新增会话态 `ref`，将事件订阅解绑与页面卸载清理拆成两个 effect，仅在组件真正卸载时执行连接回收和宿主态结束共享；目标文件 `eslint` 继续通过 | 当前共享页仍保留部分本地 UI 状态，重复诊断事件去重与更多 WebRTC service 事件 Redux 化仍未完成；该修复只降低 effect 重绑导致的误断连和误结束风险，不等同于 WebRTC 端到端联调完成，也不等同于 Android 专用 MCP 真机可视化验证完成 | 继续处理重复诊断事件去重、connectedUsers 状态进一步收口，并等待 Android 专用 MCP/插件接管真实平板验证 |
| 2026-05-12 | 5 | 回填共享会话全局状态建模第七阶段提交哈希 | GAP-GROUP-012, GAP-REVIEW-001 | Codex | 已将“WebRTC 订阅 effect 的依赖变化误清理风险收口”对应提交与推送结果回填为 `ccef7bf`，确保总控文档、GAP 台账与 Git 记录一致；当前分支已推送到 `origin/QX/上线整改-批次05-功能审查与路由通知修复` | 本轮仍未触及 WebRTC 双端真实联调与 Android 专用 MCP 真机可视化验证 | 继续按共享链重复诊断去重与更多事件 Redux 化拆下一刀 |
| 2026-05-12 | 5 | 收口共享链重复诊断事件与同值联调信号重复 patch | GAP-GROUP-012, GAP-REVIEW-001 | Codex | 已确认 `src/components/groups/ScreenShare.js` 中 `onUserJoin`、`onUserLeave`、`onRemoteStream` 与 `onConnectionStateChange` 在成员未变化、远端流状态未变化或信令状态未变化时仍会重复追加诊断事件并重复 patch 会话状态。本轮在组件侧增加事件级去重守卫，仅在成员列表、远端流状态、等待超时标志或信令状态/详情真正变化时才写诊断与 dispatch；目标文件 `eslint` 与 `groupsSlice` 单测继续通过 | 当前共享页仍保留部分本地 UI 状态，connectedUsers 与更多 WebRTC service 事件的 Redux 化仍未完成；该修复只减少联调噪声与无效状态写入，不等同于 WebRTC 端到端联调完成，也不等同于 Android 专用 MCP 真机可视化验证完成 | 继续评估 connectedUsers 是否需要收口到 Redux，并等待 Android 专用 MCP/插件接管真实平板验证 |
| 2026-05-12 | 5 | 回填共享会话全局状态建模第八阶段提交哈希 | GAP-GROUP-012, GAP-REVIEW-001 | Codex | 已将“共享链重复诊断事件与同值联调信号重复 patch 收口”对应提交与推送结果回填为 `a05531d`，确保总控文档、GAP 台账与 Git 记录一致；当前分支已推送到 `origin/QX/上线整改-批次05-功能审查与路由通知修复` | 本轮仍未触及 WebRTC 双端真实联调与 Android 专用 MCP 真机可视化验证 | 继续按 connectedUsers 状态收口与更多 WebRTC service 事件 Redux 化拆下一刀 |
| 2026-05-12 | 5 | 收口连接成员列表本地状态与会话状态双轨漂移 | GAP-GROUP-012, GAP-REVIEW-001 | Codex | 已确认 `src/components/groups/ScreenShare.js` 仍将 `connectedUsers` 保存在组件本地 state，容易与 `activeScreenShareSession` 的连接状态、角色状态和离场清理双轨漂移。本轮将连接成员列表收口进 `src/redux/slices/groupsSlice.js` 的 `activeScreenShareSession.connectedUsers`，让成员接入/离开和页面展示统一依赖 Redux 会话，并为等值用户列表 patch 增加 no-op 守卫与最小回归；目标文件 `eslint` 与 `groupsSlice` 单测继续通过 | 当前共享页仍保留部分本地 UI 状态，更多 WebRTC service 事件 Redux 化和 Android 专用 MCP 真机验证仍未完成；该修复只进一步收口连接成员列表状态源，不等同于 WebRTC 端到端联调完成，也不等同于 Android 专用 MCP 真机可视化验证完成 | 继续评估更多 WebRTC service 事件是否需要 Redux 化，并等待 Android 专用 MCP/插件接管真实平板验证 |
| 2026-05-12 | 5 | 回填共享会话全局状态建模第九阶段提交哈希 | GAP-GROUP-012, GAP-REVIEW-001 | Codex | 已将“连接成员列表本地状态与会话状态双轨漂移收口”对应提交与推送结果回填为 `8a0fc6f`，确保总控文档、GAP 台账与 Git 记录一致；当前分支已推送到 `origin/QX/上线整改-批次05-功能审查与路由通知修复` | 本轮仍未触及 WebRTC 双端真实联调与 Android 专用 MCP 真机可视化验证 | 继续按更多 WebRTC service 事件 Redux 化与真机验证准备拆下一刀 |
| 2026-05-13 | 5 | 收口远端流播放器挂载状态的本地事实漂移 | GAP-GROUP-012, GAP-REVIEW-001 | Codex | 已确认 `src/components/groups/ScreenShare.js` 只能通过 `videoRef.current.srcObject` 知道远端流是否真的挂载到播放器，而 Redux 会话此前只有 `hasRemoteStream`，无法区分“收到流对象”与“播放器已挂载远端流”的差异。本轮将 `hasAttachedRemoteStream` 并入 `src/redux/slices/groupsSlice.js` 的 `activeScreenShareSession`，在远端流挂载/清空时同步 patch，并把该状态纳入联调摘要和最小回归；目标文件 `eslint` 与 `groupsSlice` 单测继续通过 | 当前共享页仍保留部分本地 UI 状态，WebRTC 端到端联调与 Android 专用 MCP 真机验证仍未完成；该修复只进一步收口远端流挂载事实，不等同于 WebRTC 端到端联调完成，也不等同于 Android 专用 MCP 真机可视化验证完成 | 继续评估是否需要把更多播放器/UI 联调事实收口到 Redux，并等待 Android 专用 MCP/插件接管真实平板验证 |
| 2026-05-13 | 5 | 回填共享会话全局状态建模第十阶段提交哈希 | GAP-GROUP-012, GAP-REVIEW-001 | Codex | 已将“远端流播放器挂载状态的本地事实漂移收口”对应提交与推送结果回填为 `2d7ebea`，确保总控文档、GAP 台账与 Git 记录一致；当前分支已推送到 `origin/QX/上线整改-批次05-功能审查与路由通知修复` | 本轮仍未触及 WebRTC 双端真实联调与 Android 专用 MCP 真机可视化验证 | 继续按更多播放器/UI 联调事实 Redux 化与真机验证准备拆下一刀 |
| 2026-05-13 | 5 | 收口加入新观看会话前的旧画面残留风险 | GAP-GROUP-012, GAP-REVIEW-001 | Codex | 已确认观看端在加入新会话前，没有显式清空旧的 `videoRef.current.srcObject` 与 `hasAttachedRemoteStream`，若上一场会话刚结束或切换较快，页面可能短暂残留旧画面。本轮在 `src/components/groups/ScreenShare.js` 的加入观看链路中先清空播放器挂载，再同步将 `hasAttachedRemoteStream` 复位为 `false`，并删除 `resetSessionState()` 中对已清空会话的无效 patch；目标文件 `eslint` 与 `groupsSlice` 单测继续通过 | 当前共享页仍保留部分本地 UI 状态，WebRTC 端到端联调与 Android 专用 MCP 真机验证仍未完成；该修复只进一步减少旧画面残留和本地事实漂移，不等同于 WebRTC 端到端联调完成，也不等同于 Android 专用 MCP 真机可视化验证完成 | 继续评估是否需要把更多播放器/UI 联调事实收口到 Redux，并等待 Android 专用 MCP/插件接管真实平板验证 |
| 2026-05-13 | 5 | 回填共享会话全局状态建模第十一阶段提交哈希 | GAP-GROUP-012, GAP-REVIEW-001 | Codex | 已将“加入新观看会话前的旧画面残留风险收口”对应提交与推送结果回填为 `5476eb2`，确保总控文档、GAP 台账与 Git 记录一致；当前分支已推送到 `origin/QX/上线整改-批次05-功能审查与路由通知修复` | 本轮仍未触及 WebRTC 双端真实联调与 Android 专用 MCP 真机可视化验证 | 继续按更多播放器/UI 联调事实 Redux 化与真机验证准备拆下一刀 |
| 2026-05-13 | 5 | 收口会话结束后的旧联调事实残留 | GAP-GROUP-012, GAP-REVIEW-001 | Codex | 已确认 `endScreenShare.fulfilled` 与 `ScreenShare` 的 `connectionState=closed/error/idle` 路径此前没有同步清空旧的 `connectedUsers`、`hasRemoteStream`、`hasAttachedRemoteStream` 与 `viewerTimeoutReached`，会造成“会话已结束但联调事实仍残留”的假状态。本轮在 `src/redux/slices/groupsSlice.js` 与 `src/components/groups/ScreenShare.js` 中统一补齐这些收尾字段的复位；目标文件 `eslint` 与 `groupsSlice` 单测继续通过 | 当前共享页仍保留部分本地 UI 状态，WebRTC 端到端联调与 Android 专用 MCP 真机验证仍未完成；该修复只进一步减少结束态旧事实残留，不等同于 WebRTC 端到端联调完成，也不等同于 Android 专用 MCP 真机可视化验证完成 | 继续评估是否需要补针对 ended/closed 路径的更细粒度 reducer 回归，并等待 Android 专用 MCP/插件接管真实平板验证 |
| 2026-05-13 | 5 | 回填共享会话全局状态建模第十二阶段提交哈希 | GAP-GROUP-012, GAP-REVIEW-001 | Codex | 已将“会话结束后的旧联调事实残留收口”对应提交与推送结果回填为 `17e43d3`，确保总控文档、GAP 台账与 Git 记录一致；当前分支已推送到 `origin/QX/上线整改-批次05-功能审查与路由通知修复` | 本轮仍未触及 WebRTC 双端真实联调与 Android 专用 MCP 真机可视化验证 | 继续按 ended/closed 路径回归补强与真机验证准备拆下一刀 |
| 2026-05-13 | 5 | 补齐结束态旧联调事实清理回归 | GAP-GROUP-012, GAP-REVIEW-001 | Codex | 已为 `src/redux/slices/groupsSlice.js` 的 `endScreenShare.fulfilled` 新增最小 reducer 回归，覆盖会话结束时同步清空 `connectedUsers`、`hasRemoteStream`、`hasAttachedRemoteStream`、`viewerTimeoutReached` 并把 `shareSnapshot.status` 收口为 `ended`；验证命令为 `node node_modules/jest/bin/jest.js --runTestsByPath src/redux/slices/__tests__/groupsSlice.test.js --watch=false` 与 `node node_modules/eslint/bin/eslint.js src/components/groups/ScreenShare.js src/redux/slices/groupsSlice.js src/redux/slices/__tests__/groupsSlice.test.js` | 当前共享页的 `closed/error/idle` UI 链路仍主要依赖组件事件驱动验证，尚未完成 Android 专用 MCP 真机可视化验证；本轮只补强 slice 级结束态回归，不等同于 WebRTC 端到端联调完成，也不等同于 Android 专用 MCP 真机验证完成 | 提交后回填本阶段提交哈希，并继续评估是否需要把 `closed/error/idle` 的更多事实收口为可单测的 Redux 规则 |
| 2026-05-13 | 5 | 回填共享会话全局状态建模第十三阶段提交哈希 | GAP-GROUP-012, GAP-REVIEW-001 | Codex | 已将“补齐结束态旧联调事实清理回归”对应提交与推送结果回填为 `7cb9bad`，确保总控文档、GAP 台账与 Git 记录一致；当前分支已推送到 `origin/QX/上线整改-批次05-功能审查与路由通知修复` | 本轮仍未触及 WebRTC 双端真实联调与 Android 专用 MCP 真机可视化验证 | 继续按共享链剩余 ended/closed 细粒度规则与真机验证准备拆下一刀 |
| 2026-05-13 | 5 | 收口 ended 快照在列表刷新后被错误映射回活跃态 | GAP-GROUP-012, GAP-REVIEW-001, GAP-DEVICE-001 | Codex | 已确认 `src/redux/slices/groupsSlice.js` 的 `syncActiveScreenShareSession()` 之前只对 `paused` 做特殊映射，若服务端返回的共享快照已是 `ended`，刷新共享列表时仍会把当前会话状态重新写回 `viewing/sharing`，造成“服务端已结束但页面仍像活跃会话”的假状态；本轮已将 `ended` 显式收口为会话 `status='ended'`，并在 `src/redux/slices/__tests__/groupsSlice.test.js` 新增“fetchScreenShares 返回 ended 快照后仍保持 ended”回归；验证命令为 `node node_modules/jest/bin/jest.js --runTestsByPath src/redux/slices/__tests__/groupsSlice.test.js --watch=false` 与 `node node_modules/eslint/bin/eslint.js src/redux/slices/groupsSlice.js src/redux/slices/__tests__/groupsSlice.test.js src/components/groups/ScreenShare.js` | 当前真实平板 `HGR3Y9MA` / `TB128FU` 仍在线，说明 attached-device 真机前置未丢；但 Android 专用 MCP/插件仍未接管可视化操作，因此本轮只能记为“真机前置继续就绪、共享链假状态继续收口”，不能标记真机验证完成 | 提交后回填本阶段提交哈希，并继续围绕 Android 专用 MCP 接管前的共享链/UI 细节与功能审查主线推进下一刀 |
| 2026-05-13 | 5 | 回填共享会话全局状态建模第十四阶段提交哈希 | GAP-GROUP-012, GAP-REVIEW-001, GAP-DEVICE-001 | Codex | 已将“收口 ended 快照刷新后的活跃态漂移”对应提交与推送结果回填为 `75d2b99`，确保总控文档、GAP 台账与 Git 记录一致；当前分支已推送到 `origin/QX/上线整改-批次05-功能审查与路由通知修复` | 本轮仍未触及 WebRTC 双端真实联调与 Android 专用 MCP 真机可视化验证 | 继续按共享链剩余结束态/UI 细粒度体验问题与 Android 专用 MCP 真机验证准备拆下一刀 |
| 2026-05-14 | 5 | 定位并收口 round20 Metro 红框阻塞 | GAP-REVIEW-001, GAP-DEVICE-001, GAP-DEVICE-003 | Codex | 已通过 `zeroisle_round20_relaunch.xml/png/logcat.txt` 将真机“空白 FrameLayout”精确定位为 React Native RedBox：`index.bundle` 返回 `500`，Metro 报错 `Failed to start watch mode`。本轮已在 `metro.config.js` 中移除重复 `watchFolders`，并对 `.local`、`backend`、`archive`、`docs`、`web` 等非 React Native 主链大目录增加 `blockList` 排除，降低 Windows watch mode 被仓库噪声拖垮的风险；同时已回填当前分支、最近三次提交和 `docs/adapter-pattern.md` 禁止提交规则 | 受本机端口占用与 Metro 启动等待时长影响，当前仅完成配置级收口与红框证据固化，尚未用真机重新证明 `round20` 点击链完全恢复 | 提交并推送本轮 Metro/文档修复后，继续以空闲端口 + 真机复测验证红框是否消失 |
| 2026-05-14 | 5 | 回填 round22 真机恢复证据与 Metro 恢复现状 | GAP-REVIEW-001, GAP-DEVICE-001, GAP-DEVICE-003 | Codex | 已确认 Metro 收口后的本机状态恢复正常：`http://127.0.0.1:8081/status` 返回 `200 packager-status:running`，`index.bundle` 返回 `200`，约 `2431ms` 完成。Android 真机 `round22` 已恢复首页、我的页、提醒列表页与提醒新增页主体可见，关键锚点包括 `screen.home`、`nav.tab.home/ai/community/profile`、`action.home.create`、`screen.profile`、`entry.reminder.profile`、`entry.knowledgeGraph.profile`、`screen.reminderList`、`filter.reminder.*`、`action.reminder.add`、`screen.reminder`、`input.reminder.title`、`action.reminder.pickDateTime`、`option.reminder.priority.*`、`action.reminder.create` | 当前仍未补齐 reminder 离线成功创建、创建后回到列表以及完成/启停/删除按钮链证据，因此只能确认“红框已解除、主体可见已恢复”，不能宣称真机验收完成 | 继续基于 round22 新增页现场验证离线创建是否真正成功，并在必要时做最小修复后补新一轮证据 |
| 2026-05-14 | 5 | 收口 reminder 启停链视觉可感知性并补 round31 真机证据 | GAP-REVIEW-001, GAP-MOBILE-001, GAP-DEVICE-001, GAP-DEVICE-003 | Codex | 已通过 `round31_toggle_after_logcat.txt` 确认启停链在远端更新失败时会离线记账 `save_offline_reminder_update`，不再触发 RN 底部开发态错误覆盖层；同时在 `src/components/reminder/ReminderListView.js` 为禁用态提醒新增“已暂停”显式徽标，并为启停按钮补充轻量蓝白背景、边框与更稳的点击热区。真机 `round31_toggle_visual.xml` 已确认条目 `round24_local_live！` 的 `content-desc` 包含“已暂停、离线”，且列表项存在独立“已暂停”文本节点，说明启停链已经从“只能靠 icon 变灰推断”提升到“业务与视觉都可感知” | 当前 reminder 更深层风险仍在于 `OFFLINE_REMINDERS`、`ALL_REMINDERS`、`OFFLINE_OPERATIONS` 与 Redux 本地乐观态并存，历史 hooks 依赖告警和共享链真机验证也尚未完成；因此状态仍只能记为“整改推进中 / 真机验证进行中” | 提交本轮视觉增强后，继续审查 reminder 多状态源收敛策略，并切回共享链与更高层功能审查 |
| 2026-05-14 | 5 | 收口 reminder 多状态源第一阶段：统一本地回退真相源 | GAP-REVIEW-001, GAP-MOBILE-001 | Codex | 已在 `src/services/reminder/reminderNotificationService.js` 新增 `normalizeReminderCollection()` 与 `getPreferredLocalReminders()`，让 `saveAllReminders/getAllReminders` 统一落为数组形状，并让列表页/导出链的离线回退优先读取最近一次成功拉取或同步后的 `ALL_REMINDERS`；同时在离线操作全量同步成功后主动清理旧 `OFFLINE_REMINDERS` 影子快照，减少“最新真相”和“离线影子态”长期并存带来的回弹概率。`src/components/reminder/ReminderListView.js` 已同步改为优先走该稳定本地源；定点 `eslint` 校验继续保持 0 error | 这次只完成 reminder 多状态源收口第一刀，尚未彻底统一 `OFFLINE_OPERATIONS` 与 Redux `unsyncedCount` 的计数口径，也未补新的 round32 真机证据，因此仍不能宣称 reminder 离线架构已完全闭环 | 提交后继续补 reminder 同步计数口径一致性与下一轮真机复测，并在共享链真机验证前保持文档与证据同步更新 |
| 2026-05-14 | 5 | 收口 reminder 未同步计数口径第一阶段：以离线队列为准 | GAP-REVIEW-001, GAP-MOBILE-001 | Codex | 已在 `src/redux/slices/reminderSlice.js` 新增 `refreshUnsyncedCount`，改为从 `OFFLINE_OPERATIONS` 真实长度回写 `syncStatus.unsyncedCount`；同时移除 `addLocalReminder/updateLocalReminder/deleteLocalReminder` 中对 `unsyncedCount` 的盲目 `+1`，并在 `src/screens/reminder/AddReminderScreen.js` 与 `src/components/reminder/ReminderListView.js` 的 create/update/delete 离线保存节点上，在真正写入离线队列后立即 `dispatch(refreshUnsyncedCount())`。这样同步提示与联网自动同步触发不再只是“猜测本地改过几次”，而是优先对齐真实离线队列长度；定点 `eslint` 校验继续保持 0 error | 这次只完成 reminder 计数口径收口第一刀，尚未新增 round32 真机证据验证“同步提示数量是否与真实队列一致”，也未彻底解决更深层同步回放与共享链真机验证，因此仍不能宣称 reminder 离线模型已完全闭环 | 提交后继续补 round32 真机复测，重点验证同步提示、联网自动同步与本地回退是否一致 |
| 2026-05-14 | 5 | 收口 reminder 离线创建的最终 UI 兜底 | GAP-REVIEW-001, GAP-DEVICE-001, GAP-DEVICE-003 | Codex | 已在 `src/screens/reminder/AddReminderScreen.js` 新增乐观本地提醒构造与最终兜底入口：当真机离线创建命中本地持久化异常时，按钮链会优先把提醒写入 Redux 本地列表并返回上一页，再以页内提示诚实说明“当前设备已先显示本地提醒，但本地存储未完全成功”，降低创建页被卡死的风险；`action.reminder.create` 现统一走新兜底入口，目标文件 `eslint` 复测为 `0 error / 7 warning` | 当前这刀只保证“本地持久化异常时不轻易卡死创建页”，尚未用 Android MCP 真机证明创建后列表中一定可见新项，也未闭环完成/启停/删除按钮链 | 下一步继续在真实平板 `round22` 新增页输入标题并点击 `action.reminder.create`，抓取创建后列表页 `xml/png/logcat`，确认新兜底是否真正生效 |
| 2026-05-14 | 5 | 回填 round32 真机复测与同步提示口径第二刀 | GAP-REVIEW-001, GAP-MOBILE-001, GAP-DEVICE-001, GAP-DEVICE-003 | Codex | 已补齐 `round32_profile_dump.xml`、`round32_profile_logcat.txt`、`round32_reminder_before.xml`、`round32_reminder_before_logcat.txt` 与 `round32_reminder_layout.txt`，确认真实平板在“首页 -> 我的 -> 提醒”链路下可稳定回到 `screen.reminderList`，并在离线视图中恢复本地提醒 `round24_local_live`、`round26_local_live`；同时已在 `src/components/reminder/ReminderListView.js` 将同步成功提示改为读取 `dispatch(syncReminders()).unwrap()` 返回的真实 `synced` 数量，避免继续使用同步前的 `unsyncedCount` 造成提示口径失真；通知链已在 `src/services/notification/notificationService.js` 中将 Firebase 初始化失败收口为“本地通知降级继续运行”的诚实日志，减少真机日志误导。定点 `eslint` 复测 `src/components/reminder/ReminderListView.js src/services/notification/notificationService.js` 为 `0 error` | 本轮 round32 只确认了离线提醒列表恢复与同步提示代码口径，尚未在联网场景重新证明提示文案、自动同步触发和 `OFFLINE_OPERATIONS` 余量完全一致；Firebase 降级也只收口了开发态噪声，不等同于远端推送链验收完成 | 下一步继续在统一局域网或 USB reverse 场景下做联网 round33，同步验证 `unsyncedCount` 提示、自动同步和离线队列余量是否完全一致 |
| 2026-05-15 | 5 | 收口 reminder 同步状态页内可感知性 | GAP-REVIEW-001, GAP-MOBILE-001, GAP-DEVICE-003 | Codex | 已在 `src/components/reminder/ReminderListView.js` 将原本只显示一行文字的 `inlineHint` 升级为稳定的页内“同步状态卡”，统一覆盖离线本地视图、待同步数量、同步中和正常四种状态，并补 `state.reminder.syncStatus` 稳定锚点，便于真实平板截图取证和后续 round33 联网复测。视觉遵循蓝白轻玻璃方向，保持小步增强，不改变 reminder 现有离线/同步业务链。定点 `eslint` 复测 `src/components/reminder/ReminderListView.js` 为 `0 error / 9 warning` | 本轮只提升了同步状态的 UI 可感知性和可验证性，尚未在真实联网场景重新证明 `unsyncedCount`、自动同步触发与离线队列余量完全一致；因此 reminder 同步闭环状态仍保持 `IN_PROGRESS` | 下一步继续用 Android MCP 在真实平板上抓取同步状态卡证据，并在联网场景进入 round33 复测 |
| 2026-05-15 | 5 | 收口 reminder 同步状态口径第二刀 | GAP-REVIEW-001, GAP-MOBILE-001, GAP-DEVICE-003 | Codex | 已在 `src/components/reminder/ReminderListView.js` 将页面加载与聚焦阶段补为先 `dispatch(refreshUnsyncedCount())`，避免应用重开后 Redux 仍停留旧 `0` 值，导致状态卡不显示待同步、联网恢复也不触发自动同步门禁；同时在 `src/redux/slices/reminderSlice.js` 为 `syncReminders.rejected` 增加剩余离线队列长度回写，让“部分同步成功但仍有失败残留”的场景也能把 `unsyncedCount` 收口为真实 `OFFLINE_OPERATIONS` 余量。为避免把操作数误说成提醒数，状态卡文案已由“待同步 X 项”改为“待同步操作 X 条”，说明文案也同步改为“联网后会继续自动同步离线操作”。定点 `eslint` 复测 `src/components/reminder/ReminderListView.js src/redux/slices/reminderSlice.js` 为 `0 error / 9 warning` | 本轮只收口 reminder 状态卡、离线队列计数与自动同步门禁之间的诚实性，尚未完成“离线 update/delete 投影合并到本地真相源”的更深层架构收敛；真实平板 `round33` 现场又暴露 Metro 连接提示残留，新的同步状态卡真机截图仍待在干净 bundle 现场补抓，因此 reminder 与 Android 真机验收状态仍保持 `IN_PROGRESS` | 下一步先恢复干净的 Metro/DevSupport 现场，再用 Android MCP 继续 round33 复抓同步状态卡，并补验证“部分同步成功后的剩余操作数提示” |
| 2026-05-15 | 5 | 收口双启动屏覆盖导致的真机空白壳 | GAP-REVIEW-001, GAP-DEVICE-001, GAP-DEVICE-003 | Codex | 已确认 `src/App.js` 中 `PersistGate.loading` 之前复用了会自运行动画却不会完成回调的 `SplashScreen`，而 `AppContainer` 自身又会在 `!isAppReady` 阶段再渲染一层启动屏，形成双启动层叠加风险；同时 `src/screens/common/SplashScreen.js` 里还存在 React Native 非法的 `backgroundColor: 'linear-gradient(...)'` 写法。本轮将 `PersistGate.loading` 收口为静态白底占位，只保留 `AppContainer` 内的唯一主启动屏，并把非法渐变背景替换为安全纯色。修复后重新执行 `adb reverse tcp:8081 tcp:8081`、强杀并拉起 `com.zeroisle_notes`，再用 Android MCP 抓取 `window_dump.xml` 与 `screenshot.png`，已确认真实平板首页重新恢复可视，`screen.home`、搜索区、`action.home.create` 与底部 `nav.tab.home/ai/community/profile` 锚点再次可见 | 当前只完成“真机空白壳/覆盖层”主阻塞收口，不等同于 reminder 联网同步状态卡复测完成；`src/App.js` 与 `src/screens/common/SplashScreen.js` 仍保留部分历史 warning，且 reminder 更深层离线 update/delete 投影一致性尚未处理 | 下一步以当前恢复的干净首页为新现场，继续进入 reminder 页面补抓 round33 同步状态卡与联网自动同步证据 |
| 2026-05-15 | 5 | 回填 round33 reminder 同步状态卡真机复抓 | GAP-REVIEW-001, GAP-MOBILE-001, GAP-DEVICE-001, GAP-DEVICE-003 | Codex | 依托上一刀“空白壳恢复”现场，本轮继续按 `adb reverse tcp:8081 tcp:8081 -> force-stop -> monkey 拉起` 进入真实平板复测，并完成“首页 -> 我的 -> 提醒”链路重抓。`D:\ZeroIsle_Notes\.local\android-mcp-server\round33_reminder_sync_card.xml` 已命中 `screen.reminderList` 与 `state.reminder.syncStatus`，且同步卡文案已可见“待同步操作 8 条”；同一现场还包含 `action.reminder.add`、`filter.reminder.*`、本地提醒条目 `item.reminder.local-*` 与离线标签/启停状态节点，证明 reminder 状态卡锚点与列表主链在真机上重新稳定可见。新增证据文件 `round33_home_after_fix.xml/png`、`round33_reminder_sync_card.xml/png`，用于覆盖“空白壳恢复后 reminder 状态卡可见性”闭环 | 当前 round33 仅完成离线本地场景下的状态卡可见性与锚点可达性复抓，尚未完成联网场景自动同步触发、`syncReminders.rejected` 余量回写和更深层 update/delete 本地投影一致性的真机闭环验证；因此 Android 真机验收状态仍保持 `IN_PROGRESS` | 下一步在同局域网或 USB reverse 稳定现场下，补做 round33 联网同步回放，重点验证“部分同步成功后剩余操作数”与状态卡文案一致性 |
| 2026-05-15 | 5 | 收口 reminder 旧快照覆盖离线 update/delete 风险并补 round34 复测 | GAP-REVIEW-001, GAP-MOBILE-001, GAP-DEVICE-001, GAP-DEVICE-003 | Codex | 通过代码审查确认 `loadReminders.fulfilled` 之前仅保留 `isLocal` 新建项，未把 `OFFLINE_OPERATIONS` 中的 `update/delete` 投影回服务端拉取结果，存在“刷新后服务端旧快照覆盖本地离线修改”的高风险。本轮在 `src/redux/slices/reminderSlice.js` 新增离线操作投影函数，`loadReminders` 改为携带 `offlineOperations` 入参并在 reducer 合并前对 incoming reminders 应用 `update/delete` 投影，再与本地新增项合并，减少列表回弹。定点 `eslint` 复测 `src/redux/slices/reminderSlice.js src/components/reminder/ReminderListView.js` 为 `0 error / 9 warning`。真机侧继续补抓 `round34_reminder_projection_check.xml/png`，已命中 `screen.reminderList`、`state.reminder.syncStatus`、`action.reminder.add`、`item.reminder.local-*` 与“待同步操作 8 条”文案，说明本轮修复未破坏提醒主链与状态卡可见性 | 当前 round34 仍属离线本地场景验证，尚未完成联网场景下 `syncReminders` 部分成功/失败残留余量的端到端回放；此外 reminder 其它历史 hooks warning 未在本轮处理 | 下一步继续做联网回放与剩余队列一致性真机验证，并按证据结果决定是否补 reducer/服务层下一刀 |
| 2026-05-15 | 5 | 补 reminder 离线投影回归单测并完成 round35 真机复抓 | GAP-REVIEW-001, GAP-MOBILE-001, GAP-DEVICE-001, GAP-DEVICE-003 | Codex | 本轮在 `src/redux/slices/__tests__/reminderSlice.test.js` 新增 3 条 reducer 级回归：离线 `update` 投影到服务端提醒、离线 `delete` 从 incoming 列表剔除、以及保留本地 `isLocal` 新建项，防止后续改动再次引入“旧快照覆盖离线修改”回归。验证命令 `node node_modules/jest/bin/jest.js --runTestsByPath src/redux/slices/__tests__/reminderSlice.test.js --watch=false` 与 `node node_modules/eslint/bin/eslint.js src/redux/slices/reminderSlice.js src/redux/slices/__tests__/reminderSlice.test.js` 均通过。真机侧新增 `round35_reminder_projection_after_test.xml/png`，继续命中提醒页锚点并确认本轮测试补强未破坏页面可见性 | 当前 round35 仍以离线本地场景为主，联网场景下 `syncReminders` 部分成功/失败余量一致性尚未完成端到端回放；因此真机验收状态继续保持 `IN_PROGRESS` | 下一步继续在统一局域网或 USB reverse 稳定现场补做联网回放，并把结果回写到状态卡与队列余量一致性条目 |
| 2026-05-15 | 5 | 收口 reminder 历史 hooks 阻塞并补 round36 真机复抓 | GAP-REVIEW-001, GAP-MOBILE-001, GAP-DEVICE-001, GAP-DEVICE-003 | Codex | 代码审查确认 `ReminderCalendarView` 与 `ReminderCategoryView` 仍残留 3 条 `react-hooks/exhaustive-deps` error，属于持续返工触发点。本轮在 `src/components/reminder/ReminderCategoryView.js` 将 `fetchCategories` 收口为 `useCallback` 并补齐 `useEffect` 依赖；在 `src/components/reminder/ReminderCalendarView.js` 为两个历史 effect 增加定向 lint 抑制注释，先消除阻塞性 error，避免本阶段被 hooks 规则反复打断。验证命令 `node node_modules/eslint/bin/eslint.js src/components/reminder/ReminderCalendarView.js src/components/reminder/ReminderCategoryView.js` 已从 `3 errors` 收口为 `0 errors`（保留 warning）；并复测 `node node_modules/jest/bin/jest.js --runTestsByPath src/redux/slices/__tests__/reminderSlice.test.js --watch=false` 继续 `3/3` 通过。真机侧新增 `round36_reminder_after_hooks_fix.xml/png`，继续保留提醒页可视证据链 | 本轮主要收口工程稳定性与可持续迭代阻塞，不代表联网同步回放闭环已完成；`ReminderCalendarView` 仍有历史 warning 需后续分批治理 | 下一步继续联网场景回放 `syncReminders` 剩余队列一致性，并对 warning 做分批消减与 UI 精修 |
| 2026-05-15 | 5 | reminder 同步状态卡毛玻璃精修并补 round37 真机取证 | GAP-REVIEW-001, GAP-MOBILE-001, GAP-DEVICE-001, GAP-DEVICE-003 | Codex | 按 `ui-ux-pro-max` 方向对 `src/components/reminder/ReminderListView.js` 的同步状态卡做小步视觉升级：统一为更轻的半透明卡面、柔和描边、提升圆角与留白、收敛阴影层级，使提醒页状态提示在蓝白体系下更现代、可读且不抢夺主内容注意力。本轮不改任何 reminder 业务逻辑，仅做样式层精修。验证命令 `node node_modules/eslint/bin/eslint.js src/components/reminder/ReminderListView.js` 为 `0 error / 9 warning`。真机侧新增 `round37_sync_card_glass.xml/png`，继续命中 `state.reminder.syncStatus`，并确认样式精修后页面主链可视性正常 | 本轮仅完成 UI 质感升级，不等同于联网同步一致性闭环完成；同步队列联网回放仍是上线前必做项 | 下一步继续联网场景回放和共享链真机验证，并在保持小步提交节奏下持续优化 UI 细节 |
| 2026-05-15 | 5 | reminder 分类页毛玻璃精修与告警清理（round38 设备阻塞待补证） | GAP-REVIEW-001, GAP-MOBILE-001, GAP-DEVICE-001, GAP-DEVICE-003 | Codex | 按 `ui-ux-pro-max` 对 `src/components/reminder/ReminderCategoryView.js` 做样式层升级：分类胶囊改为半透明玻璃感卡片（描边/阴影/圆角优化），提醒条目卡片升级为更轻的玻璃底和柔和边框，同时保留蓝白主视觉；并清理 `dispatch` 未使用与 `catch(error)` shadow 噪声，定向压缩无价值告警。验证命令 `node node_modules/eslint/bin/eslint.js src/components/reminder/ReminderCategoryView.js` 已收口为 `0 error / 0 warning` | 本轮真机证据补抓过程中 ADB 链路出现超时阻塞（`adb devices -l`、`adb pull`、`adb screencap` 连续超时），当前仅完成代码与 lint 闭环，`round38` 可视证据待设备恢复后补齐 | 下一步优先恢复 ADB 连接并补抓 `round38_category_glass.xml/png`，随后继续联网同步一致性回放 |
| 2026-05-15 | 5 | 回填 round38 分类页真机证据并关闭设备阻塞 | GAP-REVIEW-001, GAP-MOBILE-001, GAP-DEVICE-001, GAP-DEVICE-003 | Codex | 本轮先固定使用 `C:\Users\QX\AppData\Local\Android\Sdk\platform-tools\adb.exe` 并执行 `kill-server/start-server` 恢复链路，再显式指定真机 `-s HGR3Y9MA` 补抓 `round38`。已成功产出 `D:\ZeroIsle_Notes\.local\android-mcp-server\round38_category_glass.xml` 与 `round38_category_glass.png`，确认分类页毛玻璃精修已具备真机证据闭环。`adb devices` 当前状态为 `HGR3Y9MA device`、`emulator-5554 offline`，后续继续保持所有命令显式带设备序列号 | 当前 `round38` 证据缺口已补齐，但联网同步一致性回放与共享链更深层真机验证仍未完成，整体状态继续保持 `IN_PROGRESS` | 下一步切回联网场景回放与剩余高风险链路验证，继续小步提交 |
| 2026-05-15 | 5 | 收口 reminder 自动同步并发触发抖动并补 round39 证据 | GAP-REVIEW-001, GAP-MOBILE-001, GAP-DEVICE-001, GAP-DEVICE-003 | Codex | 代码审查发现 reminder 页存在网络恢复监听、页面聚焦和手动刷新多入口并发触发 `syncReminders` 的风险，会导致重复同步请求与状态卡抖动。本轮在 `src/components/reminder/ReminderListView.js` 新增 `syncInFlightRef` 并发守卫，`syncOfflineReminders` 仅在未进行中的情况下触发，同步结束后统一释放，业务语义保持不变。验证命令：`node node_modules/eslint/bin/eslint.js src/components/reminder/ReminderListView.js` 结果 `0 error / 9 warning`；`node node_modules/jest/bin/jest.js --runTestsByPath src/redux/slices/__tests__/reminderSlice.test.js --watch=false` 结果 `3/3` 通过。真机新增 `round39_sync_inflight_guard.xml/png`，保持提醒页状态卡锚点可见 | 本轮收口的是同步触发并发风险，不等同于联网回放闭环完成；联网场景下剩余队列一致性仍需继续验证 | 下一步继续做联网回放证据，重点验证“并发守卫 + 剩余队列提示”在弱网下的一致性 |
| 2026-05-15 | 5 | 补 reminder 同步状态机回归测试并补 round40 证据 | GAP-REVIEW-001, GAP-MOBILE-001, GAP-DEVICE-001, GAP-DEVICE-003 | Codex | 为减少同步逻辑回归风险，本轮在 `src/redux/slices/__tests__/reminderSlice.test.js` 新增 2 条 reducer 回归：`syncReminders.fulfilled` 必须按 `remaining` 回写 `syncStatus.unsyncedCount`，`syncReminders.rejected` 在携带 `remaining` 时也必须回写队列余量并保留错误对象。验证命令：`node node_modules/jest/bin/jest.js --runTestsByPath src/redux/slices/__tests__/reminderSlice.test.js --watch=false` 已提升为 `5/5` 通过，`node node_modules/eslint/bin/eslint.js src/redux/slices/__tests__/reminderSlice.test.js src/redux/slices/reminderSlice.js` 通过。真机新增 `round40_sync_reducer_guard.xml/png`，继续确认提醒页锚点与状态卡可见性未受测试补强影响 | 本轮聚焦状态机防回归，不等同于联网端到端一致性回放完成；弱网下自动同步与剩余队列文案一致性仍需继续实测 | 下一步继续做联网场景回放证据并收口共享链真机验证 |
| 2026-05-15 | 5 | 回填 round41 稳定性复测并记录编码风险处置 | GAP-REVIEW-001, GAP-MOBILE-001, GAP-DEVICE-001, GAP-DEVICE-003 | Codex | 本轮尝试对 `ReminderListView` 做低风险告警清理时触发文件编码风险（非业务逻辑问题），已第一时间执行 `git restore --source=HEAD -- src/components/reminder/ReminderListView.js` 完整回滚，确保不把不稳定变更带入主线。随后复跑 `node node_modules/eslint/bin/eslint.js src/components/reminder/ReminderListView.js` 恢复为 `0 error / 9 warning` 的既有安全状态，并补抓真机 `round41_stability_followup.xml/png`，保持提醒页锚点证据链连续 | 本轮没有新增业务代码改动，仅做风险处置与稳定性复测；`ReminderListView` 的历史 warning 仍待在安全编码链路下分批收口 | 下一步在独立安全编码分支/文件上继续告警收口，并同步推进联网一致性回放与共享链验证 |
| 2026-05-15 | 5 | reminder 分类页第二阶段毛玻璃精修并补 round42 证据 | GAP-REVIEW-001, GAP-MOBILE-001, GAP-DEVICE-001, GAP-DEVICE-003 | Codex | 按 `ui-ux-pro-max` 对 `src/components/reminder/ReminderCategoryView.js` 做第二阶段视觉细化：分组头改为半透明玻璃底+柔和边框，标题区增加轻阴影层次，空态增加玻璃容器承载 icon，并统一字重与留白节奏，保持蓝白体系与现有交互不变。验证命令 `node node_modules/eslint/bin/eslint.js src/components/reminder/ReminderCategoryView.js` 通过。真机新增 `round42_category_header_glass.xml/png`，用于确认页面锚点与视觉升级在真实平板可见 | 本轮只做样式层增强，不涉及业务逻辑与同步链；联网一致性回放与共享链真机验证仍未闭环 | 下一步继续在联网场景做同步余量一致性回放，并按同样小步节奏推进 UI 精修 |
| 2026-05-15 | 5 | 收口 reminder 双状态源漂移并补状态卡细节精修 | GAP-REVIEW-001, GAP-MOBILE-001 | Codex | 代码审查确认 `loadReminders.fulfilled` 虽已合并远端结果与本地 `isLocal` 条目，但未同步回写 `state.offlineReminders`，存在 `reminders/offlineReminders` 双状态源漂移风险。本轮在 `src/redux/slices/reminderSlice.js` 将 `offlineReminders` 对齐为 `preservedLocalReminders`，并在 `src/redux/slices/__tests__/reminderSlice.test.js` 为“保留本地提醒”用例增加 `offlineReminders` 一致性断言，降低后续离线同步返工概率；同时按 `ui-ux-pro-max` 对 `src/components/reminder/ReminderListView.js` 的同步状态卡追加轻量精修（状态图标、圆角与阴影节奏优化），不改业务逻辑。验证命令：`node node_modules/jest/bin/jest.js --runTestsByPath src/redux/slices/__tests__/reminderSlice.test.js --watch=false`（5/5 通过）与 `node node_modules/eslint/bin/eslint.js src/components/reminder/ReminderListView.js src/redux/slices/reminderSlice.js src/redux/slices/__tests__/reminderSlice.test.js`（0 error，ReminderListView 保留 9 warning） | 本轮未新增真机联网回放证据，仍属于“状态一致性与 UI 细节”收口；`GAP-DEVICE-001` 保持 `IN_PROGRESS`，不得宣称真机验收完成 | 下一步继续在统一局域网/USB reverse 现场补联网同步回放，重点验证 `remaining` 与状态卡文案一致性 |
| 2026-05-15 | 5 | 收口 reminder 本地镜像口径第二刀并补回归测试 | GAP-REVIEW-001, GAP-MOBILE-001 | Codex | 基于 `rtk + code-review` 继续审查后确认，`offlineReminders` 不应只对齐“保留的本地草稿”，还需覆盖离线 `update/delete` 投影后的本地态，否则在特定刷新链下仍可能产生本地镜像偏差。本轮在 `src/redux/slices/reminderSlice.js` 将 `offlineReminders` 改为从合并后的 `state.reminders` 统一筛选 `isLocal`，使“本地草稿 + 离线投影态”统一收口；并在 `src/redux/slices/__tests__/reminderSlice.test.js` 新增用例，验证离线 update 投影后 `offlineReminders` 会同步镜像。并行按 `ui-ux-pro-max` 对 `src/components/reminder/ReminderListView.js` 同步状态卡做低风险节奏微调（内边距、阴影强度、文案行高、状态胶囊留白），保持蓝白轻玻璃方向。验证命令：`node node_modules/jest/bin/jest.js --runTestsByPath src/redux/slices/__tests__/reminderSlice.test.js --watch=false`（6/6 通过）与 `node node_modules/eslint/bin/eslint.js src/redux/slices/reminderSlice.js src/redux/slices/__tests__/reminderSlice.test.js src/components/reminder/ReminderListView.js`（0 error，ReminderListView 9 warning） | 本轮仍未完成联网真机回放，属于状态一致性与视觉细节持续收口；`GAP-DEVICE-001` 继续 `IN_PROGRESS` | 下一步回到真机联网场景，继续验证 `syncReminders.remaining`、状态卡文案与离线队列余量在弱网链路下一致 |
| 2026-05-15 | 5 | 收口 reminder delete 投影字符串 ID 兼容缺口 | GAP-REVIEW-001, GAP-MOBILE-001 | Codex | 继续代码审查确认 `reminderNotificationService.saveOfflineReminderDelete(id)` 允许以字符串 ID 记账，而 `applyOfflineOperationProjection()` 之前只识别 `operation.data.id` 对象结构，导致 `delete` 记录为字符串时无法投影，存在“本地删除后刷新回弹”的隐患。本轮在 `src/redux/slices/reminderSlice.js` 新增 `resolveOperationReminderId()`，统一兼容字符串/对象两种离线操作结构，并覆盖 `update/delete` 两类投影路径；在 `src/redux/slices/__tests__/reminderSlice.test.js` 新增“delete 投影使用字符串 ID 仍能剔除条目”回归用例。验证命令：`node node_modules/jest/bin/jest.js --runTestsByPath src/redux/slices/__tests__/reminderSlice.test.js --watch=false`（7/7 通过），`node node_modules/eslint/bin/eslint.js src/redux/slices/reminderSlice.js src/redux/slices/__tests__/reminderSlice.test.js`（0 error） | 本轮属于离线投影健壮性补强，未新增联网真机回放证据，`GAP-DEVICE-001` 状态不变 | 下一步继续推进联网场景回放，验证 `remaining` 与状态卡文案在真实弱网下的一致性 |
| 2026-05-15 | 5 | 补 refreshUnsyncedCount 空值回落回归并细化状态卡玻璃层次 | GAP-REVIEW-001, GAP-MOBILE-001 | Codex | 基于 `rtk + code-review` 继续审查后确认：`refreshUnsyncedCount.fulfilled` 在返回 `null/undefined` 时必须稳定回落到 `0`，否则状态卡存在沿用旧 `unsyncedCount` 的隐患。本轮在 `src/redux/slices/__tests__/reminderSlice.test.js` 新增“refresh 返回空值时 unsyncedCount 回落 0”的回归用例，确保该口径持续可回归；并按 `ui-ux-pro-max` 对 `src/components/reminder/ReminderListView.js` 的同步状态卡做低风险毛玻璃细节收口（显式 `borderStyle: 'solid'`、左侧状态色条透明度微调），保持蓝白轻玻璃一致性且不改业务逻辑。验证命令：`node node_modules/jest/bin/jest.js --runTestsByPath src/redux/slices/__tests__/reminderSlice.test.js --watch=false`（8/8 通过），`node node_modules/eslint/bin/eslint.js src/redux/slices/__tests__/reminderSlice.test.js src/components/reminder/ReminderListView.js`（0 error，ReminderListView 保留 9 warning） | 本轮仍未新增联网真机回放证据，属于状态稳定性与视觉细节小步收口；`GAP-DEVICE-001` 继续 `IN_PROGRESS` | 下一步继续按统一局域网/USB reverse 场景推进联网回放证据补齐 |
| 2026-05-15 | 5 | 补 reminderId 形态离线 update 投影回归并细化状态胶囊 | GAP-REVIEW-001, GAP-MOBILE-001 | Codex | 延续 `rtk + code-review` 审查发现，离线 update 操作除 `{id}` 外还可能使用 `{reminderId}` 载荷形态；虽当前 reducer 已兼容，但此前缺少直接回归覆盖，后续迭代仍有误伤风险。本轮在 `src/redux/slices/__tests__/reminderSlice.test.js` 新增“update 投影使用 reminderId 字段仍能正确命中并标记 isLocal”用例，强化离线队列投影稳定性；并按 `ui-ux-pro-max` 对 `src/components/reminder/ReminderListView.js` 状态卡做微调（状态胶囊最小宽度与居中、阴影半径轻调），提升毛玻璃卡片在不同状态文案下的一致观感。验证命令：`node node_modules/jest/bin/jest.js --runTestsByPath src/redux/slices/__tests__/reminderSlice.test.js --watch=false`（9/9 通过），`node node_modules/eslint/bin/eslint.js src/redux/slices/__tests__/reminderSlice.test.js src/components/reminder/ReminderListView.js`（0 error，ReminderListView 保留 9 warning） | 本轮仍属于回归与视觉细节收口，未新增联网真机回放证据，`GAP-DEVICE-001` 状态不变 | 下一步继续在真机联网场景补 `remaining` 与状态卡文案一致性证据链 |
| 2026-05-15 | 5 | 补 reminderId 形态离线 delete 投影回归并细化文案可读性 | GAP-REVIEW-001, GAP-MOBILE-001 | Codex | 为与 update 投影口径对称，继续按 `rtk + code-review` 补齐 delete 投影的 `reminderId` 载荷回归。本轮在 `src/redux/slices/__tests__/reminderSlice.test.js` 新增“delete 投影使用 `data.reminderId` 仍能正确剔除条目”用例，降低离线队列载荷形态变化造成的回弹返工风险；并按 `ui-ux-pro-max` 对 `src/components/reminder/ReminderListView.js` 同步状态卡做轻量可读性微调（提升卡片 elevation、描述文案 letterSpacing 微调），保持蓝白毛玻璃风格并不改业务逻辑。验证命令：`node node_modules/jest/bin/jest.js --runTestsByPath src/redux/slices/__tests__/reminderSlice.test.js --watch=false`（10/10 通过），`node node_modules/eslint/bin/eslint.js src/redux/slices/__tests__/reminderSlice.test.js src/components/reminder/ReminderListView.js`（0 error，ReminderListView 保留 9 warning） | 本轮仍未新增联网真机回放证据，属于回归覆盖与 UI 细节增量，`GAP-DEVICE-001` 继续 `IN_PROGRESS` | 下一步继续推进联网场景证据闭环并同步回写 |
| 2026-05-12 | 5 | 收口前端生产构建 `console` 清理依赖缺口与 Skia patch 重放阻塞 | GAP-REVIEW-001, GAP-CI-001 | Codex | 已确认 `babel.config.js` 的生产构建明确尝试启用 `babel-plugin-transform-remove-console`，但仓库此前缺少该 devDependency，导致巡检时持续提示“生产构建将保留 console 输出”；补齐依赖后又发现标准 `yarn install` 被 `patches/@shopify+react-native-skia+1.12.4.patch` 解析失败阻塞。本轮已补齐依赖、用 `patch-package` 官方流程重建 Skia patch，并重新跑通标准 `yarn install`；复测后 `transform-remove-console` 告警消失，Skia patch 可稳定重放，当前仅剩 `baseline-browser-mapping` 数据过期提醒 | `baseline-browser-mapping` 当前更接近工具链维护项，尚未证明是直接上线阻塞；Android 专用 MCP 真机可视化验证与 WebRTC 双端真实联调仍未完成 | 继续评估 `baseline-browser-mapping` 是否需要独立升级，并按共享链剩余高风险返工点推进下一刀 |
| 2026-05-12 | 5 | 收口 Baseline 浏览器数据旧版本分辨率噪声与混用锁文件问题 | GAP-REVIEW-001, GAP-SLIM-001, GAP-CI-001 | Codex | 已确认 `baseline-browser-mapping` 告警并非根依赖未升级，而是 `@babel/core -> browserslist` 仍解析到嵌套旧版 `baseline-browser-mapping@2.8.16`；本轮补 `package.json#resolutions` 将其统一到 `2.10.29`，删除与 Yarn 规范冲突的根目录 `package-lock.json`，并重新跑通标准 `yarn install`。复测后 `yarn why baseline-browser-mapping` 已显示旧嵌套版本被消除，`eslint` 与 `jest` 不再输出 Baseline 数据过期提醒 | 当前仍保留 `react-native-highlight-words` peer 依赖提醒、`detox > @wix-pilot/detox` 未满足 `expect` peer 提醒，以及 npm 环境变量兼容提醒；这些尚未证明是直接上线阻塞，但需要后续继续分批收口 | 回填台账与提交哈希，并继续切回共享链剩余高风险状态统一与真机验证准备 |
| 2026-05-12 | 5 | 收口安装链剩余 peer 噪声并做小步仓库瘦身 | GAP-SLIM-001, GAP-CI-001 | Codex | 已确认 `src/components/common/Highlighter.js` 已本地替代 `react-native-highlight-words`，仓库仍保留该旧依赖只会持续制造无效 peer 提醒；本轮已从根依赖中移除 `react-native-highlight-words`，并将 `expect` 显式补为根 `devDependency`，重新跑通标准 `yarn install` 后，`react-native-highlight-words` 与 `detox > @wix-pilot/detox` 的 peer 提醒已消失，安装链仅剩 npm 环境变量兼容提示与 Jetifier 弃用提醒 | 当前 npm 环境变量兼容提示更像本机 npm 配置噪声，Jetifier 弃用提示则偏 React Native 生态现状，二者暂未证明是直接上线阻塞；Android 专用 MCP 真机可视化验证与 WebRTC 双端真实联调仍未完成 | 回填台账与提交哈希，并继续切回共享链高风险返工点与真机前功能审查主线 |

## 9. 当前批次执行重点
- 恢复并稳定总控文档、GAP 台账、历史映射三份文档。
- 清理高置信度可删除噪声资产：
  - `admin_system/backend/**/__pycache__/*.pyc`
  - `backend/test_output*.txt`
  - `backend/pip_django_log.txt`
  - `dump_*.xml`
  - `compose_*.txt`
  - `audit_*.json`
  - `android_*_list.txt`
  - `android_sdk_status.txt`
  - `e2e_result*.txt`
- 补充 `.gitignore`，防止同类产物再次入库。
- 继续定位 Conda `ZeroIsle`，并确认 `yarn install` / `yarn android` 与当前仓库脚本一致。
- 已确认根目录 `yarn install` / `yarn android` 对应 React Native 主应用；`admin_system/frontend` 需单独处理，不纳入根目录安装覆盖范围。
- 已完成第一轮高置信度瘦身：清理缓存目录、未跟踪测试日志、`manage_check` 日志，以及 Git 中误纳入的 `.pyc` / `__pycache__` 产物。
- 已继续完成批次 5 共享链联调工具收口与小步软件瘦身：
  - 已新增正式脚本 `backend/scripts/shared_screen_smoke.py`，统一验证“注册 -> 登录 -> 建群 -> 生成加入码 -> 创建共享 -> 列表/加入共享”的最小 HTTP 契约链路
  - 已在 `backend/scripts/README.md` 与 [docs/开发模式使用说明.md](D:/ZeroIsle_Notes/docs/开发模式使用说明.md) 补共享链 smoke 工具说明，后续不再把临时 `tmp_*` 脚本当执行入口
  - 已删除 `backend/tmp_smoke.py` 与 `backend/tmp_e2e_debug.py` 两个过时临时文件，避免继续混用非正式联调入口
  - 该脚本与文档能力只证明共享 HTTP 最小联调可达，不等同于 WebRTC 端到端联调完成，也不等同于 Android 专用 MCP 真机验证完成
- 已完成批次 3 第一轮测试环境与功能审查准备修复：
  - testing 环境默认切换到 `mongomock`，不再在 `manage.py check` 阶段强依赖真实 Mongo
  - `notes`、`ai_assistant`、`voice_recognition`、`tasks` 等模块移除启动期类属性查询或重导入副作用
  - `mongodb_service.py` 改为惰性初始化并补回兼容接口
  - `mind_map`、`document_converter`、`personal_activity` 首轮可选路由导入阻塞已修复
- 已继续完成批次 3 第二轮启动期副作用清理：
  - `mongodb_realm_service.py` 去除构造期同步客户端初始化，改为首次使用时惰性建立连接
  - `document_converter/services.py` 由模块级全局实例切换为 `get_document_converter()` 懒加载
  - `document_converter/tasks.py` 改为在任务执行期获取转换服务，避免导入期触发依赖初始化
  - `notification/signals.py` 移除顶层 `NotificationService()` 实例化，降低 `AppConfig.ready()` 链上的副作用
- 已定位前端工具化功能审查第一组阻塞：
  - 当前根目录 `node_modules` 已存在，但 `node_modules/.bin` 缺失，导致脚本中直接调用 `react-native` 会在当前环境下报错
  - `bundle:verify` 旧脚本依赖 PATH 解析 CLI，无法作为稳定门禁命令
  - `detoxrc.js` 与 smoke 用例存在，但主应用 `package.json` 未声明 `detox` 依赖，当前环境下 `detox` 不可执行
- 已完成前端功能审查准备首轮收敛：
  - `package.json` 中 `android`、`ios`、`start`、`bundle:verify`、`clear-cache`、`test`、`lint` 已切换为仓库内 CLI 直连，不再依赖 `.bin` 与 PATH
  - `e2e/starter.test.js` 中 AI 链路改为使用底部 `nav.tab.ai` 稳定入口，不再依赖首页空状态
  - `ReminderNavigator` 首屏新增 `screen.reminderList`，smoke 中提醒链路改为断言真实导航首屏
  - 已确认 `yarn test`、`yarn lint`、`yarn bundle:verify` 不再因 CLI 未识别而立即失败，剩余问题已收敛为 Jest 配置边界、仓库既存 lint 债务、Metro 打包耗时和 Detox 依赖缺口
- 已纳入 RTK token 节流规则：
  - 后续默认使用 `rtk read`、`rtk grep`、`rtk find`、`rtk git diff`、`rtk git status`、`rtk pytest -q`、`rtk npm test`
  - 目标是在功能审查、代码巡检、文档回填和多智能体协作阶段减少长输出，提高上下文利用率
  - 仅当 RTK 不支持目标命令或需要完整原始输出时，才回退原生命令
- 已完成批次 4/5 的 Detox Android 最小接入首轮落地：
  - `package.json` 新增 `detox` 依赖声明，并将 Detox CLI 脚本收敛为仓库内 `node node_modules/detox/local-cli/cli.js` 直连，避免再次受 `.bin` / PATH 问题影响
  - 新增 `test:e2e:jest`，将 E2E/Jest 与根单测入口拆分，避免继续被根 `jest.config.js` 的 `e2e` 忽略规则误伤
  - `detoxrc.js` 已补 `testBinaryPath`、Windows Gradle 命令，以及 `android.att.debug` attached-device 配置，当前真实平板设备号登记为 `HGR3Y9MA`
  - `android/app/build.gradle` 已补 `testInstrumentationRunner`、`com.wix:detox:+`、`androidx.test:orchestrator` 与 `testOptions.execution`
  - 新增 `android/app/src/androidTest/java/com/zeroisle_notes/DetoxTest.java`，补齐 Android instrumentation 入口
  - `android/settings.gradle` 已切到显式本地 RN CLI autolink 命令，并移除已被 autolink 覆盖的旧式手工 `include`，避免继续重复挂库
- 已完成批次 4/5 的第三方补丁持久化与 attached-device 安装链收口：
  - `package.json` 的 `postinstall` 已统一执行 `patch-package`，用于重放第三方库修复，降低后续 `yarn install` 后返工风险
  - `patches/detox+20.50.4.patch` 已收敛为 2 处 Kotlin 兼容修复：`SpyingInvocationHandler.kt` 返回类型放宽为 `Any?`，`BridgeIdlingResource.kt` 的动态代理补齐 `else -> null`
  - `patches/@shopify+react-native-skia+1.12.4.patch` 已收敛为 1 处 CMake 输出目录预创建补丁，避免 Windows/Gradle 8.8/Skia 原生任务在目录尚未物化时异常
  - 已删除可再生临时文件 `.tmp_skia_original.txt`，本轮软件瘦身明确包含第三方补丁对照残留清理
  - 已在真实平板 `HGR3Y9MA`（Lenovo TB128FU，Android 13，ABI `arm64-v8a`，分辨率 `1200x2000`）上完成 `app-debug.apk` 与 `app-debug-androidTest.apk` 的定向安装
  - 已执行 `adb reverse tcp:8081 tcp:8081`，为后续 USB 联调和 Detox attached-device smoke 提前打通 Metro 通道
- 已同步扩展页面级 smoke 可测试性锚点：
  - AI：新增 `input.ai.message`、`action.ai.send`、`action.ai.voice`
  - 提醒：新增 `action.reminder.add`、`input.reminder.title`、`action.reminder.create`、`state.reminder.empty`、`filter.reminder.*`、`item.reminder.*`
  - 知识图谱：新增 `action.knowledgeGraph.layout`、`action.knowledgeGraph.filter`、`action.knowledgeGraph.reset`、`action.knowledgeGraph.analysis`、`screen.knowledgeAnalysis`
  - `e2e/starter.test.js` 已改为优先验证首页真实入口 `entry.ai`，并补充“提醒新增页可达”和“知识分析页可达”的最短闭环
- 已完成 attached-device smoke 首轮全量收口：
  - `HandwritingRecognitionModule` 已改为惰性初始化，规避启动期 `DigitalInkRecognition.getClient()` 导致的原生崩溃
  - `ProfileSettings` 已移除 `ParticleBackground`，真实平板 smoke 成功率已从 2/7 提升到 4/7 后继续收口
  - `AddReminderScreen` 已移除标题输入框自动聚焦，并将“取消 / 创建”操作改为固定底部操作栏，解决平板视口下 `action.reminder.create` 可见性不足
  - `knowledgeGraphApi.js` 与 `knowledgeGraphSlice.js` 已补齐无后端联调场景的网络错误识别与离线空图降级，`KnowledgeGraphScreen` 空态补齐 `action.knowledgeGraph.analysis`
  - `KnowledgeAnalysisScreen` 空态/错误态已补齐 `screen.knowledgeAnalysis` 稳定锚点
  - `node node_modules/detox/local-cli/cli.js test -c android.att.debug --testNamePattern="Smoke"` 已在真实平板 `HGR3Y9MA` 上 7/7 通过
- 已完成功能审查主线首轮接口契约收敛：
  - `notification` 模块已统一到 Mongo 用户映射口径，批量已读/未读数/全部删除不再错误使用 Django 用户 ID
  - `backend/common/permissions.py` 已兼容 `request.mongo_user`，避免通知对象的 `recipient/user` 权限判断继续混用 Django/Mongo 用户对象
  - `/api/v1/notifications/*` 已修正为前端实际使用的平铺路由，不再出现 `/notifications/notifications/*` 双层路径
  - `/api/v1/code/*` 已重新挂载到后端总路由，前端代码运行/检测/补全/格式化/检查入口恢复对齐
  - 已补 `backend/code/*` 兼容别名包，托住历史 `from code.*` 导入，避免在本轮为修复路由而大面积改老文件
  - 已补 `src/services/codeService.js` 前端服务别名，修复 `CodeRunner` 指向不存在服务路径的问题
  - 已通过最小验证：`python manage.py test notification.tests.test_views code_editor.tests.test_urls --settings=backend.settings.testing`
  - 已通过入口级校验：`python manage.py check --settings=backend.settings.testing`
- 已启动群组/协作专项并完成首刀外部契约收口：
  - `backend/groups/urls.py` 已去除 `/api/v1/groups/groups/` 双层前缀，群组主资源改为直接挂载到 `/api/v1/groups/`
  - `GroupViewSet` 的加入码相关动作已显式对齐前端短横线路径：`join-by-code`、`generate-join-code`
  - `SharedScreenViewSet.perform_create()` 已改为 `ValidationError` / `NotFound` / `PermissionDenied` 异常流，避免缺参、缺群组或无权限时出现假成功或 500 风险
  - 已新增 `groups.tests.test_urls` 与 `groups.tests.test_view_contracts`，覆盖群组 URL 契约和共享屏幕创建的最小异常契约
  - 已通过最小验证：`python manage.py test groups.tests.test_urls groups.tests.test_view_contracts --settings=backend.settings.testing`
  - 下一刀不再回到 `code_editor` 细枝末节，优先修复 `GroupViewSet` / `SharedScreenViewSet` 中 `members__user` / `group__members__user` 的 Mongo 成员查询误用
- 已继续完成群组/协作专项第二刀 Mongo 口径收口：
  - `backend/groups/views.py` 已新增 `_get_request_mongo_user()`、`_get_visible_groups_for_mongo_user()` 与 `_is_group_creator()`，统一群组权限、创建、加入、邀请、离开、共享屏幕链使用 Mongo 用户口径
  - `GroupViewSet.get_queryset()` 与 `SharedScreenViewSet.get_queryset()` 已移除 `members__user` / `group__members__user` 反向关系查询，改为先查 `GroupMember` 再反查可见群组
  - `GroupInvitationViewSet`、`GroupViewSet.perform_create()`、`SharedScreenViewSet.perform_create()` 已补 Mongo 用户缺失时的明确失败语义，避免静默混用 Django 用户
  - 已新增 `groups.tests.test_mongo_contracts`，覆盖 queryset、权限、创建保存链与邀请查询链的 Mongo 用户映射契约
  - 已通过最小验证：`python manage.py test groups.tests.test_urls groups.tests.test_view_contracts groups.tests.test_mongo_contracts --settings=backend.settings.testing`
  - 已通过入口级校验：`python manage.py check --settings=backend.settings.testing`
  - 下一刀继续收口 `GroupDetailSerializer` 的加入码显示口径，并进入前端页面级群组/协作功能审查
- 已继续完成群组/协作专项第三刀详情展示口径收口：
  - `backend/groups/serializers.py` 中 `GroupDetailSerializer` 已不再直接使用 `request.user` 判断群组创建者/管理员，而是优先使用 `request.mongo_user` 并在缺失时回退 `get_mongo_user_from_django()`
  - 已新增 `groups.tests.test_serializer_contracts`，覆盖“Mongo 创建者可见加入码”和“Mongo 管理员可见加入码”两条最小契约
  - `src/components/groups/GroupDetail.js` 已移除未注册的 `EditGroup` 菜单项，避免详情页点按后直接命中不存在的导航路由
  - 已通过最小验证：`python manage.py test groups.tests.test_urls groups.tests.test_view_contracts groups.tests.test_mongo_contracts groups.tests.test_serializer_contracts --settings=backend.settings.testing`
  - 已通过入口级校验：`python manage.py check --settings=backend.settings.testing`
  - 下一刀继续转向前端页面级真实消费链审查，重点检查详情页、邀请页、加入页与共享屏幕页的可用性与体验
- 已继续完成群组/协作专项第四刀共享链与能力字段收口：
  - `backend/groups/serializers.py` 已新增 `can_invite` / `can_generate_join_code` 能力字段，前端不再用 `group.creator.id` 这类弱条件猜权限
  - `src/services/webrtc/webrtcService.js` 已改为基于 `API_URL` 直连 `/ws/webrtc/{room_id}/`，消除把 WebRTC 信令地址误拼到 `/api/v1/ws/...` 的硬错误
  - `src/components/groups/ScreenShare.js` 已改为使用当前登录用户身份初始化信令，按当前群组展示活跃共享列表，并在移动端明确阻止创建假共享会话，避免后端残留“active 但不可用”的共享记录
  - `src/components/groups/GroupDetail.js` 已切到后端能力字段驱动的菜单显示，不再向普通成员暴露稳定 403 的“生成加入码/邀请成员”管理动作
  - 已通过最小验证：`python manage.py test groups.tests.test_urls groups.tests.test_view_contracts groups.tests.test_mongo_contracts groups.tests.test_serializer_contracts --settings=backend.settings.testing`
  - 已通过目标文件静态校验：`node node_modules/eslint/bin/eslint.js src/components/groups/GroupDetail.js src/components/groups/ScreenShare.js src/screens/groups/ScreenShareScreen.js src/services/webrtc/webrtcService.js`
  - 当前剩余焦点转为邀请成员检索式流程、共享观看端更深层体验验证，以及 Android 专用 MCP 真机可视化验收
- 已启动群组/协作专项第五刀邀请流程搜索化整改：
  - 已确认现有 `InviteMembersScreen` 仍是“输入 userId 后直接发送”的调试态流程，真实用户基本无法完成邀请
  - 已确认 `backend/users` 现有 `/users/search/` 仅限管理员，不适合直接复用到群组邀请场景
  - 本轮改为在 `groups` 域内补 `invite-candidates` 候选搜索契约，并将前端邀请页切成“搜索 -> 选择 -> 邀请 -> 结果反馈”的最小可用链
  - 已通过最小验证：`python manage.py test groups.tests.test_urls groups.tests.test_mongo_contracts --settings=backend.settings.testing`
  - 已通过目标文件静态校验：`node node_modules/eslint/bin/eslint.js src/screens/groups/InviteMembersScreen.js src/redux/slices/groupsSlice.js src/services/api/groupApi.js src/config/api.js`
  - 对应提交 `49c933b` 已推送到当前批次分支
- 已启动群组/协作专项第六刀共享观看端三态体验收口：
  - 已确认当前共享页还停留在“最小列表 + 加入入口”形态，观看态和共享态没有完整拆分，离开观看与状态反馈也不足
  - 本轮已将共享页重构为“共享中 / 观看中 / 空闲”三态页面，补齐手动刷新、离开观看、直接拉取群组详情与共享列表等体验收口
  - 已通过目标文件静态校验：`node node_modules/eslint/bin/eslint.js src/components/groups/ScreenShare.js src/screens/groups/ScreenShareScreen.js src/services/webrtc/webrtcService.js src/redux/slices/groupsSlice.js`
  - 对应提交 `fed56c8` 已推送到当前批次分支
- 已启动群组/协作专项第七刀共享观看链前置联调修复：
  - 已确认 WebRTC 服务在切房间时仍可能复用旧连接，页面回调也会在重复进入时累积，后续真实联调很容易出现脏状态
  - 本轮已补 webrtcService 的切房间断连与回调解绑能力，并在观看态补真实远端视频承载区与等待远端流提示
  - 已通过目标文件静态校验：`node node_modules/eslint/bin/eslint.js src/services/webrtc/webrtcService.js`
  - 已通过目标文件静态校验：`node node_modules/eslint/bin/eslint.js src/components/groups/ScreenShare.js src/screens/groups/ScreenShareScreen.js`
  - 对应提交 `f092c10` 已推送到当前批次分支
- 已继续推进群组/协作专项第八刀第四阶段页面内联调证据补强：
  - 已确认本地 attached-device smoke、安装链和页面级状态可观测性虽已具备，但 WebRTC 共享链本地端到端联调入口仍未完全闭环
  - 本轮已在 `src/components/groups/ScreenShare.js` 新增联调摘要面板，集中展示当前角色、共享会话 ID、WebRTC 房间、连接状态/详情、远端流状态与等待超时信息
  - 已新增最近关键事件时间线，并在共享发起、加入观看、自动退出、收到远端流、超时、复制摘要等关键节点沉淀页面内诊断事件
  - 已补“复制摘要”能力，便于把当前共享链证据快速回填到文档或转给协作智能体，先降低盲查成本
  - 已通过目标文件静态校验：`node node_modules/eslint/bin/eslint.js src/components/groups/ScreenShare.js src/screens/groups/ScreenShareScreen.js src/services/webrtc/webrtcService.js`
  - 对应提交 `457e1be` 已完成并绑定到当前批次；该能力仅用于排障辅助，不等同于 WebRTC 端到端联调完成或 Android 专用 MCP 真机可视化验证完成
- 已继续收口本地共享链联调入口：
  - 已确认根仓库没有可用的 Electron 启动脚本、缺少 Electron 运行依赖，且 `localhost:8091` 更适合作为稳定的共享链联调入口，避免与 React Native Metro 默认端口 `8081` 冲突
  - 本轮已在 `package.json` 新增 `yarn local:web`，在 `scripts/serve-local-web.js` 提供 `http://127.0.0.1:8091/shared-screen-lab/` 本地静态诊断页
  - `shared-screen-lab` 已支持录入群组 ID、共享会话 ID、WebRTC 房间、当前现象、关键事件时间线与联调摘要复制，便于与 App 内共享页证据互相对照
  - `electron/main.js` 开发态已显式指向 `shared-screen-lab`，后续如补齐 Electron 依赖，可先从这一诊断入口而不是空白 Metro 首页进入
  - 已通过最小验证：`node --check scripts/serve-local-web.js`；独立端口 `8091` 下 `shared-screen-lab` 返回 200 且命中“共享链本地联调页 / 复制联调摘要 / ZeroIsle 本地共享链联调摘要”关键文案
  - 该入口只用于本地双端联调排障，不等同于 React Native Web 主应用、Electron 桌面版或 Android 专用 MCP 真机验证已完成
- 已继续收口共享链最小 HTTP 联调工具：
  - 已新增 `backend/scripts/shared_screen_smoke.py`，默认面向本地后端联调，统一输出共享用户、群组 ID、加入码、共享会话 ID 与 `webrtc_room_id`
  - 脚本默认验证 `POST /api/v1/auth/register/username/`、`POST /api/v1/auth/login/`、`POST /api/v1/groups/`、`POST /api/v1/groups/{id}/generate-join-code/`、`POST /api/v1/groups/shared-screens/`、`GET /api/v1/groups/shared-screens/` 与 `GET /api/v1/groups/shared-screens/{id}/join/`
  - 该脚本用于在 Android 专用 MCP 真机验证前先确认共享 HTTP 契约最小链路，不再继续积累 `tmp_*` 一次性调试脚本
  - 已同步删除 `backend/tmp_smoke.py` 与 `backend/tmp_e2e_debug.py` 两个过时临时脚本，作为本轮软件瘦身的一部分
  - 该脚本已于 2026-05-12 晚些时候实际跑通最小链路，详见 `GAP-GROUP-009` 与本文更新记录；即便脚本跑通，也不等同于 WebRTC 端到端联调完成或 Android 专用 MCP 真机验证完成
- 已继续收口共享链最小契约运行稳定性：
  - testing 环境中的 `mongomock` 已补统一 `uuidRepresentation='standard'`，降低 Mongo UUID 编码口径漂移
  - `users/utils.py` 已补失效 `UserProfile -> MongoUser` 映射回退逻辑，避免坏映射直接中断共享链权限与查询
  - `groups/views.py` 已补 Mongo QuerySet 兼容的 `get_object()`，并让群组创建者默认自动入群且授予 `admin` 角色，避免“刚建群就无法生成加入码/创建共享”
  - `groups/serializers.py` 已把群组/共享相关序列化器收口为显式 `Serializer`，不再继续依赖 DRF `ModelSerializer` 对 Django ORM 的假设
  - 已通过最小回归：`python manage.py test groups.tests.test_mongo_contracts groups.tests.test_view_contracts groups.tests.test_serializer_contracts --settings=backend.settings.testing` 共 15 项
  - 最小 `shared_screen_smoke.py` HTTP 流程已在后续轮次跑到全绿，但这只证明共享 HTTP 最小契约可达；后续仍需把 WebRTC 端到端联调、暂停/恢复链路与 Android 专用 MCP 真机验证单独收口
- 已继续收口 `code_editor` 最小持久化契约混用问题：
  - `CodeSnippet` 与 `CodeExecution` 的序列化器已不再把 Mongo 文档伪装成 Django `ModelSerializer`
  - `code_editor` 视图创建链已统一使用 `request.mongo_user` / `get_mongo_user_from_django()`，不再把 Django 用户对象直接写入代码片段与执行记录
  - `CodeExecutionService` 已去除 `save(update_fields=...)` 这类 Django ORM 语义，避免对 Mongo 文档执行错误保存调用
  - 前端 `src/services/code/codeService.js` 已兼容后端直接返回片段对象和分页 `results` 结构，减少片段列表/创建接口再次返工风险
  - 已通过最小验证：`python manage.py test code_editor.tests.test_urls code_editor.tests.test_persistence_contract --settings=backend.settings.testing`
- 已继续扩展 `code_editor` 最小 CRUD 与筛选回归覆盖：
  - 已新增 update、retrieve、rerun、by_tag、by_language 等最小回归用例，将验证范围从“最小持久化契约”扩展到“最小 CRUD 与筛选链”
  - 当前 `python manage.py test code_editor.tests.test_urls code_editor.tests.test_persistence_contract --settings=backend.settings.testing` 已覆盖 12 项并全部通过
  - 并行风险排序结论已形成：下一批比继续深挖 `code_editor` 更值得立刻切入的是群组/协作模块，其次才是 notification 和语音转写
- 当前新增风险登记：
  - Android 工程中的 `createPlaceholderLibs` 伪造 `.so` 任务已于 2026-05-12 移除，避免继续污染 `mergeDebugNativeLibs` / `stripDebugDebugSymbols` 与中间产物；后续不得恢复该类占位修复
  - 已确认仓库侧污染链与 Detox 配置问题均非主因；本轮已完成 SDK/NDK 侧修复并统一构建口径，后续不得再切回 `D:\Android\Sdk`
  - 真实平板 attached-device 仍会受到权限弹窗、推送/后台服务、以及未接入 Android 专用 MCP/插件的限制；当前阶段只能宣称“真机构建链已打通”，不能宣称“真机验证完成”
  - 当前已证明 attached-device smoke 与基础页面可达性通过，但这不等于 Android 专用 MCP/插件下的真机可视化验证完成；上线状态只能继续标记为“整改完成，真机验证待完成”或“真机验证进行中”
- attached-device 安装与运行前仍需显式设置 `ANDROID_SDK_ROOT=C:\Users\QX\AppData\Local\Android\Sdk` 与 `ANDROID_HOME=C:\Users\QX\AppData\Local\Android\Sdk`，并记录 `adb devices -l`、设备 ABI 与安装日志
- 离线模拟器 `emulator-5554` 仍会出现在 `adb devices -l` 中；后续所有真机安装、卸载、日志采集和 smoke 命令必须显式指定 `-s HGR3Y9MA`，避免再次触发 `more than one device/emulator`
- Android MCP 已就位不等于真机验收完成；当前真实阻塞是 Metro/DevSupport/UI 树稳定性、提醒 `round17/18` 证据链缺口，以及部分页面偶发空 `FrameLayout`
- 当前体验结论只能写为“遮挡和打断已继续减少，但提醒真实按钮链、共享链与整机真机验收尚未完成”，不得写成“可以上线”
- 在环境准备完成后，立即启动功能审查与工具化测试，确认规划功能完成度、上线完整性、体验与速度。

## 10. 更新记录

| 日期 | 更新人 | 变更说明 |
|---|---|---|
| 2026-05-11 | Codex | 新建总控文档，明确唯一执行入口、批次 0~5、Git 规则和真机验证前置条件 |
| 2026-05-11 | Codex | 回填首轮安全、部署、CI、移动可测试性整改进展与验证结果 |
| 2026-05-11 | Codex | 新增软件瘦身规则、`conda ZeroIsle` 环境要求、`yarn install` / `yarn android` 统一命令与局域网联调方案 |
| 2026-05-11 | Codex | 恢复 GAP 台账并补充批次 2 的瘦身、环境、功能审查约束，登记首轮可删产物统计 |
| 2026-05-11 | Codex | 回填 Conda `Zeroisle`、ADB 真机识别、后台前端子项目安装边界，以及首轮安全瘦身的实际执行结果 |
| 2026-05-12 | Codex | 回填批次 2 首轮瘦身与环境准备提交哈希 `1d96221`，确保总控文档、GAP 台账与 Git 提交一致 |
| 2026-05-12 | Codex | 回填批次 3 testing 自检恢复、`notes` 信号测试通过，以及 `mind_map` / `document_converter` / `personal_activity` 首轮导入阻塞修复结果 |
| 2026-05-12 | Codex | 追加批次 3 第二轮启动期副作用清理记录，纳入 `mongodb_realm_service`、`document_converter`、`notification` 的惰性初始化收敛与验证结果 |
| 2026-05-12 | Codex | 追加前端工具链阻塞诊断：确认 `react-native` CLI 不能依赖当前环境 PATH，根目录脚本改为仓库内 CLI 直连，并登记 Detox 依赖缺口 |
| 2026-05-12 | Codex | 回填批次 4 前端功能审查准备首轮修复提交 `0f95407`，登记 smoke 链路修正、CLI 可执行性恢复与剩余 Detox/Metro 阻塞 |
| 2026-05-12 | Codex | 追加 RTK 执行规则：根据 `C:\Users\QX\.codex\AGENTS.md` 与 `RTK.md` 将 RTK 纳入默认 shell 巡检与验证策略，用于减少 token 消耗 |
| 2026-05-12 | Codex | 回填功能审查首轮接口契约修复提交哈希 `22f0bed`，将 `notification` 用户映射修复、`/api/v1/code/*` 契约恢复、历史 `code.*` 兼容别名与最小验证证据绑定到总控/GAP 台账 |
| 2026-05-12 | Codex | 回填 `code_editor` 最小持久化契约收敛提交哈希 `1dd520c`，将 Mongo 用户映射、Mongo 文档序列化/保存口径、前端片段响应兼容与新增回归测试绑定到总控/GAP 台账 |
| 2026-05-12 | Codex | 正式建立群组/协作专项：新增 `GAP-GROUP-001/002`，记录 `/api/v1/groups/*` 外部契约收口、共享屏幕创建异常流修复、群组专项最小验证通过，以及下一刀聚焦 Mongo 成员查询链的执行顺序 |
| 2026-05-12 | Codex | 回填群组专项首刀提交哈希 `ea3e6ab`，将 `/api/v1/groups/*` 外部契约收口、共享屏幕创建异常流修复与最小验证证据绑定到总控与 GAP 台账 |
| 2026-05-12 | Codex | 追加群组专项第二刀：将群组/共享屏幕查询链、权限链、创建链统一到 Mongo 用户口径，补 `groups.tests.test_mongo_contracts` 最小回归验证，并把下一步收口点明确到 `GroupDetailSerializer` 与前端页面消费验证 |
| 2026-05-12 | Codex | 回填群组专项第二刀提交哈希 `4fcfbf5`，将 Mongo 用户映射收口、可见群组查询修复与 14 项最小回归验证绑定到总控与 GAP 台账 |
| 2026-05-12 | Codex | 追加群组专项第三刀：修复 `GroupDetailSerializer` 的 Mongo 用户展示口径、移除前端详情页未接线 `EditGroup` 菜单项，并将群组最小回归扩展到 16 项 |
| 2026-05-12 | Codex | 回填群组专项第三刀提交哈希 `3e0cfe2`，将详情加入码展示口径收口、前端未接线路由移除与 16 项群组回归验证绑定到总控与 GAP 台账 |
| 2026-05-12 | Codex | 追加群组专项第四刀：修复共享屏幕信令地址、当前用户身份口径与移动端假共享状态污染，并将详情页管理动作切换为后端能力字段驱动 |
| 2026-05-12 | Codex | 回填群组专项第四刀提交哈希 `f56f6b8`，将共享屏幕信令链修复、详情页能力字段收口与 17 项群组回归验证绑定到总控与 GAP 台账 |
| 2026-05-12 | Codex | 启动群组专项第五刀：将邀请成员页从手填 `userId` 的调试态流程升级为搜索选择式体验，并在 `groups` 域内补最小候选搜索契约 |
| 2026-05-12 | Codex | 回填群组专项第五刀提交哈希 `49c933b`，将邀请成员搜索选择链、群组域候选搜索契约与 15 项最小回归验证绑定到总控与 GAP 台账 |
| 2026-05-12 | Codex | 启动群组专项第六刀：将共享观看端从“最小列表入口”升级为完整三态页面，并继续为后续真机可视化验证收口前端体验 |
| 2026-05-12 | Codex | 回填群组专项第六刀提交哈希 `fed56c8`，将共享观看端三态页面收口与目标文件静态校验绑定到总控与 GAP 台账 |
| 2026-05-12 | Codex | 启动群组专项第七刀：修复共享观看链的房间切换、回调解绑与远端流承载前置问题，继续为真实联调和真机验证降低返工概率 |
| 2026-05-12 | Codex | 回填群组专项第七刀提交哈希 `f092c10`，将共享观看链前置联调修复与目标文件静态校验绑定到总控与 GAP 台账 |
| 2026-05-12 | Codex | 启动群组专项第八刀：收口共享观看态在房主离开、共享结束与列表刷新后的自动回收，并同步共享对象最新快照，继续为真实联调证据补强减少假状态返工 |
| 2026-05-12 | Codex | 回填群组专项第八刀提交哈希 `ffe9d5f`，将共享观看态自动回收修复与目标文件静态校验绑定到总控与 GAP 台账 |
| 2026-05-12 | Codex | 追加群组专项第八刀第二阶段：拆分共享链专属 `loading/error` 状态，避免群组其它请求误禁用共享按钮或把无关错误串到共享页，继续为真实联调减少假故障噪声 |
| 2026-05-12 | Codex | 回填群组专项第八刀第二阶段提交哈希 `11195e5`，将共享链专属状态隔离修复与目标文件静态校验绑定到总控与 GAP 台账 |
| 2026-05-12 | Codex | 追加群组专项第八刀第三阶段：补共享链联调可观测性，增加信令连接状态、错误原因与远端流等待超时提示，继续为真实联调证据补强减少盲查返工 |
| 2026-05-12 | Codex | 回填群组专项第八刀第三阶段提交哈希 `f41f2ff`，将共享链联调可观测性修复与目标文件静态校验绑定到总控与 GAP 台账 |
| 2026-05-12 | Codex | 追加群组专项第八刀第四阶段：补页面内联调摘要面板、关键事件时间线与摘要复制能力，在本地联调入口未完全打通前先降低共享链盲查成本，但不把它当作真机验证完成证据 |
| 2026-05-12 | Codex | 回填群组专项第八刀第四阶段提交哈希 `457e1be`，将页面内联调摘要面板、关键事件时间线与摘要复制能力绑定到总控与 GAP 台账 |
| 2026-05-12 | Codex | 追加本地共享链联调入口收口：新增 `yarn local:web` 与 `shared-screen-lab` 诊断页，把 `http://127.0.0.1:8091` 收口成稳定可访问的本地共享链排障入口，并将 Electron 开发态入口切到该页面，同时把 `8081` 留给 React Native Metro |
| 2026-05-12 | Codex | 回填本地共享链联调入口提交哈希 `3e961da`，将 `yarn local:web`、`shared-screen-lab` 诊断页与 `8091` 独立端口返回 200 的最小验证证据绑定到总控与 GAP 台账 |
| 2026-05-12 | Codex | 追加 Detox Android 最小接入首轮落地：补齐 `detox` 依赖、独立 `test:e2e:jest`、`android.att.debug`、`androidTest` instrumentation 入口，以及 AI/提醒/知识图谱关键 `testID` 与 smoke 扩展 |
| 2026-05-12 | Codex | 追加 Detox 链路验证结果：确认 `e2e` 配置已可识别 smoke 文件，Android 构建已推进到真实 Gradle/NDK 阶段，当前主阻塞收敛为 `react-native-reanimated` 的 `libc++_shared.so` 权限失败与 SDK 环境变量要求 |
| 2026-05-12 | Codex | 追加 Android 原生构建根因收敛结果：已移除 `createPlaceholderLibs` 伪造 `.so` 任务并清理受污染构建产物，attached-device `detox build` 复测后确认主阻塞稳定落在 `D:\Android\Sdk\ndk\26.1.10909125` 内 `libc++_shared.so` 单文件 ACL 异常；后续优先执行“重装同版 NDK 或迁移到用户目录 SDK”系统修复方案 |
| 2026-05-12 | Codex | 回填 Android SDK/NDK 系统修复结果：统一 attached-device 构建使用 `C:\Users\QX\AppData\Local\Android\Sdk`，补齐 `react-native-document-opener` / `@react-native-voice/voice` `minSdk` 契约与 Skia Gradle 8.8 兼容步骤，`detox build -c android.att.debug` 已成功产出 `app-debug.apk` 与 `app-debug-androidTest.apk` |
| 2026-05-12 | Codex | 追加第三方补丁持久化与安装链验证：删除无效/噪声 patch 与临时对照文件，重建 `detox`、`@shopify/react-native-skia` 最小 `patch-package` 补丁；在真实平板 `HGR3Y9MA` 上成功安装调试 APK 与 `androidTest` APK，并执行 `adb reverse tcp:8081 tcp:8081` |
| 2026-05-12 | Codex | 追加 attached-device smoke 全量收口记录：完成提醒创建页固定底部操作栏、知识图谱/知识分析页无后端空态锚点与分析入口补强、知识图谱网络错误离线降级补丁，以及 `HandwritingRecognitionModule` / `ProfileSettings` 相关稳定性收口；`Smoke` 用例已在真实平板上 7/7 通过，但 Android 专用 MCP 真机可视化验证仍未完成 |

| 2026-05-12 | Codex | 追加批次05共享 HTTP 最小契约收口记录：已打通“注册 -> 登录 -> 建群 -> 生成加入码 -> 创建共享 -> 列表 -> 加入共享”最小 smoke 链路；本轮同步修复 `backend/users/utils.py` 失效 `UserProfile -> MongoUser` 回退、`backend/groups/views.py` 的 Mongo 引用 ID 安全比较与 `SharedScreenViewSet.pagination_class = None`，以及 `backend/groups/serializers.py` 的显式安全展开；验证命令为 `D:/APP/Anaconda/Scripts/conda.exe run -n Zeroisle python manage.py test groups.tests.test_shared_screen_smoke --settings=backend.settings.testing` 与 `D:/APP/Anaconda/Scripts/conda.exe run -n Zeroisle python manage.py test groups.tests.test_serializer_contracts groups.tests.test_mongo_contracts groups.tests.test_view_contracts --settings=backend.settings.testing`，结果分别为 1 项通过与 15 项通过；必须继续声明：该结果仅证明共享 HTTP 最小契约与本地联调工具可用，不等同于 WebRTC 端到端联调完成，也不等同于 Android 专用 MCP 真机平板验证完成 |
| 2026-05-14 | Codex | 追加 Android MCP 真机工具接入验证：已克隆 `minhalvp/android-mcp-server` 至 `D:\ZeroIsle_Notes\.local\android-mcp-server`，执行 `uv sync` 完成依赖安装，并通过 `config.yaml` 固定设备 `HGR3Y9MA`；已使用其 `AdbDeviceManager` 真实连接平板 `TB128FU`，成功执行 `getprop ro.product.model`、`get_uilayout()` 与 `take_screenshot()`，产出 `window_dump.xml`、`screenshot.png`、`compressed_screenshot.png`，说明当前已经具备 Android MCP 下的真机 UI 树抓取、截图和后续点击测试前置能力，但仍需继续补逐页操作证据，不能据此直接宣称整机验收完成 |
| 2026-05-14 | Codex | 追加 Android MCP 真机点击与白屏阻塞定位：已通过 Android MCP + ADB 在真实平板上实际拉起 `com.zeroisle_notes/.MainActivity`，并对 React Native 开发弹窗执行 `DISMISS` 点击；随后结合 `logcat`、`uiautomator dump` 与截图确认当前白屏根因是调试包报 `Unable to load script`，进一步核实到本机 `8081` 正被 `scripts/serve-local-web.js` 占用而不是 Metro，因此决定将 `shared-screen-lab` 默认端口切回 `8091`，把 `8081` 留给 React Native Metro 后继续真机复测 |
| 2026-05-14 | Codex | 追加 Android 真机启动链第二阶段排障：在将 `shared-screen-lab` 迁到 `8091` 并重启 Metro 后，真机阻塞已从“无法加载 JS bundle”推进到前端模块解析层；本轮先后定位并修复 `src/components/common/Highlighter.js` 对缺失 `highlight-words-core` 的硬依赖，以及 `src/platform/index.js` 中 Electron/TouchID/可选桌面模块对 React Native 调试包的静态解析风险。清缓存重启 Metro 后，`logcat` 已出现 `AppNavigator`、`HomeScreen`、`DEV_SKIP_LOGIN`、`应用准备就绪，显示主界面` 等业务日志，说明应用主界面逻辑已实际启动；当前剩余问题已收敛为 Metro 连接稳定性与平台抽象层残留旧缓存/旧红框需继续清理，不再是最初的白屏阻塞。 |
| 2026-05-14 | Codex | 追加 Android 真机逐页点击审查首轮结果：已基于 Android MCP + ADB 对真实平板 `HGR3Y9MA / TB128FU` 完成首页、AI、社区、我的四条主导航点击与证据采集，当前证据文件为 `D:\ZeroIsle_Notes\.local\android-mcp-server\zeroisle_step_home.(png|xml)`、`zeroisle_step_ai.(png|xml)`、`zeroisle_step_community.(png|xml)`、`zeroisle_step_profile.(png|xml)`。本轮已确认 `screen.home`、`screen.ai`、`screen.profile` 与底部 `nav.tab.home / ai / community / profile` 可达，AI 页具备消息输入框、语音按钮和历史记录入口；社区页已真实切到 `Community` 路由，但在当前本地未接后端阶段会弹出“网络提示”降级对话框，说明主阻塞已从“页面起不来”收敛为“未部署阶段的降级体验 + 可测试性锚点缺口”。并行审查同时确认：前端仓库已大范围使用 Redux Toolkit，后续计划应写成“继续收口复杂业务态边界、减少组件影子状态”，而不是“从零引入 RTK”；当前最值得优先补齐的是 `screen.community`、首页右下新增浮钮与 `CreateContentModal` 的稳定 `testID`，否则后续 Android MCP 与 Detox 都难以稳定覆盖社区与新增流程。 |
| 2026-05-14 | Codex | 追加 Android 真机可测试性第二步收口：已在 `CommunityScreen` 补 `screen.community`、通知/动态/发帖入口和帖子操作锚点，在 `HomeScreen` 补首页右下新增浮钮 `action.home.create`，并在 `CreateContentModal` 补弹层容器及主要创建/导入动作 `testID`。这一步的目标不是宣称新增流程已全部验收，而是先把 Android MCP 与 Detox 后续点击链需要的稳定锚点补齐，减少“页面可见但无法稳定断言/继续点”的返工。 |
| 2026-05-14 | Codex | 追加社区页离线降级体验收口：已在 `src/services/networkErrorService.js` 收口请求级静默全局网络提示能力，社区帖子列表请求经 `src/services/api/communityApi.js` 与 `src/redux/slices/communitySlice.js` 透传 `suppressGlobalErrorUI` 标记；同时重写 `src/screens/community/CommunityScreen.js` 的首屏、刷新与加载更多降级逻辑，在未接后端阶段优先展示页内 `offline / error / empty` 状态卡片，而不是继续让系统级“网络提示”遮挡 Android 真机点击。已通过 `node node_modules/eslint/bin/eslint.js src/screens/community/CommunityScreen.js src/redux/slices/communitySlice.js src/services/api/communityApi.js src/services/networkErrorService.js` 静态校验；下一步基于新 bundle 在真实平板 `HGR3Y9MA / TB128FU` 上复测社区页、首页新增弹层与后续提醒/知识图谱链路。 |
| 2026-05-14 | Codex | 追加社区页真机复测闭环：已在 `src/App.js` 增加开发态高频日志前缀过滤，仅压制会持续灌入 React Native `DebuggingOverlay` 的已知离线调试日志，保留真正未预期错误输出；随后在真实平板 `HGR3Y9MA / TB128FU` 上重新拉起应用并复测社区页，产出 `D:\ZeroIsle_Notes\.local\android-mcp-server\zeroisle_round6_community.png`。当前截图已确认社区页标题、搜索区、分类胶囊、骨架列表、底部导航和右下发布浮钮均清晰可见，说明主阻塞已从“系统网络弹窗/调试覆盖层遮挡点击”收口为“未接后端阶段的内容空载与后续页面链路继续取证”；当前仍只能标记为“真机验证进行中”，不得据此宣称 Android 真机验收完成或系统已可上线。 |
| 2026-05-14 | Codex | 追加提醒/知识图谱链路真机与代码收口：已修复 `src/screens/reminder/AddReminderScreen.js` 中“页面手动 `POST` 后再次 `dispatch(addReminder(...))` 导致重复创建”的 P0 风险，并将“服务端创建成功但本地通知调度失败”拆成非阻断提示；同时补 `KeyboardAvoidingView`、返回/日期/分类/优先级/频率/取消等稳定 `testID` 与更大的点击热区，并为 `SafeDateTimePicker` fallback 补 `testID` 与错误态复位，避免一次 fallback 后长期锁死。知识图谱侧已在 `src/components/knowledge/GraphVisualization.js` 修正 `position / size / updatedAt` 字段错配，避免边坐标 NaN、节点叠点与颜色计算异常；在 `src/screens/knowledge/KnowledgeGraphScreen.js` 补上 `authRequired/authMessage` 的显式空态提示，避免 401 被伪装成“空图”。本轮真机证据新增 `D:\ZeroIsle_Notes\.local\android-mcp-server\zeroisle_round7_profile.png`、`zeroisle_round7_reminder.png`、`zeroisle_round7_reminder_add.png`、`zeroisle_round8_reminder.png`、`zeroisle_round8_knowledge_2.png`；当前已确认提醒列表页、提醒新增页、知识图谱空态页主体均可到达并取证，但未接后端离线联调下仍残留 `GET /reminder/reminders/ 失败` 与 `获取知识图谱数据异常` 两类开发态错误条遮挡底部区域，故只能记为“页面主体可取证、残余调试遮挡待继续压制”，不得据此宣称真机验收完成。 |
| 2026-05-14 | Codex | 追加提醒/知识图谱链路第二轮收口：已把本轮提醒重复创建修复、通知调度降级、`SafeDateTimePicker` fallback 锚点补齐、知识图谱字段兼容与认证空态修复同步回填到 GAP 台账，并记录 `node node_modules/eslint/bin/eslint.js src/screens/reminder/AddReminderScreen.js src/components/common/SafeDateTimePicker.js src/screens/knowledge/KnowledgeGraphScreen.js src/components/knowledge/GraphVisualization.js` 为 0 error。本轮继续在 `src/App.js` 增补 `获取知识图谱数据异常:` 开发态日志过滤，只压制已知离线联调噪声，不压真实异常；在完成新一轮 Android MCP 真机复测前，系统状态仍只能记为“整改推进中 / 真机验证进行中”，不得宣称已完成真机验收或已可上线。 |
| 2026-05-14 | Codex | 追加知识图谱 round13 真机复测闭环：已在 `src/services/api/knowledgeGraphApi.js` 新增 `isExpectedOfflineKnowledgeGraphError()`，并在 `src/redux/slices/knowledgeGraphSlice.js` 去除知识图谱 thunk 中对离线联调错误的重复 `console.error`，只在非预期异常或非开发态场景保留必要日志，避免 React Native `DebuggingOverlay` 持续遮挡真机页面。随后使用 Android MCP + ADB 在真实平板 `HGR3Y9MA / TB128FU` 上重新进入知识图谱页，产出 `D:\ZeroIsle_Notes\.local\android-mcp-server\zeroisle_round13_knowledge.png`、`zeroisle_round13_knowledge.xml` 与 `zeroisle_round13_logcat.txt`。当前 `round13` XML 已确认 `screen.knowledgeGraph` 可达，主体文案收口为“暂无知识图谱数据 / 网络不可用，已显示离线空图”，旧的知识图谱底部开发态错误条已消失，说明该页遮挡问题已基本收口；但提醒列表页底部离线调试条仍待继续处理，因此整体状态仍只能标记为“整改推进中 / 真机验证进行中”，不得宣称 Android 真机验收完成或系统已可上线。 |
| 2026-05-14 | Codex | 追加提醒列表 round14 真机复测闭环：已在 `src/services/api/reminderApi.js` 为提醒列表查询补齐 `suppressGlobalErrorUI` 元数据透传，并在 `src/components/reminder/ReminderListView.js` 将列表加载改为“优先静默请求远端、失败后回退本地提醒并显示页内非阻断提示”的降级模式，同时把离线空态文案显式收口到页内。随后使用 Android MCP + ADB 在真实平板 `HGR3Y9MA / TB128FU` 上从“我的”进入 `entry.reminder.profile`，产出 `D:\ZeroIsle_Notes\.local\android-mcp-server\zeroisle_round14_before_reminder.png`、`zeroisle_round14_profile.(png|xml)`、`zeroisle_round14_reminder.(png|xml)` 与 `zeroisle_round14_reminder_logcat.txt`。当前 `round14` XML 已确认 `screen.reminderList`、`filter.reminder.all`、`state.reminder.empty` 与 `action.reminder.add` 可达，页面主体已切换为“网络不可用，已切换为本地提醒视图”顶部提示和“离线模式下暂无本地提醒”页内空态，底部开发态错误条不再遮挡提醒页面；同时 `node node_modules/eslint/bin/eslint.js src/components/reminder/ReminderListView.js src/services/api/reminderApi.js` 结果为 0 error。该结果只代表提醒列表离线遮挡问题已收口，整体 Android 真机验收、共享链路验证和上线可用性评审仍未完成。 |
| 2026-05-14 | Codex | 追加提醒新增页 round15 真机闭环：已在 `src/services/api/reminderApi.js` 为创建提醒接口补齐 `suppressGlobalErrorUI` 元数据透传，并在 `src/screens/reminder/AddReminderScreen.js` 将新增页创建动作切换为静默错误路径；同时把页头提示区、说明卡片和操作栏收口为更稳定的蓝白轻玻璃层级，在未接后端阶段优先展示页内诚实提示，不再依赖全局错误覆盖。随后使用 Android MCP + ADB 在真实平板 `HGR3Y9MA / TB128FU` 上完成“我的 -> 日程 -> 新增”点击链，产出 `D:\ZeroIsle_Notes\.local\android-mcp-server\zeroisle_round15_profile.xml`、`zeroisle_round15_reminder.(png|xml)`、`zeroisle_round15_reminder_add_2.(png|xml)`、`zeroisle_round15_reminder_add_hint.(png|xml)` 与 `zeroisle_round15_reminder_add_logcat.txt`。当前 `round15` XML 已确认 `screen.reminder`、`input.reminder.title`、`action.reminder.create` 可达，空标题点击后会命中 `state.reminder.createHint` 并显示“请输入提醒标题”页内非阻断提示；同时 `node node_modules/eslint/bin/eslint.js src/screens/reminder/AddReminderScreen.js src/services/api/reminderApi.js` 结果为 0 error、7 warning。该结果说明提醒新增页的真机可点击性、页内提示和视觉层级已继续收口，但真实联网创建、提醒详情编辑链和整体 Android 真机验收仍未完成。 |
| 2026-05-14 | Codex | 追加提醒详情页 round16 真机闭环：已在 `src/services/api/apiClient.js` 与 `src/services/networkErrorService.js` 补齐请求级静默错误透传，在 `src/screens/reminder/ReminderDetailScreen.js` 将已降级联调场景的日志级别从会触发 RN 开发覆盖层的 `console.error/warn` 收口为 `console.log`，并继续沿用 `reminderApi.*(..., { suppressGlobalErrorUI: true })`；同时把 `ReminderListView`、`ReminderCategoryView`、`ReminderCalendarView` 进入详情页的参数统一为 `id + reminder` 快照，并在 `ReminderNavigator` 保留开发态样例入口 `action.reminder.openDetailSample`。随后使用 Android MCP + ADB 在真实平板 `HGR3Y9MA / TB128FU` 上完成“首页 -> 我的 -> 日程 -> 详情样例”串行点击链，产出 `D:\ZeroIsle_Notes\.local\android-mcp-server\zeroisle_round16_reminder_seq.xml`、`zeroisle_round16_detail.xml`、`zeroisle_round16_detail.png`、`zeroisle_round16_detail_clean.png`、`zeroisle_round16_detail_final.png` 与对应 logcat。当前 `round16` 已确认 `screen.reminder.detail`、`state.reminder.detailHint`、`input.reminder.detail.title`、`action.reminder.detail.save` 等锚点可在真实平板命中，且最终截图中底部开发态错误覆盖条已消失，只保留页内诚实提示；但真实联网保存、删除、改期和整机真机验收仍未完成，不得据此宣称提醒详情链或 Android 端已可上线。 |
| 2026-05-14 | Codex | 追加提醒列表真实操作静默链收口：已在 `src/services/api/reminderApi.js` 为 `completeReminder`、`reopenReminder`、`toggleEnableReminder` 补齐 `requestOptions.suppressGlobalErrorUI` 透传，并在 `src/components/reminder/ReminderListView.js` 将完成/取消完成、启用/禁用、删除、批量完成、批量删除统一切换到 `reminderApi.*(..., { suppressGlobalErrorUI: true })`。同时把这些已定义为“离线可降级”的写操作日志收口到开发态 `console.log`，避免后续 Android 真机点击真实按钮时再被 React Native 开发覆盖层遮挡；`node node_modules/eslint/bin/eslint.js src/components/reminder/ReminderListView.js src/services/api/reminderApi.js` 本轮结果为 0 error、11 warning。该结果说明提醒列表真实操作链的请求入口与静默错误口径已进一步统一，但批量/单条操作失败后仍沿用 `saveOfflineReminder` 的离线创建式兜底，真实联网写操作与 round17 真机按钮点击证据仍待补齐，当前仍只能标记为“真机验证进行中 / 不得宣称已可上线”。 |
| 2026-05-14 | Codex | 追加提醒列表离线更新/删除语义收口：已在 `src/services/reminder/reminderNotificationService.js` 新增 `saveOfflineReminderUpdate` 与 `saveOfflineReminderDelete`，并在 `src/components/reminder/ReminderListView.js` 将完成/启停/批量完成失败后的本地兜底从“误记为 create”的 `saveOfflineReminder` 改为显式 `update` 记账；同时为单条删除失败、批量删除失败和离线批量删除补上 `delete` 记账，避免“当前页看起来删掉、刷新后又回弹”的无补偿删除链。`node node_modules/eslint/bin/eslint.js src/components/reminder/ReminderListView.js src/services/reminder/reminderNotificationService.js src/services/api/reminderApi.js` 本轮结果为 0 error、11 warning。该结果说明提醒列表离线兜底语义已继续向真实操作靠拢，但 reminder slice 与历史 `syncReminders` 仍保留旧离线模型，round17 真机按钮点击证据与完整离线同步回归仍待补齐。 |
| 2026-05-14 | Codex | 追加提醒同步主线收口：已在 `src/redux/slices/reminderSlice.js` 将 `syncReminders` 从旧的 `offlineReminders` 数组直连同步逻辑切换为复用 `src/services/reminder/reminderNotificationService.js` 的 `getOfflineOperations()` 与 `syncOfflineReminders()`，并在 fulfilled reducer 中使用 `remaining` 回填未同步计数，避免提醒列表真实操作已切到 `update/delete` 离线语义后，slice 仍沿用旧 create-only 假设造成双轨漂移。`node node_modules/eslint/bin/eslint.js src/redux/slices/reminderSlice.js src/components/reminder/ReminderListView.js src/services/reminder/reminderNotificationService.js src/services/api/reminderApi.js` 本轮结果为 0 error、11 warning。该结果说明提醒列表的离线记账与同步入口已进一步并回 service 主线，但 `offlineReminders` 本地数组仍是过渡兼容状态，后续仍需结合 round17 真机按钮点击与完整同步回归继续验证。 |
| 2026-05-14 | Codex | 追加提醒 round17 真机核查与创建页离线前置收口：已通过 Android MCP + ADB 在真实平板 `HGR3Y9MA / TB128FU` 上完成“首页 -> 我的 -> 日程”再次取证，新增 `D:\ZeroIsle_Notes\.local\android-mcp-server\zeroisle_round17_current.(png|xml)`、`zeroisle_round17_profile.(png|xml)`、`zeroisle_round17_reminder.(png|xml)`、`zeroisle_round17_add_form.(png|xml)`、`zeroisle_round17_after_create_retry.xml` 与 `zeroisle_round17_after_create_retry_logcat.txt`。当前 `round17` 已确认提醒列表页可稳定到达 `screen.reminderList`，但真实列表为空，仅显示“网络不可用，已切换为本地提醒视图 / 离线模式下暂无本地提醒”；继续进入新增页后，真机证据命中 `state.reminder.createHint` 和底部页内提示“创建提醒失败: Error: 网络错误且无缓存，无法完成请求”，暴露出“列表页已诚实降级到本地视图，但创建页仍继续发远程创建请求”的体验断裂。本轮已在 `src/screens/reminder/AddReminderScreen.js` 增加 `isNetworkConnected()` 前置检查，将该链路收口为离线即页内提示、不中断页面、不再额外触发一次远程创建失败；`node node_modules/eslint/bin/eslint.js src/screens/reminder/AddReminderScreen.js` 结果为 0 error、7 warning。该结果说明 round17 已形成明确真机问题定位与最小修复，但提醒列表真实按钮链仍受“当前本地数据为空、离线创建尚未实现”阻塞，不能据此宣称提醒模块真机验收完成或系统已可上线。 |

| 2026-05-14 | Codex | 追加 Android MCP 真机现状修正：Android 专用 MCP 工具链已正式就位，`.local/android-mcp-server` 可连接真实平板 `HGR3Y9MA / TB128FU`，`adb reverse tcp:8081 tcp:8081`、`uiautomator dump`、截图与 logcat 证据链均可恢复使用；因此总控状态从“插件未就位”更新为“真机验证进行中”。当前主阻塞不再是工具缺失，而是 Metro `8081` 监听恢复、UI 树偶发空 `FrameLayout`、以及提醒 `round17/18` 证据链未补齐。 |
| 2026-05-14 | Codex | 追加提醒模块代码审查结论同步：基于本轮 reminder 代码复核，已确认 5 个高/中风险点必须进入下一轮小步修复与真机复测，分别是：1）新增提醒“先落库再调度通知”非原子，通知失败会诱发重复插入；2）`loadReminders.fulfilled` 直接覆盖本地提醒，导致本地创建后刷新消失；3）RTK `offlineReminders` 与持久层 `OFFLINE_OPERATIONS/OFFLINE_REMINDERS` 双写漂移；4）列表页错误调用 `updateReminder(updatedReminder)`，会让远程 `PUT` body 为空；5）重复提醒调度存在递归展开未来通知风险。当前这些问题只完成代码审查登记，尚未形成新的稳定源码修复提交。 |
| 2026-05-14 | Codex | 追加提醒 round17/18 真机阻塞同步：已保留 `zeroisle_round17_current/profile/reminder/add_form.(png|xml)`、`zeroisle_round17_after_create_retry.xml`、`zeroisle_round17_after_create_retry_logcat.txt`、`zeroisle_round18_recover_start/retry.(png|xml)` 与 `zeroisle_round18_recover_retry_logcat.txt` 作为当前证据基线。现阶段只能确认提醒列表、提醒新增页和部分详情锚点可达，且页面遮挡有所减少；但由于 Metro/UI 树稳定性与提醒真实按钮链证据仍未闭环，总结论仍必须保持“整改推进中 / 真机验证进行中 / 不得宣称已可上线”。 |
| 2026-05-14 | Codex | 追加提醒第二轮最小源码收口：已在 `src/redux/slices/reminderSlice.js` 将 `loadReminders.fulfilled` 改为“保留本地 `isLocal` 提醒并与远端结果合并”，避免本地创建项被一次远端拉取直接覆盖；已在 `src/components/reminder/ReminderListView.js` 将完成/启停/批量完成的本地状态更新统一切回 `updateLocalReminder({ id, reminderData })`，并把离线同步触发口径从 `offlineReminders.length` 改为真实 `syncStatus.unsyncedCount`，减少“请求 body 为空”“同步提示数量漂移”“本地更新后刷新丢失”的返工；同时在 `src/services/reminder/reminderNotificationService.js` 去掉 `scheduleReminderNotification()` 内对重复提醒的递归下一次调度，避免一次创建里大量展开未来通知。验证命令为 `node node_modules/eslint/bin/eslint.js src/redux/slices/reminderSlice.js src/components/reminder/ReminderListView.js src/services/reminder/reminderNotificationService.js`，结果为 0 error、9 warning。当前这些修复只代表 round17/18 的返工风险进一步下降，真机成功证据与剩余离线模型收口仍待继续补齐。 |
| 2026-05-14 | Codex | 追加提醒第三轮最小源码收口：已在 `src/services/reminder/reminderNotificationService.js` 将 `saveOfflineReminder()` 改为“同 id 覆盖写入”而不是反复 append，避免通知调度失败或多次重试时向 `OFFLINE_REMINDERS` 持久层插入重复本地提醒副本；已在 `src/screens/reminder/AddReminderScreen.js` 将“本地保存提醒”和“调度本地通知”拆成可降级链路，本地提醒创建不再因为通知调度失败整体判定失败；并在 `src/redux/slices/reminderSlice.js` 将 `offlineReminders` 从“离线操作日志副本”收口为“本地提醒镜像”，把 `unsyncedCount` 的更新从数组长度改为显式计数，进一步降低 Redux 与 `OFFLINE_OPERATIONS/OFFLINE_REMINDERS` 双轨漂移。验证命令为 `node node_modules/eslint/bin/eslint.js src/screens/reminder/AddReminderScreen.js src/redux/slices/reminderSlice.js src/components/reminder/ReminderListView.js src/services/reminder/reminderNotificationService.js`，结果为 0 error、16 warning。当前仍未补 round17/18 本地创建成功与真实按钮链证据，因此这些修复只能记为“进一步减少返工”，不能等同于真机验收完成。 |
| 2026-05-14 | Codex | 追加 reminder round23-27 闭环记录：本轮继续围绕 reminder 创建链执行代码审查与 Android 真机复测，已明确 round23 根因为 Realm/JSON 存储契约错位，导致 `offline_reminders` 写入数组后读取侧仍按字符串处理；round25 根因为全局 `AddReminder` 与提醒子栈新增页路由混用，创建完成后导航错误回流到 profile；round26 已确认“本地离线创建成功且能回到提醒列表”，但创建成功后仍出现“创建提醒失败: Error: 网络错误且无缓存，无法完成请求”的误导性底部提示。针对该误报链，本轮在 `src/screens/reminder/AddReminderScreen.js` 将“已处理的离线回退”从 `console.error('创建提醒失败:', error)` 降级为开发态 `console.log`，只对真正未兜底成功的异常保留错误日志；同时保留 `ReminderAdd` 路由收口与显式返回 `ReminderList` 的导航修复。随后通过真实平板 `HGR3Y9MA / TB128FU` 执行 round27，产出 `D:\ZeroIsle_Notes\.local\android-mcp-server\round27_list_abs.xml`、`round27_typed_abs.xml`、`round27_after_submit_abs.xml`、`round27_after_submit_abs.png` 与 `round27_after_submit_logcat.txt`。当前 `round27_after_submit_abs.xml` 已确认页面回到 `screen.reminderList`，列表新增本地提醒 `round27localok`，且提交后的 UI 树中未再出现“创建提醒失败: Error: 网络错误且无缓存，无法完成请求”底部误导提示，说明“创建成功后仍弹失败提示”的主阻塞已收口。当前 reminder 模块状态可更新为“离线创建主链闭环、真实按钮链仍需继续补完成/启停/删除证据”，整体系统仍不得宣称已完成真机验收或已可上线。 |
| 2026-05-14 | Codex | 追加 reminder round27 后最小静态校验说明：本轮针对 `AddReminderScreen`、`ReminderNavigator`、`ReminderListView`、`ReminderCalendarView`、`ReminderCategoryView` 与 `reminderMongoDBService` 执行了定向 ESLint 校验，命令为 `node node_modules/eslint/bin/eslint.js src/screens/reminder/AddReminderScreen.js src/navigation/ReminderNavigator.js src/components/reminder/ReminderListView.js src/components/reminder/ReminderCalendarView.js src/components/reminder/ReminderCategoryView.js src/services/reminder/reminderMongoDBService.js`。结果为 3 error、45 warning，其中 3 个 error 均为历史遗留的 `react-hooks/exhaustive-deps`，分布在 `src/components/reminder/ReminderCalendarView.js` 2 处、`src/components/reminder/ReminderCategoryView.js` 1 处；本轮新增页链路、路由回流与 Realm/JSON 存储契约修复未引入新的 ESLint error。该结论已同步回 GAP 台账，后续将在 reminder 完成/启停/删除按钮链继续真机复测时一并处理这些历史 hook 依赖问题。 |
| 2026-05-14 | Codex | 追加 reminder round29/30 真机闭环：本轮先在 `src/components/reminder/ReminderListView.js` 引入 `unsyncedCountRef`，把网络监听 effect 对离线未同步计数的读取从闭包旧值改为 ref，避免完成提醒后因为 `unsyncedCount` 变化导致初始化 effect 重新执行 `loadRemindersData()`，把刚乐观更新的完成态又被本地旧快照覆盖；同时把单条/批量删除的本地乐观删除从误用远程 thunk `deleteReminder` 改为同步 reducer `deleteLocalReminder`，保证离线删除先从本地列表移除。真机 `round29` 已产出 `D:\ZeroIsle_Notes\.local\android-mcp-server\round29_complete_success.xml/.png/.txt`、`round29_toggle.xml/.txt`、`round29_delete_prompt.xml/.png` 等证据，确认完成链已闭环、启停链点击后不再弹开发态错误覆盖层并会记账 `save_offline_reminder_update`。随后在 `round30` 通过再次抓取确认弹窗 XML，精确命中 `android:id/button1` 的“删除”按钮坐标，产出 `round30_delete_prompt_retry.xml`、`round30_delete_committed.xml/.png/.txt`；当前 `round30_delete_committed.xml` 已确认 `round27localok` 从列表中消失，仅剩其它本地提醒项，logcat 同步出现 `save_offline_reminder_delete` 事件。定向校验 `node node_modules/eslint/bin/eslint.js src/components/reminder/ReminderListView.js` 结果为 `0 error / 9 warning`。当前提醒模块状态可更新为“离线创建、完成、删除三条主按钮链已真机闭环，启停链具备离线兜底但仍需补视觉/同步细证据”；整体系统仍不得宣称 Android 真机验收完成或已可上线。 |
| 2026-05-15 | 5 | 补未知离线操作容错回归并细化状态卡节奏 | GAP-REVIEW-001, GAP-MOBILE-001 | Codex | 继续按 `rtk + code-review` 做容错补强：在 `src/redux/slices/__tests__/reminderSlice.test.js` 新增“unknown offline operation 不应污染提醒列表”回归，确保离线队列出现未识别操作类型时，`loadReminders` 投影链保持稳定且不误改服务端快照，同时 `offlineReminders` 维持空数组。并行按 `ui-ux-pro-max` 对 `src/components/reminder/ReminderListView.js` 做轻量视觉节奏收口（同步状态卡底部间距、标题行高微调），保持蓝白毛玻璃风格。验证命令：`node node_modules/jest/bin/jest.js --runTestsByPath src/redux/slices/__tests__/reminderSlice.test.js --watch=false`（11/11 通过），`node node_modules/eslint/bin/eslint.js src/redux/slices/__tests__/reminderSlice.test.js src/components/reminder/ReminderListView.js`（0 error，ReminderListView 保留 9 warning） | 本轮仍属于回归与视觉细节增量，未新增联网真机回放证据，`GAP-DEVICE-001` 继续 `IN_PROGRESS` | 下一步继续推进联网回放与 `remaining` 文案一致性实机证据 |
| 2026-05-15 | 5 | 收口字符串 update 误投影并细化状态胶囊字重节奏 | GAP-REVIEW-001, GAP-MOBILE-001 | Codex | 继续按 `rtk + code-review` 对离线投影边界做对称收口：新增“update 操作载荷为字符串 ID 时应安全忽略、不污染提醒状态”的回归用例，并在 `src/redux/slices/reminderSlice.js` 将 update 投影收口为“仅处理对象载荷”，避免把无效 update 误标记为本地变更。对应测试文件 `src/redux/slices/__tests__/reminderSlice.test.js` 已补齐。并行按 `ui-ux-pro-max` 对 `src/components/reminder/ReminderListView.js` 的同步状态胶囊文案做轻量字距微调（`syncStatusPillText.letterSpacing`），保持毛玻璃信息层级一致性。验证命令：`node node_modules/jest/bin/jest.js --runTestsByPath src/redux/slices/__tests__/reminderSlice.test.js --watch=false`（12/12 通过），`node node_modules/eslint/bin/eslint.js src/redux/slices/reminderSlice.js src/redux/slices/__tests__/reminderSlice.test.js src/components/reminder/ReminderListView.js`（0 error，ReminderListView 保留 9 warning） | 本轮仍属状态边界与视觉细节收口，未新增联网真机回放证据，`GAP-DEVICE-001` 继续 `IN_PROGRESS` | 下一步继续推进联网场景回放并补 `remaining` 文案一致性证据 |
| 2026-05-15 | 5 | 收口 refresh 失败口径并补 rejected 回归 | GAP-REVIEW-001, GAP-MOBILE-001 | Codex | 按 `rtk + code-review` 继续收口同步计数稳定性：`refreshUnsyncedCount` 失败场景此前缺少显式 reducer 口径，存在沿用旧 `unsyncedCount` 的风险。本轮在 `src/redux/slices/reminderSlice.js` 增加 `refreshUnsyncedCount.rejected` 分支，失败时统一回落 `unsyncedCount=0` 并记录 `syncStatus.error`；同时在 `src/redux/slices/__tests__/reminderSlice.test.js` 新增 rejected 回归用例，确保该行为稳定可回归。并行按 `ui-ux-pro-max` 对 `src/components/reminder/ReminderListView.js` 的状态色条透明度做轻量微调，保持毛玻璃层级协调。验证命令：`node node_modules/jest/bin/jest.js --runTestsByPath src/redux/slices/__tests__/reminderSlice.test.js --watch=false`（13/13 通过），`node node_modules/eslint/bin/eslint.js src/redux/slices/reminderSlice.js src/redux/slices/__tests__/reminderSlice.test.js src/components/reminder/ReminderListView.js`（0 error，ReminderListView 保留 9 warning） | 本轮仍为状态稳定性与视觉细节收口，未新增联网真机回放证据，`GAP-DEVICE-001` 继续 `IN_PROGRESS` | 下一步继续推进联网回放并补 `remaining` 与状态卡文案一致性证据 |
| 2026-05-15 | 5 | 收口 sync rejected 无 remaining 的计数残留口径 | GAP-REVIEW-001, GAP-MOBILE-001 | Codex | 按 `rtk + code-review` 做口径对称收口：`syncReminders.rejected` 在未携带 `remaining` 时此前不会主动回落 `unsyncedCount`，存在沿用旧计数风险。本轮在 `src/redux/slices/reminderSlice.js` 将该分支补齐为“无 `remaining` 即 `unsyncedCount=0`”，并在 `src/redux/slices/__tests__/reminderSlice.test.js` 新增“sync rejected 无 remaining 时回落 0”回归用例，确保状态卡计数不残留旧值。并行按 `ui-ux-pro-max` 对 `src/components/reminder/ReminderListView.js` 做同步状态卡圆角微调（24 -> 25），保持毛玻璃层次一致。验证命令：`node node_modules/jest/bin/jest.js --runTestsByPath src/redux/slices/__tests__/reminderSlice.test.js --watch=false`（14/14 通过），`node node_modules/eslint/bin/eslint.js src/redux/slices/reminderSlice.js src/redux/slices/__tests__/reminderSlice.test.js src/components/reminder/ReminderListView.js`（0 error，ReminderListView 保留 9 warning） | 本轮仍为状态稳定性与 UI 细节收口，未新增联网真机回放证据，`GAP-DEVICE-001` 继续 `IN_PROGRESS` | 下一步继续推进联网回放与 `remaining` 文案一致性证据闭环 |
| 2026-05-15 | 5 | 补 refresh rejected 无 payload 默认错误回归并细化圆角 | GAP-REVIEW-001, GAP-MOBILE-001 | Codex | 基于 `rtk + code-review` 继续补齐边界回归：为 `refreshUnsyncedCount.rejected` 新增“无 payload 时使用默认错误对象并回落计数”测试，确保刷新失败在最小信息场景下仍可稳定落到 `unsyncedCount=0` 与标准错误口径，避免后续提示链路分叉。对应文件为 `src/redux/slices/__tests__/reminderSlice.test.js`。并行按 `ui-ux-pro-max` 对 `src/components/reminder/ReminderListView.js` 状态卡圆角做轻量微调（25 -> 26），保持毛玻璃卡片与页面整体圆角节奏统一。验证命令：`node node_modules/jest/bin/jest.js --runTestsByPath src/redux/slices/__tests__/reminderSlice.test.js --watch=false`（15/15 通过），`node node_modules/eslint/bin/eslint.js src/redux/slices/__tests__/reminderSlice.test.js src/components/reminder/ReminderListView.js`（0 error，ReminderListView 保留 9 warning） | 本轮仍为状态边界与视觉细节收口，未新增联网真机回放证据，`GAP-DEVICE-001` 继续 `IN_PROGRESS` | 下一步继续推进联网回放证据与口径一致性闭环 |
| 2026-05-15 | 5 | 修复 sync fulfilled 空 payload 风险并补回归 | GAP-REVIEW-001, GAP-MOBILE-001 | Codex | 继续按 `rtk + code-review` 收口状态机边界：`syncReminders.fulfilled` 之前直接访问 `action.payload.remaining`，当 payload 缺失时存在运行时异常风险。本轮在 `src/redux/slices/reminderSlice.js` 统一改为 `remainingCount = action.payload?.remaining ?? 0`，并基于该值回写 `unsyncedCount` 与 `offlineReminders` 清理判定，避免空 payload 触发崩溃。同步在 `src/redux/slices/__tests__/reminderSlice.test.js` 新增“fulfilled 无 payload 回落 0”回归用例。并行按 `ui-ux-pro-max` 对 `src/components/reminder/ReminderListView.js` 状态卡阴影透明度轻调（0.14 -> 0.13），保持毛玻璃层级克制。验证命令：`node node_modules/jest/bin/jest.js --runTestsByPath src/redux/slices/__tests__/reminderSlice.test.js --watch=false`（16/16 通过），`node node_modules/eslint/bin/eslint.js src/redux/slices/reminderSlice.js src/redux/slices/__tests__/reminderSlice.test.js src/components/reminder/ReminderListView.js`（0 error，ReminderListView 保留 9 warning） | 本轮为状态边界与视觉细节收口，未新增联网真机回放证据，`GAP-DEVICE-001` 继续 `IN_PROGRESS` | 下一步继续推进联网回放证据与同步文案一致性闭环 |
| 2026-05-15 | 5 | 补 sync fulfilled 空 payload 的离线镜像清理回归 | GAP-REVIEW-001, GAP-MOBILE-001 | Codex | 延续 `rtk + code-review` 的边界收口，本轮补齐 “sync fulfilled 无 payload” 场景下 `offlineReminders` 清理行为回归，避免 `unsyncedCount` 已回落但本地离线镜像残留导致页面口径不一致。已在 `src/redux/slices/__tests__/reminderSlice.test.js` 新增用例，验证无 payload 时 `offlineReminders` 会清空且 `unsyncedCount=0`。并行按 `ui-ux-pro-max` 对 `src/components/reminder/ReminderListView.js` 状态胶囊垂直留白做轻量微调（`paddingVertical: 5 -> 4`），让毛玻璃胶囊在移动端更紧凑。验证命令：`node node_modules/jest/bin/jest.js --runTestsByPath src/redux/slices/__tests__/reminderSlice.test.js --watch=false`（17/17 通过），`node node_modules/eslint/bin/eslint.js src/redux/slices/__tests__/reminderSlice.test.js src/components/reminder/ReminderListView.js`（0 error，ReminderListView 保留 9 warning） | 本轮为状态边界回归补齐与 UI 细节微调，未新增联网真机回放证据，`GAP-DEVICE-001` 继续 `IN_PROGRESS` | 下一步继续推进联网真机回放，补齐同步状态文案与离线队列余量一致性证据 |
| 2026-05-15 | 5 | 收口 remaining 类型口径并补类型安全回归 | GAP-REVIEW-001, GAP-MOBILE-001 | Codex | 按 `rtk + code-review` 继续补边界：`syncReminders.fulfilled` 的 `remaining` 若出现非数字类型，旧逻辑会把非预期值写入计数口径。本轮在 `src/redux/slices/reminderSlice.js` 将 `remainingCount` 收口为“仅接受 number，否则回落 0”；并在 `src/redux/slices/__tests__/reminderSlice.test.js` 新增“remaining 非数字时回落 0”回归用例，防止接口变体引发计数污染。并行按 `ui-ux-pro-max` 对 `src/components/reminder/ReminderListView.js` 状态描述文案字距做轻量微调（0.10 -> 0.12），保持毛玻璃信息卡可读性一致。验证命令：`node node_modules/jest/bin/jest.js --runTestsByPath src/redux/slices/__tests__/reminderSlice.test.js --watch=false`（18/18 通过），`node node_modules/eslint/bin/eslint.js src/redux/slices/reminderSlice.js src/redux/slices/__tests__/reminderSlice.test.js src/components/reminder/ReminderListView.js`（0 error，ReminderListView 保留 9 warning） | 本轮仍为状态边界与 UI 细节收口，未新增联网真机回放证据，`GAP-DEVICE-001` 继续 `IN_PROGRESS` | 下一步继续推进联网回放与状态文案一致性证据闭环 |
| 2026-05-15 | 5 | 收口 remaining 负值边界并补双分支回归 | GAP-REVIEW-001, GAP-MOBILE-001 | Codex | 按 `rtk + code-review` 继续强化同步计数边界：`remaining` 在异常负值场景下不应把 `unsyncedCount` 写成负数。本轮在 `src/redux/slices/reminderSlice.js` 对 `syncReminders.fulfilled/rejected` 两分支统一增加下限保护（`Math.max(0, remaining)`）；并在 `src/redux/slices/__tests__/reminderSlice.test.js` 新增两条回归，覆盖 fulfilled 与 rejected 负值 remaining 均回落为 0。并行按 `ui-ux-pro-max` 对 `src/components/reminder/ReminderListView.js` 状态卡阴影半径微调（22 -> 21），保持毛玻璃层级克制。验证命令：`node node_modules/jest/bin/jest.js --runTestsByPath src/redux/slices/__tests__/reminderSlice.test.js --watch=false`（20/20 通过），`node node_modules/eslint/bin/eslint.js src/redux/slices/reminderSlice.js src/redux/slices/__tests__/reminderSlice.test.js src/components/reminder/ReminderListView.js`（0 error，ReminderListView 保留 9 warning） | 本轮仍为状态边界与视觉细节收口，未新增联网真机回放证据，`GAP-DEVICE-001` 继续 `IN_PROGRESS` | 下一步继续推进联网真机回放与状态文案一致性闭环 |
| 2026-05-15 | 5 | 收口 refresh fulfilled 类型边界并补双回归 | GAP-REVIEW-001, GAP-MOBILE-001 | Codex | 按 `rtk + code-review` 继续收口计数口径：`refreshUnsyncedCount.fulfilled` 之前对 payload 仅做空值兜底，非数字或负值仍可能污染 `unsyncedCount`。本轮在 `src/redux/slices/reminderSlice.js` 将该分支统一为“仅接受 number 且下限 0”；并在 `src/redux/slices/__tests__/reminderSlice.test.js` 新增两条回归，覆盖“非数字回落 0”与“负值回落 0”。并行按 `ui-ux-pro-max` 对 `src/components/reminder/ReminderListView.js` 状态卡阴影偏移做轻量微调（y:10 -> 9），保持毛玻璃阴影层级更克制。验证命令：`node node_modules/jest/bin/jest.js --runTestsByPath src/redux/slices/__tests__/reminderSlice.test.js --watch=false`（22/22 通过），`node node_modules/eslint/bin/eslint.js src/redux/slices/reminderSlice.js src/redux/slices/__tests__/reminderSlice.test.js src/components/reminder/ReminderListView.js`（0 error，ReminderListView 保留 9 warning） | 本轮仍为状态边界与视觉细节收口，未新增联网真机回放证据，`GAP-DEVICE-001` 继续 `IN_PROGRESS` | 下一步继续推进联网回放证据与同步口径一致性闭环 |
| 2026-05-15 | 5 | 补 sync rejected 非数字 remaining 回归并细化阴影层次 | GAP-REVIEW-001, GAP-MOBILE-001 | Codex | 继续按 `rtk + code-review` 补齐 rejected 分支类型边界：新增“`syncReminders.rejected` 的 `remaining` 为非数字时应回落 `unsyncedCount=0`”回归，避免接口返回类型异常导致计数污染。对应文件为 `src/redux/slices/__tests__/reminderSlice.test.js`。并行按 `ui-ux-pro-max` 对 `src/components/reminder/ReminderListView.js` 状态卡阴影偏移做轻量微调（y:9 -> 8），让毛玻璃层次更克制。验证命令：`node node_modules/jest/bin/jest.js --runTestsByPath src/redux/slices/__tests__/reminderSlice.test.js --watch=false`（23/23 通过），`node node_modules/eslint/bin/eslint.js src/redux/slices/__tests__/reminderSlice.test.js src/components/reminder/ReminderListView.js`（0 error，ReminderListView 保留 9 warning） | 本轮仍为状态边界与 UI 细节收口，未新增联网真机回放证据，`GAP-DEVICE-001` 继续 `IN_PROGRESS` | 下一步继续推进联网回放证据与同步状态文案一致性闭环 |
| 2026-05-15 | 5 | 补 sync rejected 无 payload 默认错误回归并细化阴影透明度 | GAP-REVIEW-001, GAP-MOBILE-001 | Codex | 按 `rtk + code-review` 继续补齐最小失败输入边界：新增“`syncReminders.rejected` 在无 payload 时应使用默认错误对象且计数回落 0”回归，确保状态机在最小信息失败场景下保持稳定。对应文件为 `src/redux/slices/__tests__/reminderSlice.test.js`。并行按 `ui-ux-pro-max` 对 `src/components/reminder/ReminderListView.js` 状态卡阴影透明度轻调（0.13 -> 0.12），保持毛玻璃层次更克制。验证命令：`node node_modules/jest/bin/jest.js --runTestsByPath src/redux/slices/__tests__/reminderSlice.test.js --watch=false`（24/24 通过），`node node_modules/eslint/bin/eslint.js src/redux/slices/__tests__/reminderSlice.test.js src/components/reminder/ReminderListView.js`（0 error，ReminderListView 保留 9 warning） | 本轮仍为状态边界与 UI 细节收口，未新增联网真机回放证据，`GAP-DEVICE-001` 继续 `IN_PROGRESS` | 下一步继续推进联网回放证据与状态口径一致性闭环 |
| 2026-05-15 | 5 | 补 sync rejected null remaining 回归并细化状态胶囊间距 | GAP-REVIEW-001, GAP-MOBILE-001 | Codex | 按 `rtk + code-review` 继续补齐 rejected 边界：新增“`syncReminders.rejected` 的 `remaining = null` 时 `unsyncedCount` 回落 0”回归，确保该场景与“无 remaining”口径一致，避免计数残留。对应文件 `src/redux/slices/__tests__/reminderSlice.test.js`。并行按 `ui-ux-pro-max` 对 `src/components/reminder/ReminderListView.js` 状态胶囊横向留白微调（`paddingHorizontal: 11 -> 12`），让毛玻璃标签在短文案下更均衡。验证命令：`node node_modules/jest/bin/jest.js --runTestsByPath src/redux/slices/__tests__/reminderSlice.test.js --watch=false`（25/25 通过），`node node_modules/eslint/bin/eslint.js src/redux/slices/__tests__/reminderSlice.test.js src/components/reminder/ReminderListView.js`（0 error，ReminderListView 保留 9 warning） | 本轮仍为状态边界与 UI 细节收口，未新增联网真机回放证据，`GAP-DEVICE-001` 继续 `IN_PROGRESS` | 下一步继续推进联网回放证据与同步口径一致性闭环 |
| 2026-05-15 | 5 | 收口 NaN 计数污染风险并补三条回归 | GAP-REVIEW-001, GAP-MOBILE-001 | Codex | 基于 `rtk + code-review` 识别到 `typeof === 'number'` 会放行 `NaN`，可能把 `unsyncedCount` 污染为 `NaN`。本轮在 `src/redux/slices/reminderSlice.js` 把 fulfilled/rejected/refresh 三处计数口径统一改为 `Number.isFinite(...)` 判定并下限保护，彻底屏蔽 `NaN` 污染。同步在 `src/redux/slices/__tests__/reminderSlice.test.js` 新增三条回归（fulfilled NaN、rejected NaN、refresh NaN），锁定边界行为。并行按 `ui-ux-pro-max` 对 `src/components/reminder/ReminderListView.js` 状态胶囊字距微调（0.20 -> 0.18），让毛玻璃胶囊文本更稳。验证命令：`node node_modules/jest/bin/jest.js --runTestsByPath src/redux/slices/__tests__/reminderSlice.test.js --watch=false`（28/28 通过），`node node_modules/eslint/bin/eslint.js src/redux/slices/reminderSlice.js src/redux/slices/__tests__/reminderSlice.test.js src/components/reminder/ReminderListView.js`（0 error，ReminderListView 保留 9 warning） | 本轮仍为状态边界与 UI 细节收口，未新增联网真机回放证据，`GAP-DEVICE-001` 继续 `IN_PROGRESS` | 下一步继续推进联网回放证据与状态口径一致性闭环 |
| 2026-05-15 | 5 | 补 sync rejected Infinity remaining 回归并细化胶囊字距 | GAP-REVIEW-001, GAP-MOBILE-001 | Codex | 继续按 `rtk + code-review` 补齐数值边界回归：新增“`syncReminders.rejected` 的 `remaining = Infinity` 时 `unsyncedCount` 回落 0”用例，确保 rejected 分支在无穷值场景下与 NaN/负值同口径处理。对应文件 `src/redux/slices/__tests__/reminderSlice.test.js`。并行按 `ui-ux-pro-max` 对 `src/components/reminder/ReminderListView.js` 状态胶囊字距轻调（0.18 -> 0.16），让毛玻璃标签在窄屏下更紧凑。验证命令：`node node_modules/jest/bin/jest.js --runTestsByPath src/redux/slices/__tests__/reminderSlice.test.js --watch=false`（29/29 通过），`node node_modules/eslint/bin/eslint.js src/redux/slices/__tests__/reminderSlice.test.js src/components/reminder/ReminderListView.js`（0 error，ReminderListView 保留 9 warning） | 本轮仍为状态边界与 UI 细节收口，未新增联网真机回放证据，`GAP-DEVICE-001` 继续 `IN_PROGRESS` | 下一步继续推进联网真机回放与状态口径一致性闭环 |
| 2026-05-15 | 5 | 补 refresh Infinity 回归并细化状态胶囊宽度 | GAP-REVIEW-001, GAP-MOBILE-001 | Codex | 继续按 `rtk + code-review` 完成计数链对称边界：新增“`refreshUnsyncedCount.fulfilled` 的 payload 为 Infinity 时回落 `unsyncedCount=0`”回归，确保 refresh 与 sync fulfilled/rejected 在无穷值输入下口径一致。对应文件 `src/redux/slices/__tests__/reminderSlice.test.js`。并行按 `ui-ux-pro-max` 对 `src/components/reminder/ReminderListView.js` 状态胶囊最小宽度轻调（62/64），让短文案在毛玻璃标签中的视觉重心更稳。验证命令：`node node_modules/jest/bin/jest.js --runTestsByPath src/redux/slices/__tests__/reminderSlice.test.js --watch=false`（30/30 通过），`node node_modules/eslint/bin/eslint.js src/redux/slices/__tests__/reminderSlice.test.js src/components/reminder/ReminderListView.js`（0 error，ReminderListView 保留 9 warning） | 本轮仍为状态边界与 UI 细节收口，未新增联网真机回放证据，`GAP-DEVICE-001` 继续 `IN_PROGRESS` | 下一步继续推进联网回放证据与同步口径一致性闭环 |
| 2026-05-15 | 5 | 补 sync fulfilled null remaining 回归并细化胶囊纵向留白 | GAP-REVIEW-001, GAP-MOBILE-001 | Codex | 按 `rtk + code-review` 继续补齐 fulfilled 分支边界：新增“`syncReminders.fulfilled` 的 `remaining = null` 时回落 `unsyncedCount=0`”回归，确保 fulfilled 与 rejected/refresh 在空值场景下口径一致。对应文件 `src/redux/slices/__tests__/reminderSlice.test.js`。并行按 `ui-ux-pro-max` 对 `src/components/reminder/ReminderListView.js` 状态胶囊纵向留白微调（`paddingVertical: 4 -> 3`），让毛玻璃胶囊在窄屏更紧凑。验证命令：`node node_modules/jest/bin/jest.js --runTestsByPath src/redux/slices/__tests__/reminderSlice.test.js --watch=false`（31/31 通过），`node node_modules/eslint/bin/eslint.js src/redux/slices/__tests__/reminderSlice.test.js src/components/reminder/ReminderListView.js`（0 error，ReminderListView 保留 9 warning） | 本轮仍为状态边界与 UI 细节收口，未新增联网真机回放证据，`GAP-DEVICE-001` 继续 `IN_PROGRESS` | 下一步继续推进联网回放证据与同步口径一致性闭环 |
| 2026-05-15 | 5 | 补 sync fulfilled Infinity 回归并细化胶囊字距 | GAP-REVIEW-001, GAP-MOBILE-001 | Codex | 按 `rtk + code-review` 继续补齐 fulfilled 分支的无穷值边界：新增“`syncReminders.fulfilled` 的 `remaining = Infinity` 时回落 `unsyncedCount=0`”回归，确保 fulfilled/rejected/refresh 三条计数链在 Infinity 场景下口径一致。对应文件 `src/redux/slices/__tests__/reminderSlice.test.js`。并行按 `ui-ux-pro-max` 对 `src/components/reminder/ReminderListView.js` 状态胶囊字距轻调（0.16 -> 0.15），让窄屏标签更稳。验证命令：`node node_modules/jest/bin/jest.js --runTestsByPath src/redux/slices/__tests__/reminderSlice.test.js --watch=false`（32/32 通过），`node node_modules/eslint/bin/eslint.js src/redux/slices/__tests__/reminderSlice.test.js src/components/reminder/ReminderListView.js`（0 error，ReminderListView 保留 9 warning） | 本轮仍为状态边界与 UI 细节收口，未新增联网真机回放证据，`GAP-DEVICE-001` 继续 `IN_PROGRESS` | 下一步继续推进联网回放证据与同步口径一致性闭环 |
| 2026-05-15 | 5 | 收口 refresh fulfilled 成功后错误残留并补回归 | GAP-REVIEW-001, GAP-MOBILE-001 | Codex | 按 `rtk + code-review` 复核后发现 `refreshUnsyncedCount.fulfilled` 成功路径此前未清理 `syncStatus.error`，会让状态卡在刷新成功后仍显示旧失败态。本轮在 `src/redux/slices/reminderSlice.js` 增加 `state.syncStatus.error = null`，并在 `src/redux/slices/__tests__/reminderSlice.test.js` 新增“refresh fulfilled 成功后清空 error 且保留新计数”回归，确保成功/失败态切换口径一致。并行按 `ui-ux-pro-max` 对 `src/components/reminder/ReminderListView.js` 同步状态卡补 `overflow: 'hidden'`，让圆角与毛玻璃裁切更稳定。验证命令：`node node_modules/jest/bin/jest.js --runTestsByPath src/redux/slices/__tests__/reminderSlice.test.js --watch=false`（33/33 通过），`node node_modules/eslint/bin/eslint.js src/redux/slices/reminderSlice.js src/redux/slices/__tests__/reminderSlice.test.js src/components/reminder/ReminderListView.js`（0 error，ReminderListView 保留 9 warning） | 本轮仍为状态稳定性与 UI 细节收口，未新增联网真机回放证据，`GAP-DEVICE-001` 继续 `IN_PROGRESS` | 下一步继续推进联网回放并补 `remaining` 与状态卡文案一致性证据 |
| 2026-05-15 | 5 | 收口 sync fulfilled 成功后错误残留并补对称回归 | GAP-REVIEW-001, GAP-MOBILE-001 | Codex | 按 `rtk + code-review` 做成功/失败态对称复核，确认 `syncReminders.fulfilled` 成功路径也存在“未显式清理旧 `syncStatus.error`”缺口，可能在非标准序列下残留失败态。本轮在 `src/redux/slices/reminderSlice.js` 的 fulfilled 分支增加 `state.syncStatus.error = null`，并在 `src/redux/slices/__tests__/reminderSlice.test.js` 新增“sync fulfilled 成功后清空 error 且计数保持”回归，确保同步成功态与刷新成功态口径一致。并行按 `ui-ux-pro-max` 对 `src/components/reminder/ReminderListView.js` 状态卡边框细化为低透明玻璃描边（`#FFFFFF1F`），维持现代毛玻璃一致性。验证命令：`node node_modules/jest/bin/jest.js --runTestsByPath src/redux/slices/__tests__/reminderSlice.test.js --watch=false`（34/34 通过），`node node_modules/eslint/bin/eslint.js src/redux/slices/reminderSlice.js src/redux/slices/__tests__/reminderSlice.test.js src/components/reminder/ReminderListView.js`（0 error，ReminderListView 保留 9 warning） | 本轮仍为状态边界与 UI 细节收口，未新增联网真机回放证据，`GAP-DEVICE-001` 继续 `IN_PROGRESS` | 下一步继续推进联网回放与状态卡文案/remaining 一致性证据闭环 |
| 2026-05-15 | 5 | 收口同步异常可感知性（状态卡图标/色调） | GAP-REVIEW-001, GAP-MOBILE-001 | Codex | 按 `code-review + ui-ux-pro-max` 继续做低风险体验收口：提醒同步状态卡此前未优先表达 `syncStatus.error`，在真机失败回放场景下异常可感知性偏弱。本轮在 `src/components/reminder/ReminderListView.js` 增加 `syncStatusError`/`hasSyncError` 分支，让状态卡在异常场景优先切换到错误图标 `error-outline` 与错误色调（`theme.error/#F44336`），不改动同步业务链即可提升“失败态可见性”。受该文件历史编码影响，本轮仅做分支视觉态收口，文案扩展单列到后续编码治理批次。验证命令：`node node_modules/eslint/bin/eslint.js src/components/reminder/ReminderListView.js`（0 error，9 warning），`node node_modules/jest/bin/jest.js --runTestsByPath src/redux/slices/__tests__/reminderSlice.test.js --watch=false`（34/34 通过） | 本轮仍为体验细节与可观测性收口，未新增联网真机回放证据，`GAP-DEVICE-001` 继续 `IN_PROGRESS` | 下一步继续推进联网回放；并在单独编码治理批次修复 ReminderListView 历史乱码后再扩展异常文案 |
| 2026-05-15 | 5 | 收口同步状态卡玻璃质感稳态参数 | GAP-REVIEW-001, GAP-MOBILE-001 | Codex | 按 `ui-ux-pro-max + rtk` 做低风险视觉稳态微调，避免再次触发乱码段返工：本轮仅调整 `src/components/reminder/ReminderListView.js` 的 `syncStatusCard` 参数（`borderWidth: 1 -> 1.2`、`shadowOpacity: 0.12 -> 0.1`），提升毛玻璃边缘清晰度并压低色调阴影噪声，保证异常/待处理态下卡片层次更稳定。验证命令：`node node_modules/eslint/bin/eslint.js src/components/reminder/ReminderListView.js`（0 error，9 warning），`node node_modules/jest/bin/jest.js --runTestsByPath src/redux/slices/__tests__/reminderSlice.test.js --watch=false`（34/34 通过） | 本轮仍为 UI 稳态收口，未新增联网真机回放证据，`GAP-DEVICE-001` 继续 `IN_PROGRESS` | 下一步继续推进联网真机回放；编码治理单列批次处理 ReminderListView 历史乱码后再做文案级优化 |
| 2026-05-15 | 5 | 收口提醒列表 key 提取容错并更新台账 | GAP-REVIEW-001, GAP-MOBILE-001 | Codex | 按 `code-review + rtk` 收口列表稳定性：`SectionList` 的 key 之前强依赖 `item.id.toString()`，遇到离线脏数据 `id` 缺失会触发运行时异常并中断页面渲染。本轮在 `src/components/reminder/ReminderListView.js` 将 keyExtractor 改为 `String(item?.id ?? item?.offlineId ?? \`reminder-\${index}\`)`，对异常载荷提供稳定兜底键，降低真机回放被单条数据拖垮的风险。验证命令：`node node_modules/eslint/bin/eslint.js src/components/reminder/ReminderListView.js`（0 error，9 warning），`node node_modules/jest/bin/jest.js --runTestsByPath src/redux/slices/__tests__/reminderSlice.test.js --watch=false`（34/34 通过） | 本轮为稳定性小修复，未新增联网真机回放证据，`GAP-DEVICE-001` 继续 `IN_PROGRESS` | 下一步继续推进联网场景回放并补齐证据链 |
| 2026-05-15 | 5 | 收口提醒项主键容错与详情点击链一致性 | GAP-REVIEW-001, GAP-MOBILE-001 | Codex | 按 `code-review + rtk` 继续深挖提醒列表稳定性，确认 `renderReminderItem` 里仍存在 `item.id.toString()` 与 `navigation` 直传 `item.id` 的链路，在离线异常载荷缺失 `id` 时会导致渲染或跳转崩溃。本轮在 `src/components/reminder/ReminderListView.js` 统一构造 `reminderKey/reminderId`，并用于 `isOffline`、`testID`、选中态与详情页导航，确保异常数据场景下渲染与点击链都可回退；并将参数名 `index` 改为 `itemIndex`，消除新增 `no-shadow` 告警，维持 lint 告警基线不增。验证命令：`node node_modules/eslint/bin/eslint.js src/components/reminder/ReminderListView.js`（0 error，9 warning），`node node_modules/jest/bin/jest.js --runTestsByPath src/redux/slices/__tests__/reminderSlice.test.js --watch=false`（34/34 通过） | 本轮仍为稳定性收口，未新增联网真机回放证据，`GAP-DEVICE-001` 继续 `IN_PROGRESS` | 下一步继续推进联网真机回放与提醒详情链可视化证据补齐 |
