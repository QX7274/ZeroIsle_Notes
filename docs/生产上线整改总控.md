# 生产上线整改总控（精简主入口）

> 唯一执行入口：本文件  
> GAP 台账：[DOC_GAP_TRACKER](D:/ZeroIsle_Notes/docs/DOC_GAP_TRACKER.md)  
> 历史映射：[ARCHIVE_TO_GAP_MAPPING](D:/ZeroIsle_Notes/docs/ARCHIVE_TO_GAP_MAPPING.md)  
> 长历史归档：[生产上线整改总控-历史归档](D:/ZeroIsle_Notes/docs/archive/生产上线整改总控-历史归档.md)
> 页面矩阵：[页面能力矩阵](D:/ZeroIsle_Notes/docs/页面能力矩阵.md)
> 子批次记录：[子批次执行记录-批次01（10页）](D:/ZeroIsle_Notes/docs/子批次执行记录-批次01（10页）.md)

## 1. 当前目标与上线判定
- 目标：把仓库从“可开发/可演示”推进到“可追踪/可验证/可上线”。
- 当前上线状态：`整改进行中（IN_PROGRESS）`，不得标记“已上线”。
- 原因：Android 真机验证虽已开始，但共享链深层端到端与联网同步一致性证据未完全闭环。

## 2. 固定执行规则（强约束）
- Git：
  - 基线提交已入 `main`：`a1931d5`。
  - 当前工作分支：`QX/上线整改-批次05-功能审查与路由通知修复`。
  - 每个可独立验收修复必须“文档回填 -> 中文详细提交（问题/原因/修改/验证/风险）-> push”。
  - 禁止提交：`docs/adapter-pattern.md`（用户未授权纳入）。
- 环境：
  - Python 一律使用 Conda `Zeroisle`（规范名 `ZeroIsle`，本机 env 实名为 `Zeroisle`）。
  - 命令基准：`D:/APP/Anaconda/Scripts/conda.exe run -n Zeroisle ...`
  - 前端安装：`yarn install`；Android 运行：`yarn android`。
  - 文档编码检查：`powershell -ExecutionPolicy RemoteSigned -File scripts/tools/check-doc-encoding.ps1`
- 真机与联调：
  - 设备：`HGR3Y9MA`（TB128FU）。
  - Metro 端口：`8081`；本地共享实验页默认：`8091`（避免占用 8081）。
  - 未部署阶段允许“手机热点/同局域网 + USB/ADB reverse”方案，不以公网互通作为阶段前置。
- 瘦身：
  - 允许删除：缓存、编译产物、可再生临时文件、重复证据副本。
  - 禁止删除：规划功能源码、未完成功能依据、当前执行主文档、活跃 GAP 绑定证据。

## 3. 批次状态（仅保留活跃视图）
- 批次0（基线）：`DONE`，提交 `a1931d5`。
- 批次1（P0 安全）：`IN_PROGRESS`，已做首轮清理，仍需密钥轮换与契约收口。
- 批次2（P0/P1 环境与瘦身）：`IN_PROGRESS`，Conda/yarn/ADB 基线已统一，文档与证据仍需持续瘦身。
- 批次3（P0 部署与配置）：`IN_PROGRESS`，ASGI/健康检查主线已收口，迁移与启动链仍待演练闭环。
- 批次4（P0/P1 CI 与可测试性）：`IN_PROGRESS`，Detox attached-device smoke 已通过，需持续防回归。
- 批次5（功能审查与真机）：`IN_PROGRESS`，已连续多轮真机取证，仍未达到“全功能可上线”判定。

## 4. 最近关键进展（高信号）
- 最近稳定证据轮次已推进到 `round64`（提交 `44a616e` 已推送）。
- Android MCP 真机能力已就位，可执行截图、UI dump、日志抓取与页面点击链验证。
- reminder 主链（离线创建/完成/删除/启停）已有多轮真机证据，当前重点转向“联网同步一致性”。
- 群组/共享链已完成多阶段 RTK 化与状态一致性收口，当前重点转向“端到端真实可用性”。

## 5. 接下来 3 个执行动作（禁止空转）
1. 文档瘦身闭环：主文档与 GAP 台账只保留“当前状态 + 活跃项 + 最近结论”，超长流水迁移归档。  
2. 功能审查闭环：以 GAP 为单位逐条推进，不再做“仅新增 round 编号”的低价值记录。  
3. 真机闭环：优先补共享链与联网同步链的可视证据，形成“可上线/不可上线”的可复核结论。
4. 页面级闭环：每轮必须在页面矩阵中推进至少 1 个页面子功能验收，并更新子批次记录。

## 6. 归档策略
- 本文件只保留控制面板信息，历史长流水统一迁移到 `docs/archive/生产上线整改总控-历史归档.md`。
- GAP 台账仅保留活跃与关键完成项，历史明细迁移到 `docs/archive/DOC_GAP_TRACKER-历史归档.md`。

## 7. 今日执行日志（精简）
| 日期 | 动作 | 结果 | 风险/下一步 |
|---|---|---|---|
| 2026-05-15 | 执行文档瘦身重构（主入口） | 已将总控文档改为精简控制面板，移除超长流水入口 | 下一步同步压缩 GAP 台账并提交推送 |
| 2026-05-15 | 建立页面能力矩阵与子批次执行记录，并新增 UTF-8 编码检查脚本 | 已形成逐界面/逐功能/逐子功能执行入口，支持并行推进与证据闭环 | 下一步进入 round65，按 10 页批次执行功能+UI+真机验证 |
| 2026-05-15 | round65 首轮：Profile 页面阻断交互收口 | 已将头像更新与资料保存从阻断式弹窗改为页内状态提示，并补 `state.profile.inlineStatus` 锚点，降低流程中断与重复点击风险 | 下一步继续同批次推进 Community 与 Reminder 的页内状态一致性与毛玻璃统一复核 |
| 2026-05-15 | round65 次轮：Community 分类筛选可测性与玻璃层级收口 | 已补齐 `filter.community.*` 分类 testID 并统一分类区轻毛玻璃边界样式，提升真机点击证据稳定性与视觉一致性 | 下一步补 round65 的 Community 真机截图/XML，并推进 Reminder 同步状态链复核 |
